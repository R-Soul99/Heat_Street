---
phase: 04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der
plan: 10
type: execute
wave: 5
depends_on: ["04.1-09"]
files_modified:
  - src/core/road-geometry.ts
  - src/core/crest-geometry.ts
  - tools/map-compiler/author/heightfield.ts
  - public/maps/juliette-ga.map.json
  - public/maps/juliette-ga.glb
  - public/maps/juliette-ga.collision.json
autonomous: false
requirements: [D-06, D-07, D-02a, D-04]

must_haves:
  truths:
    - "A human has driven every road in the recompiled area and signed off that no road floats and no shoulder skirt reads as ridiculous (D-06)"
    - "Surface-type behaviour, junction geometry, building collision and frame budget are confirmed unregressed on the regenerated geometry — OSM surface=* tagging is unaffected by this phase's elevation changes (D-07)"
    - "Every tunable this phase introduced has been judged against the real compiled area, not only against unit tests"
  artifacts:
    - path: "public/maps/juliette-ga.map.json"
      provides: "the signed-off compiled area (recompiled if the session forced a retune)"
    - path: "tools/map-compiler/author/heightfield.ts"
      provides: "relief constants as judged by the session"
  key_links:
    - from: "the D-06 session findings"
      to: "src/core/road-geometry.ts / tools/map-compiler/author/heightfield.ts"
      via: "retune-and-recompile, the same loop plans 04-10 and 04-11 used"
      pattern: "ASSUMED"
---

<objective>
Run the full D-06 drive-every-road human sign-off session against the recompiled flat-terrain
area, at the same bar as Phase 4's own SC1 completion, and land any retunes the session forces.

Purpose: D-06 is explicit — flat terrain removes the floating-road/skirt failure MODE, but the
session must still confirm nothing else regressed on regenerated geometry (surface-type
behaviour, junction geometry, building collision, frame budget). Every constant this phase
introduced is an `[ASSUMED]` reasoned default that CONTEXT.md authorises retuning empirically
against the real compiled area, exactly as `SMOOTHING_WINDOW` and `HEIGHTFIELD_SINK_M` were
retuned in Phase 4.

Output: a human sign-off record, plus any retuned constants with their measured effect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-CONTEXT.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-09-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Run the game:   npm run dev      (then open the printed localhost URL)
Debug panel:    append ?debug to the URL; Backquote toggles it (src/debug/debug-gate.ts)
                lil-gui tuning panel + stats-gl frame overlay are gated behind it
Recompile:      npm run compile-map -- --area juliette-ga
Full gate:      npm run check

Tunables this phase introduced, all tagged [ASSUMED] and in scope for retuning here:
  src/core/road-geometry.ts              TARGET_SHOULDER_WIDTH_M = 6
  tools/map-compiler/author/heightfield.ts  RELIEF_AMPLITUDE_M = 2   (D-02a, developer-chosen over +/-5m)
                                            RELIEF_FALLOFF_DISTANCE_M = 130
                                            RELIEF_FEATURE_SIZE_M = 220 / RELIEF_COARSE_FEATURE_SIZE_M = 500
                                            HEIGHTFIELD_SINK_M = 0.1
  src/core/crest-geometry.ts             JULIETTE_GA_CRESTS radii/heights (3 crests)

Authored crest locations to drive to (local ENU metres, from src/core/crest-geometry.ts):
  juliette-north-ridge       centre (-779.7,  263.2)  radius 45m  height 9m
  juliette-main-street-rise  centre (-420.0,  102.2)  radius 30m  height 5m
  juliette-south-approach    centre (-1227.2, -1062.3) radius 28m height 6m

Known carry-forward open items that are NOT this session's job to fix (from STATE.md), but
SHOULD be observed and reported if they present differently on flat terrain:
  - oblique-angle junction surface bug (gravel rendering through tarmac at node 45)
  - road width possibly too narrow ("quite difficult to stay on the roads at speed") — explicitly
    left pending a re-drive now that the floating-road defect is gone
  - the ADR 0003 occlusion fade/steepen/off comparison, still never fairly run
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-06 drive-every-road SC1 sign-off session</name>
  <what-built>
Phase 04.1 replaced DEM-derived elevation with flat roads (every node and edge point at y=0), a
capped +/-2m procedural off-road relief that fades to exactly zero at every paved edge, a
6m shoulder verge (down from 20m), a 0.1m heightfield sink (down from 4.5m), and three authored
off-road terrain crests. The Juliette, GA area has been recompiled and every automated gate is
green: 0 terrain-above-road violations, shoulder grade p95 under 8 degrees and max under
15 degrees (against the DEM era's 12.9 degree median and 26.9 degree p95), relief within its
+/-2m cap, and two consecutive compiles byte-identical.
  </what-built>
  <read_first>
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-09-SUMMARY.md (the measurement baseline and the full compile log — the numbers this session is judging against)
    - .planning/STATE.md (the two Phase 04-11 entries describing the exact defects this phase set out to fix, and the four `[Phase 04-11, open]` carry-forward items)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-CONTEXT.md (D-06's explicit "same bar as Phase 4's own completion, not just an automated-test-and-spot-check pass")
  </read_first>
  <action>
Operator task, not a code change. Start the dev server, open the game with `?debug`, and walk the
10-step protocol in `<how-to-verify>` below in order, recording PASS / PASS-WITH-NOTE / FAIL plus
a one-line reason for each step. Do not edit any source file from this task — every finding is
carried into Task 2, which owns the retune scope. Capture the verdict list verbatim into the plan
SUMMARY; a summarised "all good" does not satisfy D-06's "same bar as Phase 4's own completion"
requirement.
  </action>
  <how-to-verify>
Start the dev server with `npm run dev` and open the printed URL with `?debug` appended.

Work through every step and record a verdict for each. Do not batch-approve.

1. **Drive every road segment in the area.** Confirm no road floats above the terrain anywhere,
   and that leaving the road at any point lets the car drive back on without getting stuck.
   This is the literal SC1 bar from Phase 04-11.
2. **Judge the shoulder verge.** The ramp is now 6m wide per side and nearly flat. Confirm it no
   longer reads as "ridiculous... 20m extra road width." Note specifically whether 6m of
   ROAD-COLOURED verge still reads as extra road width — shoulders render with the road's own
   surface material, so if it does, the fix is either a further width reduction or reassigning
   the shoulder surface, and this session is where that call gets made.
3. **Off-road relief.** Drive well away from the road network. Confirm the terrain has visible
   rolling texture rather than reading as a flat plane, and that +/-2m reads as the "noticeably
   subtler" option the developer chose over +/-5m rather than as too subtle to notice. If it
   reads wrong in either direction, say which and by roughly how much.
4. **Relief at the road edge.** Drive slowly off the pavement at several points on wide (primary)
   and narrow (residential/track) roads. Confirm there is no lip, step or bump where the terrain
   meets the shoulder, and no jitter or vibration of the car sitting just off the pavement
   (the coincident-collider failure mode the 0.1m sink exists to prevent).
5. **Authored crests.** Drive to each of the three crest locations listed in this plan's
   `<interfaces>` block. For each: confirm it is visible and solid, that its base blends into the
   terrain with no cliff or seam, and that approaching it at speed produces a real "launch over
   the hill" moment. Report the approximate speed at which the car leaves the ground.
6. **Surface types (D-07 check).** Drive tarmac, gravel and dirt-road segments. D-07 predicts
   nothing about grip or FX should change from this phase. If anything feels different from the
   Phase 3 sign-off, that is a GENUINE NEW FINDING, not an expected consequence — flag it as such.
7. **Junction geometry.** Cross several junctions, including a mixed-surface one. Confirm the
   fan geometry reads correctly and there is no new step or seam at junction boundaries. Also
   report whether the known oblique-angle gravel-through-tarmac artifact at node 45 still
   presents on flat terrain.
8. **Building collision.** Drive into several buildings, including ones on relief-textured
   ground away from the road. Confirm you cannot pass under or through any building (plan 04-11
   fixed this by extending building colliders down to the local terrain, and flat terrain should
   make it strictly easier).
9. **Frame budget.** With the `?debug` stats overlay visible, drive a busy stretch and record
   frame time, draw calls, triangle count and body count. Compare against
   `docs/frame-budget.md`'s targets and Phase 3's recorded reading (16.68ms, 57 draws,
   692 tris in the test scene).
10. **Road width re-judgement.** STATE.md's open item "quite difficult to stay on the roads at
    speed" was deliberately left pending exactly this re-drive. Now that the floating-road defect
    is gone, report whether road width still feels wrong. Do NOT change `LANE_WIDTH_M` or
    `DEFAULT_LANES_BY_CLASS` in this session — that is a separate decision with its own blast
    radius; just record the verdict.

For every step, record PASS, PASS-WITH-NOTE, or FAIL plus a one-line reason.
  </how-to-verify>
  <acceptance_criteria>
    - All 10 steps have an explicitly recorded verdict (PASS / PASS-WITH-NOTE / FAIL) with a reason
    - Steps 1, 2, 4, 5 and 8 are PASS or PASS-WITH-NOTE — a FAIL on any of these blocks the phase and routes to Task 2
    - Step 5 records an approximate launch speed for each of the three named crests
    - Step 9 records concrete numbers for frame time, draw calls, triangles and body count
    - The full step-by-step verdict list is captured in the plan SUMMARY, not summarised as "all good"
  </acceptance_criteria>
  <resume-signal>Type "approved" if every step passed, or list the failing/noted steps with what you saw</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Land the session's findings — retune, recompile, re-gate</name>
  <files>src/core/road-geometry.ts, src/core/crest-geometry.ts, tools/map-compiler/author/heightfield.ts, public/maps/juliette-ga.map.json, public/maps/juliette-ga.glb, public/maps/juliette-ga.collision.json</files>
  <read_first>
    - The Task 1 verdict list captured in this plan's SUMMARY-in-progress
    - src/core/road-geometry.ts (the `TARGET_SHOULDER_WIDTH_M` doc comment's revision-history convention — any change here extends it, never replaces it)
    - tools/map-compiler/author/heightfield.ts (`HEIGHTFIELD_SINK_M`'s multi-revision comment — the "measured-then-revised, with the measurement cited" style every retune must follow)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-09-SUMMARY.md (the measurement baseline any retune must be re-measured against)
  </read_first>
  <action>
Apply every FAIL and every actionable PASS-WITH-NOTE from Task 1.

Scope of what may change here (and nothing else):
- `TARGET_SHOULDER_WIDTH_M` in `src/core/road-geometry.ts` — if the verge still reads as extra
  road width (step 2).
- `RELIEF_AMPLITUDE_M`, `RELIEF_FALLOFF_DISTANCE_M`, `RELIEF_FEATURE_SIZE_M`,
  `RELIEF_COARSE_FEATURE_SIZE_M` in `tools/map-compiler/author/heightfield.ts` — if off-road
  relief reads too subtle or too strong (step 3). CONTEXT.md explicitly authorises retuning
  D-02a's +/-2m against the real compiled area; it does NOT authorise abandoning the cap.
- `HEIGHTFIELD_SINK_M` — only if step 4 found jitter or a visible step at the pavement edge.
- The three entries in `JULIETTE_GA_CRESTS` (radius and height only, never centre coordinates
  without re-measuring paved-edge clearance) — if step 5 found a crest unlaunchable, too violent,
  or seamed at its base.

Hard constraints on any retune:
- Extend, never replace, the constant's existing doc comment; record what was driven, what was
  observed, the old value, the new value and why, in this repo's established style.
- `MIN_SHOULDER_WIDTH_M <= TARGET_SHOULDER_WIDTH_M` must still hold.
- `RELIEF_FALLOFF_DISTANCE_M` must stay at least 3x the grid spacing (~43m), i.e. at least ~129.
- `HEIGHTFIELD_SINK_M` must stay strictly greater than 0.
- Any crest radius change must keep at least 5m of clearance to the nearest paved edge; re-measure
  it against the real artifact and update `tests/crest-geometry.test.ts`'s fixture distances if
  a centre moves.
- Do NOT change `LANE_WIDTH_M` or `DEFAULT_LANES_BY_CLASS` (step 10 is a record-only verdict),
  and do NOT change any surface grip or FX value (step 6's D-07 findings are recorded as new
  findings for a later phase, not fixed here).

After any change: recompile with `npm run compile-map -- --area juliette-ga`, re-run the
measurement script from plan 04.1-09 Task 3 and confirm every threshold still passes, re-run
`npm run check`, and confirm two consecutive compiles are still byte-identical.

If Task 1 produced no FAIL and no actionable note, make no code change and record explicitly in
the SUMMARY that the session passed first-pass with zero retunes — the same way plan 02-08's
checkpoint result was recorded. Do not invent a cosmetic change to justify the task.

Record every finding that is NOT fixed here (the junction artifact, road width, occlusion
comparison, any D-07 surface surprise) as a carry-forward entry for STATE.md's Blockers/Concerns
section, tagged `[Phase 04.1, open]`, with enough detail to act on later.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npm run check && node -e "const g=JSON.parse(require('fs').readFileSync('public/maps/juliette-ga.map.json','utf8'));for(const n of g.nodes)if(n.y!==0)throw new Error('node y');for(const e of g.edges)for(const p of e.points)if(p[1]!==0)throw new Error('point y');const c=require('./public/maps/juliette-ga.collision.json');if(!(c.heightfield.sinkM>0))throw new Error('sink must stay > 0');console.log('post-session invariants OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - The post-session invariant command prints `post-session invariants OK`
    - If any constant changed: its doc comment contains the old value, the new value and what was driven to justify it, and `npm run compile-map -- --area juliette-ga` was re-run with the measurement thresholds re-verified and recorded
    - If nothing changed: the SUMMARY states explicitly that the session passed first-pass with zero retunes
    - Every unfixed finding is written up as a `[Phase 04.1, open]` carry-forward entry in the SUMMARY, ready for STATE.md
    - `grep -c "LANE_WIDTH_M" src/core/road-geometry.ts` is unchanged from before this plan (road width was not touched)
  </acceptance_criteria>
  <done>Every session finding is either landed with a documented, re-measured retune or written up as an explicit carry-forward; the full gate is green and the compiled area is the signed-off one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human judgement → shipped constants | The session is authorised to change `[ASSUMED]` tunables only, within the hard constraints listed in Task 2 |
| regenerated artifacts → runtime parsers | Unchanged; any recompile re-runs the compiler's own `parseRoadGraph` / `parseMapCollision` self-checks before writing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-27 | Tampering | a feel-session retune silently breaking a locked decision | mitigate | Task 2 lists hard constraints per constant (`MIN <= TARGET`, falloff at least 3x grid spacing, sink strictly > 0, 5m crest clearance) and the automated verify re-asserts exact road flatness and a positive sink after any change |
| T-04.1-28 | Repudiation | scope creep during a broad hands-on session | mitigate | Task 2 enumerates exactly which files and constants may change and explicitly forbids road-width and surface-grip edits, routing those findings to carry-forward entries instead |
| T-04.1-29 | Denial of Service | a retune inflating geometry past the frame budget | mitigate | Step 9 records real frame/draw/triangle numbers, and any recompile re-runs plan 04.1-09's measurement gates including the `.glb` triangle inventory |
| T-04.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. Any executor adding a package must STOP and run the Package Legitimacy Gate with a blocking human checkpoint |
</threat_model>

<verification>
1. All 10 session steps carry an explicit recorded verdict; steps 1, 2, 4, 5 and 8 are not FAIL.
2. `npm run check` — green.
3. If anything was retuned: `npm run compile-map -- --area juliette-ga` re-run, plan 04.1-09's measurement thresholds re-verified, two consecutive compiles byte-identical.
4. Unfixed findings are written up as `[Phase 04.1, open]` carry-forward entries.
</verification>

<success_criteria>
- A human has driven every road in the recompiled area and signed off at Phase 4's own SC1 bar.
- The 20m-skirt defect this phase exists to fix is confirmed gone by direct observation, not only by measurement.
- Each of the three authored crests is confirmed visible, solid, seamless at its base and launchable, with a recorded launch speed.
- Every retune is documented with what was driven and what was measured; every unfixed finding is captured as a carry-forward.
</success_criteria>

<output>
Create `.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-10-SUMMARY.md` when done
</output>
