---
phase: 04.1
slug: flatten-terrain-remove-dem-elevation-drop-real-world-dem-der
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-19
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 5.0.0 (pinned in `package.json`) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run tools/map-compiler/graph/elevation.test.ts tools/map-compiler/author/heightfield.test.ts tests/road-geometry.test.ts tests/shoulder-clearance.test.ts` |
| **Full suite command** | `npm run test` (also `npm run check` for typecheck+lint+test) |
| **Estimated runtime** | ~30 seconds (quick), ~2 minutes (full suite incl. typecheck+lint) |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run` scoped to the files touched by that task (elevation, heightfield, road-geometry, shoulder-clearance, collision, gltf, cli as applicable)
- **After every plan wave:** Run `npm run check` (full typecheck + lint + full suite)
- **Before `/gsd-verify-work`:** Full suite must be green AND D-06's full human drive-every-road SC1-equivalent re-verification session must be completed
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | 0 | D-01 | — | Every compiled node/edge point has `y === 0` (except authored D-04 overrides) | unit | `npx vitest run tools/map-compiler/graph/elevation.test.ts` | ⚠️ W0 — needs near-total rewrite | ⬜ pending |
| TBD | TBD | 0 | D-02/D-02a/D-02b | — | Off-road relief bounded to `[-2, 2]`, reaches exactly `0` at every point on the shoulder's outer boundary | unit | `npx vitest run tools/map-compiler/author/heightfield.test.ts` | ⚠️ W0 — needs rewrite | ⬜ pending |
| TBD | TBD | 1 | D-01 + D-02b (shoulder gap eliminated) | T-04-01 (schema parse integrity, unaffected) | `buildRoadShoulders`'s outer rail height equals (or is within the new small sink margin of) the paved rail height everywhere | unit | `npx vitest run tests/road-geometry.test.ts` | ✅ exists — `grounding fix` describe block needs re-tuned assertions | ⬜ pending |
| TBD | TBD | 1 | Building-clearance still valid at new widths | — | `resolvePointShoulderWidth` still clamps correctly at the new `TARGET_SHOULDER_WIDTH_M` | unit | `npx vitest run tests/shoulder-clearance.test.ts` | ✅ exists — needs new constant values threaded through | ⬜ pending |
| TBD | TBD | 1 | Compiled artifact shape unchanged | T-04-01 (prototype-pollution-safe parse, unaffected) | Schema self-check still passes on regenerated `.map.json`/`.collision.json` | unit | `npx vitest run tests/road-graph-schema.test.ts` | ✅ exists, no rewrite needed | ⬜ pending |
| TBD | TBD | 1 | D-04 authored crests present in compiled data | — | Node/edge y values match the hard-coded override table exactly | unit | `npx vitest run tools/map-compiler/graph/elevation.test.ts` (new describe block) | ⚠️ new test case needed | ⬜ pending |
| TBD | TBD | 2 | Orphaned `geotiff` dependency removed cleanly | — | `npm run check` stays green after `package.json`/`dem.ts` removal | integration | `npm run check` | ✅ existing gate, no new file needed | ⬜ pending |
| TBD | TBD | 2 | D-06 (full human re-verification) | — | Drive every road in the recompiled area; confirm no regression in surface behavior, junction geometry, building collision, frame budget | manual, SC1-equivalent | N/A — human browser session, same protocol as Phase 04-11 | manual-only, by design | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs, plan IDs, and wave assignments above are placeholders (TBD) — the planner fills these in against the actual PLAN.md task breakdown; the requirement→test mapping and file-existence assessment come directly from RESEARCH.md's Validation Architecture section and must not be re-derived from scratch.*

---

## Wave 0 Requirements

- [ ] `tools/map-compiler/graph/elevation.test.ts` — near-total rewrite: every existing test asserts DEM-smoothing/densification behavior (`SMOOTHING_WINDOW`, `densifyEdgePoints`, `arcLengthMovingAverage`) that no longer exists once that machinery is deleted per D-01/D-03
- [ ] `tools/map-compiler/author/heightfield.test.ts` — rewrite for the new relief generator (bounded amplitude, distance-to-road falloff reaching exactly 0)
- [ ] `tools/map-compiler/sources/dem.test.ts` — DELETE alongside `dem.ts`
- [ ] New test file or describe block for the distance-to-road/falloff pure function, if extracted as its own named export (recommended — genuinely new geometry math not covered by any existing test)
- [ ] `tests/road-geometry.test.ts`'s `buildRoadShoulders — grounding fix` describe block — new assertions reflecting the near-zero-gap expectation, not the old 4.5m-sink expectation

*(Vitest itself is already fully configured — no install/config gap, only test-content gaps listed above.)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Full drive-every-road re-verification of the recompiled flat-terrain area | D-06 | Visual/feel judgment (no floating roads/skirts, surface-type behavior, junction geometry, building collision, frame budget) cannot be reduced to a unit assertion | Same protocol as Phase 04-11's SC1 human sign-off session: drive every road segment in the recompiled Juliette, GA area in-browser; confirm no floating-road/skirt artifacts and no regressions vs. Phase 4's baseline |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING/rewrite-needed references (elevation.test.ts, heightfield.test.ts, dem.test.ts deletion, road-geometry.test.ts grounding-fix block)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
