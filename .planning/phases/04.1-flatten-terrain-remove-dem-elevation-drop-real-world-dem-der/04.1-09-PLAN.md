---
phase: 04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der
plan: 09
type: execute
wave: 4
depends_on: ["04.1-08"]
files_modified:
  - public/maps/juliette-ga.map.json
  - public/maps/juliette-ga.glb
  - public/maps/juliette-ga.collision.json
  - tests/compiled-map.test.ts
autonomous: true
requirements: [D-01, D-02, D-02a, D-02b, D-04, D-07]

must_haves:
  truths:
    - "The shipped Juliette, GA area is compiled from flat, hand-authored terrain"
    - "No point of the shipped terrain rises above the road surface anywhere"
    - "Shoulder ramp grade is measurably gentle across the whole real map, not just in theory"
    - "The shipped .glb carries the three authored crests"
  artifacts:
    - path: "public/maps/juliette-ga.map.json"
      provides: "recompiled flat road graph"
      contains: "none-flat-authored"
    - path: "public/maps/juliette-ga.collision.json"
      provides: "recompiled collision sidecar with the +/-2m relief heightfield"
      contains: "heightfield"
    - path: "tests/compiled-map.test.ts"
      provides: "real-artifact assertions inverted from DEM-relief to exact flatness"
  key_links:
    - from: "tests/compiled-map.test.ts"
      to: "public/maps/juliette-ga.map.json"
      via: "?raw import + parseRoadGraph against the real shipped artifact"
      pattern: "juliette-ga.map.json"
---

<objective>
Recompile the reference area from the flat-terrain pipeline, invert the real-artifact test suite
from "assert real DEM relief exists" to "assert exact flatness", and measure the geometry the
D-06 human session will judge.

Purpose: every prior plan in this phase is unit-tested in isolation. This is where the actual
shipped artifact is regenerated and the phase's headline claims — no floating roads, no
ridiculous skirts, terrain never above the road — become measured numbers rather than arguments.

Output: three regenerated artifacts, an updated real-artifact suite, and a recorded measurement
baseline for plan 04.1-10's human session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-CONTEXT.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-RESEARCH.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-08-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Compile command:   npm run compile-map -- --area juliette-ga
Outputs:           public/maps/juliette-ga.map.json, .glb, .collision.json

DEM-era baseline being replaced (measured 2026-09-19 from the pre-phase artifact):
  node y 110.1m - 148.9m overall (38.8m relief); within edges 8/29/34, 128.1m - 154.1m
  compilerVersion "0.5.0"
  attribution.dem "U.S. Geological Survey 3D Elevation Program (public domain)"
  average road-to-terrain gap 5.09m; shoulder ramp median 12.9 deg, p95 26.9 deg (plan 04-11)
  compiled bounds minX -3759.53, minZ -2474.21, maxX 1744.63, maxZ 3182.55

Post-phase expected values:
  every node.y and every edge point y === 0 exactly
  source.compilerVersion "0.6.0"
  source.demSource "none-flat-authored"
  attribution.dem "Terrain elevation is flat and hand-authored — no DEM data is used (see docs/adr/0001-map-data-source.md)"
  collision.heightfield.sinkM === 0.1; every stored height within [-2.1, 1.9]

tests/compiled-map.test.ts assertions that MUST change (current line numbers):
   66  attribution.dem                              -> new sentence
  102  "at least 10 distinct node y values"         -> exactly 1 distinct value, 0
  105  "graph.nodes.some((n) => n.y !== 0)"         -> every n.y === 0
  108  "at least 10m relief, 50-250m band"          -> relief exactly 0
  137  "no edge gradient exceeds 0.5"               -> every edge gradient exactly 0
  154  compilerVersion "0.5.0"                      -> "0.6.0"
Assertions that stay valid unchanged: schema conformance, dense contiguous ids, endpoint/node
coordinate match, closed surface enum, ISO-8601 snapshot, bounds containment, >=2 surfaces,
>=20 edges, >=3 junctions, cycle presence, exact endpoint-Y-to-node-Y equality, validateGraph
pass and its paired negative case.

src/core/heightfield-sample.ts:
  export function sampleHeightfieldBilinear(grid, x, z): number   // used by the measurement script
src/core/road-geometry.ts:
  export const TARGET_SHOULDER_WIDTH_M   // 6 after plan 04.1-02
tools/map-compiler/author/heightfield.ts:
  export const RELIEF_AMPLITUDE_M, HEIGHTFIELD_SINK_M
src/core/crest-geometry.ts:
  export function crestsForArea(areaId: string): readonly AuthoredCrest[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompile the area and verify reproducibility</name>
  <files>public/maps/juliette-ga.map.json, public/maps/juliette-ga.glb, public/maps/juliette-ga.collision.json</files>
  <read_first>
    - tools/map-compiler/cli.ts (as rewired in plan 04.1-08 — so the executor knows which summary lines to expect and capture)
    - .planning/STATE.md (the two Phase 04-11 entries recording the prior compile's measured numbers this compile is compared against)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-RESEARCH.md ("Runtime State Inventory" — the artifacts are DESTROYED, not migrated, by a recompile)
  </read_first>
  <action>
Run `npm run compile-map -- --area juliette-ga` from the repository root.

Capture the FULL console output verbatim into the plan SUMMARY — every stage summary line (fetch,
build report, flat elevation report, validation, heightfield, collision, glTF including the new
crest counts). These printed numbers are the phase's primary record and must not be paraphrased.

Assert from that output before proceeding:
- the flat elevation report prints `maxAbsNodeY` and `maxAbsPointY` as `0.000` and emits no
  non-zero-y warning;
- validation reports PASSED with 0 failures;
- the heightfield summary's height range lies within `[-2.1, 1.9]` metres and reports
  `sink 0.1m applied`;
- the glTF summary reports 3 crests with a non-zero triangle count;
- no line anywhere mentions DEM, 3DEP, Copernicus or a `.tif`.

If validation aborts, do NOT loosen the gate — diagnose and report. A failing map is never
written, so the previous artifacts stay in place and the repository is not left half-migrated.

Then run the SAME command a second time and confirm reproducibility: `.map.json` and
`.collision.json` must be byte-identical between runs and the `.glb` must have an identical byte
length. Record the comparison method and result in the SUMMARY. A difference means
non-determinism leaked into the relief generator (RESEARCH.md's `Math.random` /
`Math.sin`-hash warning) and must be fixed before continuing.

Stage all three regenerated artifacts. Do not hand-edit any of them.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npm run compile-map -- --area juliette-ga &amp;&amp; node -e "const g=JSON.parse(require('fs').readFileSync('public/maps/juliette-ga.map.json','utf8'));const ys=new Set(g.nodes.map(n=>n.y));if(ys.size!==1||!ys.has(0))throw new Error('nodes not flat');for(const e of g.edges)for(const p of e.points)if(p[1]!==0)throw new Error('edge '+e.id+' non-zero point y');if(g.source.compilerVersion!=='0.6.0')throw new Error('compilerVersion '+g.source.compilerVersion);if(g.source.demSource!=='none-flat-authored')throw new Error('demSource '+g.source.demSource);console.log('flat OK',g.nodes.length,'nodes',g.edges.length,'edges')"</automated>
  </verify>
  <acceptance_criteria>
    - The compile exits 0 and prints `validation: PASSED — 0 failures`
    - The one-liner verify command prints `flat OK` (every node y and every edge point y is exactly 0, compilerVersion 0.6.0, demSource none-flat-authored)
    - Two consecutive compiles produce byte-identical `public/maps/juliette-ga.map.json` and `public/maps/juliette-ga.collision.json`
    - `grep -ci "3dep\|copernicus\|usgs" public/maps/juliette-ga.map.json` returns 0
    - `node -e "const c=require('./public/maps/juliette-ga.collision.json');if(c.heightfield.sinkM!==0.1)throw new Error('sink '+c.heightfield.sinkM);const hs=c.heightfield.heights;const mn=Math.min(...hs),mx=Math.max(...hs);if(mn< -2.1001||mx>1.9001)throw new Error('relief out of cap: '+mn+' '+mx);console.log('relief OK',mn.toFixed(3),mx.toFixed(3))"` prints `relief OK`
    - The full compile output is pasted verbatim into the plan SUMMARY
  </acceptance_criteria>
  <done>All three artifacts are regenerated from the flat pipeline, validated, within the relief cap, reproducible byte-for-byte on rerun, and the compile log is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Invert tests/compiled-map.test.ts to the flat-terrain contract</name>
  <files>tests/compiled-map.test.ts</files>
  <read_first>
    - tests/compiled-map.test.ts (the whole file — in particular the elevation describe block around lines 100-155 and the attribution test at line 62)
    - public/maps/juliette-ga.map.json (the freshly recompiled artifact from Task 1)
    - public/maps/juliette-ga.collision.json (the freshly recompiled sidecar)
    - tools/map-compiler/author/heightfield.ts (for `RELIEF_AMPLITUDE_M` and `HEIGHTFIELD_SINK_M`, which the new heightfield assertions must import rather than re-literal)
  </read_first>
  <action>
Rewrite the elevation-related assertions in `tests/compiled-map.test.ts` so the suite asserts
exact flatness instead of DEM relief. Leave every topology, schema, surface-enum, bounds,
attribution-shape and `validateGraph` test structurally untouched.

Change:
- The `attribution.dem` expectation to the exact new sentence
  `"Terrain elevation is flat and hand-authored — no DEM data is used (see docs/adr/0001-map-data-source.md)"`,
  and add `expect(graph.source.demSource).toBe("none-flat-authored")` beside it.
- `compilerVersion` from `"0.5.0"` to `"0.6.0"`, renaming the test so it names the flat-terrain
  stage set rather than plan 04-10's heightfield stage.
- The "non-zero, varying node y values" test: invert it to assert `new Set(graph.nodes.map(n => n.y)).size === 1`
  and that the single value is exactly `0`. Rewrite the test name to state the inverted
  rationale — a NON-flat map would now mean the flat-elevation stage silently no-opped or a DEM
  path was reintroduced.
- The "at least 10m relief / 50-250m plausibility band" test: replace with a relief-is-exactly-0
  assertion (max node y minus min node y `toBe(0)`).
- The gradient test: keep the per-edge gradient computation but tighten the expectation from
  `<= 0.5` to `toBe(0)` for every edge, since a flat polyline has zero rise everywhere. Keep the
  per-edge failure message naming the offending edge id.

Add a new describe block `compiled map — flat terrain (phase 04.1)` asserting against the real
artifacts:
1. Every edge point's `y` is exactly `0` (iterate all edges and all points; report the offending
   edge id and point index on failure).
2. Every edge's `lengthM` equals its own 2D XZ polyline length within `1e-6`.
3. Loading `public/maps/juliette-ga.collision.json` (via the same `?raw` import style this repo's
   real-artifact tests already use) and `parseMapCollision`: `heightfield.sinkM` equals the
   imported `HEIGHTFIELD_SINK_M`, and every stored height lies within
   `[-(RELIEF_AMPLITUDE_M + HEIGHTFIELD_SINK_M) - 1e-3, RELIEF_AMPLITUDE_M - HEIGHTFIELD_SINK_M + 1e-3]`
   using the imported constants, never literals.
4. Terrain-never-above-road, the phase's headline claim, asserted against real data: for every
   point of every edge, `sampleHeightfieldBilinear(heightfield, x, z) <= 1e-6`. Report the worst
   violation's edge id, point index and height on failure. The DEM-era artifact had 123 such
   violations before plan 04-11's densification fix and a worst case of 3.25m — name that in the
   test so the regression this guards against is legible.
5. Shoulder-grade bound on real geometry: for every point of every edge, sample the terrain at
   `TARGET_SHOULDER_WIDTH_M` metres perpendicular to the centreline on BOTH sides (reuse the
   segment-tangent/perpendicular math style already in this repo's geometry tests), compute
   `Math.abs(terrainY) / TARGET_SHOULDER_WIDTH_M`, and assert the maximum is below
   `Math.tan((15 * Math.PI) / 180)`. Also assert the p95 is below `Math.tan((8 * Math.PI) / 180)`.
   Print the median, p95 and max in the failure message so the numbers land in CI output even
   when green is close.
6. Authored crests are present and placed: `crestsForArea("juliette-ga")` returns 3 entries, and
   for each, the minimum distance from its `(centerX, centerZ)` to any edge's paved edge
   (centreline distance minus that edge's `widthM / 2`, computed over the real artifact) exceeds
   its own `radiusM` by at least `5` metres — the real-data version of plan 04.1-03's fixture
   clearance test, and the guard that D-01's flat roads are untouched by D-04b's crests.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npx vitest run tests/compiled-map.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/compiled-map.test.ts` exits 0 with 0 failures
    - `grep -c "flat terrain (phase 04.1)" tests/compiled-map.test.ts` returns 1
    - `grep -c "0.6.0" tests/compiled-map.test.ts` returns at least 1 and `grep -c "0.5.0" tests/compiled-map.test.ts` returns 0
    - `grep -c "RELIEF_AMPLITUDE_M\|HEIGHTFIELD_SINK_M\|TARGET_SHOULDER_WIDTH_M" tests/compiled-map.test.ts` returns at least 3 (constants are imported, not re-literalled)
    - `grep -c "3.25" tests/compiled-map.test.ts` returns at least 1 (the DEM-era terrain-above-road regression is named in the test)
    - Temporarily editing one node's `y` to `1` in the artifact makes at least 3 tests fail (verify, then restore by recompiling)
  </acceptance_criteria>
  <done>The real-artifact suite asserts exact flatness, the relief cap, terrain-never-above-road, a sub-15-degree shoulder grade across the whole map, and authored-crest clearance — all against the regenerated artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measurement baseline for the D-06 session</name>
  <files>public/maps/juliette-ga.map.json</files>
  <read_first>
    - tests/compiled-map.test.ts (as written in Task 2 — the measurement script reuses the same sampling approach, so the two cannot disagree)
    - .planning/STATE.md (the Phase 04-11 entries recording median 12.9 deg / p95 26.9 deg / average 5.09m gap — the before-column of the comparison table this task produces)
    - src/core/crest-geometry.ts (crest ids, centres, radii and heights to report)
  </read_first>
  <action>
Write a throwaway measurement script into the session scratchpad directory (NOT into the
repository — this is a one-off report generator, not shipped code) that loads
`public/maps/juliette-ga.map.json` and `public/maps/juliette-ga.collision.json` and prints:

1. Node/edge/point counts; min, max and distinct count of node `y` and of edge-point `y`.
2. Terrain-above-road: count and worst case of points where the bilinearly sampled heightfield
   height exceeds `0`.
3. Shoulder-grade distribution: for every edge point, both perpendicular offsets at
   `TARGET_SHOULDER_WIDTH_M`, computing `atan(|terrainY| / TARGET_SHOULDER_WIDTH_M)` in degrees —
   report min, median, p95, max and the edge id of the worst case.
4. Off-road relief distribution across the whole heightfield: min, max, mean and standard
   deviation of the stored heights, plus the count of grid cells whose height differs from
   `-HEIGHTFIELD_SINK_M` by more than `0.25m` (a non-triviality signal — if this is near zero the
   terrain is effectively a flat plane, which D-02 explicitly rejects).
5. Per-crest report: id, centre, radius, height, the measured distance from the centre to the
   nearest paved edge, and the nearest building edge distance.
6. A `.glb` inventory read with `@gltf-transform/core`'s `NodeIO` (already a project dependency):
   the list of mesh names present, confirming `crests`, `terrain`, `buildings` and at least one
   `roads-<surface>` mesh all exist, plus the total triangle count.

Put the script's full output into the plan SUMMARY as a before/after comparison table against
the DEM-era numbers recorded in STATE.md (average gap 5.09m, median ramp 12.9 deg, p95 26.9 deg,
123 terrain-above-road violations pre-densification).

Gate the task on these thresholds — if any fails, report it rather than adjusting the threshold:
- terrain-above-road violations: `0`
- shoulder grade p95: under `8` degrees
- shoulder grade max: under `15` degrees
- off-road relief cells differing from `-HEIGHTFIELD_SINK_M` by more than `0.25m`: at least 25%
  of all cells (proves the relief generator is producing real texture, not a flat plane)
- `.glb` contains a mesh named `crests`

If the relief non-triviality threshold fails, the correct response is to retune
`RELIEF_AMPLITUDE_M` / `RELIEF_FEATURE_SIZE_M` in `tools/map-compiler/author/heightfield.ts` and
recompile — CONTEXT.md explicitly authorises empirical retuning of these reasoned defaults — not
to lower the threshold. Record any retune and its measured effect in the SUMMARY.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npx vitest run tests/compiled-map.test.ts tests/map-scene.test.ts tests/road-graph-schema.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - The measurement script runs and its full output is pasted into the plan SUMMARY
    - Terrain-above-road violation count is exactly 0
    - Shoulder-grade p95 is under 8 degrees and max is under 15 degrees, both reported as numbers in the SUMMARY
    - At least 25% of heightfield cells differ from `-HEIGHTFIELD_SINK_M` by more than 0.25m
    - The `.glb` mesh-name inventory includes `crests`, `terrain` and `buildings`
    - `npm run check` exits 0
    - The SUMMARY contains a before/after table citing 5.09m / 12.9 deg / 26.9 deg as the DEM-era column
  </acceptance_criteria>
  <done>A recorded, numeric before/after comparison exists for the D-06 session to reason against, and every gate threshold is met without loosening.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated `.map.json` / `.collision.json` → runtime `parseRoadGraph` / `parseMapCollision` | The shipped artifacts crossing into the game; the compiler self-checks both before writing |
| Overpass cache → `buildGraph` | Retained and unchanged; roads and buildings still come from OSM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-23 | Tampering | hand-edited compiled artifact | mitigate | Artifacts are produced only by `npm run compile-map`; the task forbids hand-editing and the two-run byte-identity check would expose any manual edit as a diff on the next compile |
| T-04.1-24 | Tampering | prototype pollution via a malformed compiled artifact | mitigate | Unaffected and re-proven: `tests/compiled-map.test.ts` re-runs `parseRoadGraph` against the real regenerated file, and `parseMapCollision` is exercised by the new heightfield assertions — the field-by-field reconstruction (T-04-01) is confirmed still to be the only parse path |
| T-04.1-25 | Repudiation | a green suite that no longer proves anything (assertions weakened to fit new data) | mitigate | Each inverted assertion is TIGHTENED, not loosened (`>= 10 distinct y` becomes `exactly 1`; `gradient <= 0.5` becomes `=== 0`), and Task 2's acceptance criteria require that corrupting one node's `y` breaks at least 3 tests |
| T-04.1-26 | Denial of Service | crest/terrain triangle growth blowing the frame budget | mitigate | Task 3's `.glb` inventory reports the total triangle count against `docs/frame-budget.md`'s ~45,000 figure, so growth is measured rather than discovered in play |
| T-04.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. The measurement script uses only `@gltf-transform/core`, already a pinned project dependency. Any new package requires STOPPING and running the Package Legitimacy Gate with a blocking human checkpoint |
</threat_model>

<verification>
1. `npm run compile-map -- --area juliette-ga` — exits 0, validation PASSED, no DEM/3DEP/Copernicus/.tif in the log.
2. Two consecutive compiles produce byte-identical `.map.json` and `.collision.json`.
3. `npm run check` — green.
4. The measurement script's gates all pass: 0 terrain-above-road violations, shoulder p95 under 8 degrees, max under 15 degrees, relief non-triviality over 25%, `crests` mesh present.
</verification>

<success_criteria>
- Three regenerated, reproducible, validated artifacts are staged.
- The real-artifact suite asserts exact flatness, the +/-2m relief cap, terrain-never-above-road, a sub-15-degree shoulder grade and authored-crest clearance.
- A numeric before/after comparison against the DEM-era baseline is recorded for the D-06 session.
</success_criteria>

<output>
Create `.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-09-SUMMARY.md` when done
</output>
