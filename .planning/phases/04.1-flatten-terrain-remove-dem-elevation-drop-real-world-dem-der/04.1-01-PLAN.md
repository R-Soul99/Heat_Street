---
phase: 04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/map-compiler/graph/elevation.ts
  - tools/map-compiler/graph/elevation.test.ts
autonomous: true
requirements: [D-01, D-03]

must_haves:
  truths:
    - "Every compiled road node and every edge centreline point has y exactly 0"
    - "No DEM sampler, projector, smoothing window or densification step runs during road elevation"
    - "Edge lengthM is the exact 2D (XZ) polyline length"
  artifacts:
    - path: "tools/map-compiler/graph/elevation.ts"
      provides: "applyFlatElevation + FLAT_Y, replacing applyElevation"
      exports: ["applyFlatElevation", "FLAT_Y", "FlatElevationReport"]
      contains: "export const FLAT_Y = 0"
    - path: "tools/map-compiler/graph/elevation.test.ts"
      provides: "flatness unit tests replacing the DEM-smoothing suite"
  key_links:
    - from: "tools/map-compiler/graph/elevation.ts"
      to: "src/core/road-graph.ts"
      via: "type-only import of RoadGraph/RoadGraphEdge/RoadGraphNode"
      pattern: "import type \\{[^}]*RoadGraph"
---

<objective>
Replace the DEM-sampling/smoothing/densifying road-elevation compiler stage with a
constant-zero flat-elevation pass (D-01, D-03).

Purpose: D-01 removes the road-to-terrain height mismatch by construction rather than
by a bridging algorithm. Everything in `elevation.ts` that exists to cope with real DEM
noise (`SMOOTHING_WINDOW`, `GRADIENT_WARNING_THRESHOLD`, `MAX_SEGMENT_LENGTH_M`,
`densifyEdgePoints`, `arcLengthMovingAverage`, `smoothEdgeElevation`,
`cumulativeArcLengthXZ`, `computeMaxGradient`, `polylineLength3D`) has no job once `y`
is a constant.

Output: `applyFlatElevation(graph)` and a rewritten unit suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-CONTEXT.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

From src/core/road-graph.ts (type-only, unchanged by this plan):
  RoadGraphNode  = { id, osmNodeId, x, y, z, junction, ... }
  RoadGraphEdge  = { id, osmWayId, from, to, points: [number,number,number][], lengthM, widthM, surface, roadClass, ... }
  RoadGraph      = { schemaVersion, source, attribution, origin, bounds, nodes, edges }

Current export surface of tools/map-compiler/graph/elevation.ts (ALL of this is replaced):
  export const SMOOTHING_WINDOW = 5
  export const GRADIENT_WARNING_THRESHOLD = 0.35
  export const MAX_SEGMENT_LENGTH_M = 25
  export interface EdgeGradientReportEntry { edgeId, maxGradient }
  export interface ElevationReport { minNodeElevationM, maxNodeElevationM, reliefM, edgeGradients, steepEdges }
  export function applyElevation(graph, sampler: ElevationSampler, projector: Projector): { graph, report }

Known consumer that will NOT compile until plan 04.1-08 lands (expected, do not fix here):
  tools/map-compiler/cli.ts  — imports applyElevation + ElevationReport
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace applyElevation with applyFlatElevation</name>
  <files>tools/map-compiler/graph/elevation.ts</files>
  <read_first>
    - tools/map-compiler/graph/elevation.ts (the full current file — it is being gutted, and its header/doc-comment conventions must be carried forward)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-PATTERNS.md (section "tools/map-compiler/graph/elevation.ts" — the documented-constant convention, the pure-map-transform convention, and the throw-with-function-name-and-id error convention to copy)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-CONTEXT.md (D-01, D-03)
  </read_first>
  <action>
Rewrite `tools/map-compiler/graph/elevation.ts` so it contains a flat-baseline pass and nothing else.

Delete outright (they are dead once `y` is constant by construction, per CONTEXT.md's Claude's-Discretion note):
`SMOOTHING_WINDOW`, `GRADIENT_WARNING_THRESHOLD`, `MAX_SEGMENT_LENGTH_M`, `EdgeGradientReportEntry`,
`ElevationReport`, `cumulativeArcLengthXZ`, `arcLengthMovingAverage`, `smoothEdgeElevation`,
`densifyEdgePoints`, `polylineLength3D`, `computeMaxGradient`, `applyElevation`, and both the
`ElevationSampler` and `Projector` type imports. The only remaining import must be the type-only
import of `RoadGraph`, `RoadGraphEdge`, `RoadGraphNode` from `../../../src/core/road-graph.ts`.

Add:
- `export const FLAT_Y = 0;` with a doc comment in this file's established documented-constant
  style, stating that it is D-03's literal flat baseline (a deliberate decision, not a
  placeholder awaiting a generator), that D-01 makes it apply to every node and every edge point
  with no exception, and that D-04's authored terrain crests are OFF-ROAD terrain features owned
  by `src/core/crest-geometry.ts`, never a y-override on a paved road point.
- A private `polylineLength2D(points)` helper computing `sqrt(dx*dx + dz*dz)` over consecutive
  points, with a doc comment stating 2D length is EXACT (not an approximation) once every point
  shares one y, which is why it replaces the old `polylineLength3D`.
- `export interface FlatElevationReport` with readonly fields: `nodeCount: number`,
  `edgeCount: number`, `pointCount: number`, `maxAbsNodeY: number`, `maxAbsPointY: number`,
  `totalEdgeLengthM: number`. Doc-comment it as the regression signal that flatness actually
  held — `maxAbsNodeY` and `maxAbsPointY` must both print as 0 for a correct compile, which is
  strictly more useful than a boolean.
- `export function applyFlatElevation(graph: RoadGraph): { readonly graph: RoadGraph; readonly report: FlatElevationReport }`.

`applyFlatElevation` behaviour:
- `nodes = graph.nodes.map((node) => ({ ...node, y: FLAT_Y }))`.
- `edges = graph.edges.map((edge) => ...)` where each edge's `points` become
  `edge.points.map(([x, , z]) => [x, FLAT_Y, z])` and `lengthM` becomes
  `polylineLength2D(points)`. Preserve the existing defensive invariant check in the existing
  error-message style: if `edge.from` or `edge.to` is not present in the graph's own `nodes[]`,
  throw `applyFlatElevation: edge id=${edge.id} references node id(s) not present in the graph's own nodes[]`.
- Return `{ graph: { ...graph, nodes, edges }, report }`. Never mutate `graph` or anything
  reachable from it — carry the existing "Returns a NEW graph" invariant sentence into the new
  doc comment verbatim in meaning.

Security (T-04.1-01): construct every node and edge as an explicit object spread of the
already-parsed typed value plus named fields only. Do not introduce any `Object.assign` over
untyped input, any `JSON.parse` here, or any dynamic property write keyed by a string from
outside this function — `src/core/road-graph.ts`'s field-by-field reconstruction remains the
only parse boundary.

Rewrite the file header comment in the existing house style: what the file now does, WHY (cite
D-01 and D-03 by ID), what was deleted and why it is dead rather than merely unused, and restate
the existing layering discipline sentence (pure data transform over `RoadGraph`; no `node:fs`,
no network, no `three`, no Rapier; `tests/layering.test.ts` does not scan `tools/**` so this
file's own discipline is the only guard).

Do NOT edit `tools/map-compiler/cli.ts` from this plan — it is owned by plan 04.1-08 and will
report unresolved-import errors until then. That is expected.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npx tsc --noEmit 2>&1 | grep -v "^tools/map-compiler/cli.ts" | grep -v "^$" | grep "error TS" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const FLAT_Y = 0" tools/map-compiler/graph/elevation.ts` returns 1
    - `grep -E "SMOOTHING_WINDOW|GRADIENT_WARNING_THRESHOLD|MAX_SEGMENT_LENGTH_M|densifyEdgePoints|arcLengthMovingAverage|smoothEdgeElevation|polylineLength3D|computeMaxGradient|applyElevation\b" tools/map-compiler/graph/elevation.ts` returns no matches
    - `grep -E "ElevationSampler|Projector|sources/dem" tools/map-compiler/graph/elevation.ts` returns no matches
    - `grep -c "export function applyFlatElevation" tools/map-compiler/graph/elevation.ts` returns 1
    - `npx tsc --noEmit` reports errors ONLY in `tools/map-compiler/cli.ts` (zero errors in any other file)
  </acceptance_criteria>
  <done>`applyFlatElevation`, `FLAT_Y` and `FlatElevationReport` are the file's only exports; every DEM-era function and constant is gone; the file imports only road-graph types.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite elevation.test.ts against the flat contract</name>
  <files>tools/map-compiler/graph/elevation.test.ts</files>
  <read_first>
    - tools/map-compiler/graph/elevation.test.ts (the full current file — every existing test targets DEM smoothing/densification and is being replaced; reuse its graph-fixture builders and its `demSource`/`attribution` fixture block shape)
    - tools/map-compiler/graph/elevation.ts (as rewritten in Task 1)
    - tools/map-compiler/author/heightfield.test.ts (sibling compiler-stage test style — describe/it phrasing convention)
  </read_first>
  <action>
Replace the body of `tools/map-compiler/graph/elevation.test.ts` with a suite for
`applyFlatElevation`. Keep the file's existing synthetic `RoadGraph` fixture builders (the ones
that construct `schemaVersion`/`source`/`attribution`/`origin`/`bounds`/`nodes`/`edges`) — they
are still correct; delete every `ElevationSampler` fixture (`makeSawtoothSampler`,
`makeNoisyJunctionSampler`, `makeRampSampler`, `makeBumpSampler`) and every `projector`
construction, and delete the `../sources/dem.ts` import.

Cover at minimum:
1. Every returned node has `y` exactly `0` (`toBe(0)`, exact equality, not a tolerance) — build
   the fixture with deliberately non-zero input node `y` values so a no-op pass fails this test.
2. Every returned edge point's `y` is exactly `0`, again from a fixture whose input points carry
   non-zero `y`.
3. `lengthM` equals the hand-computed 2D length for a fixture edge with a known right-triangle
   geometry (e.g. points forming a 3-4-5 layout in XZ with junk non-zero input `y`), asserted to
   within `1e-9`, proving the y component was dropped rather than included.
4. Input immutability: the original `graph` object, its `nodes` array, its `edges` array and one
   edge's `points` array are all unchanged after the call (compare against a structuredClone
   snapshot taken before the call).
5. The report: `maxAbsNodeY` and `maxAbsPointY` are both `0`, `nodeCount`/`edgeCount`/`pointCount`
   match the fixture's real counts, and `totalEdgeLengthM` equals the sum of the returned edges'
   `lengthM`.
6. The invariant throw: an edge whose `from` references a node id absent from `nodes[]` makes
   `applyFlatElevation` throw an error whose message contains both `applyFlatElevation` and
   `edge id=` plus the offending edge id.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npx vitest run tools/map-compiler/graph/elevation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tools/map-compiler/graph/elevation.test.ts` exits 0 with at least 6 passing tests and 0 failures
    - `grep -E "ElevationSampler|sources/dem|projector|Projector" tools/map-compiler/graph/elevation.test.ts` returns no matches
    - `grep -c "toBe(0)" tools/map-compiler/graph/elevation.test.ts` returns at least 2
    - The suite fails if `applyFlatElevation` is stubbed to return its input unchanged (verify by temporarily returning `{ graph, report }` with the untouched graph, observing at least 2 failures, then reverting)
  </acceptance_criteria>
  <done>The elevation suite asserts exact flatness, exact 2D lengths, input immutability, report contents and the invariant throw; no DEM sampler or projector appears anywhere in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled `.map.json` → runtime `parseRoadGraph` | The only untrusted-shaped input crossing into the game; unchanged by this plan |
| Overpass JSON → `buildGraph` | Pre-existing, upstream of this file; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | `applyFlatElevation` output shape | mitigate | Nodes/edges are rebuilt with explicit named-field object spreads over already-typed values; no `JSON.parse`, no `Object.assign` over untyped input, no dynamic string-keyed property write — `src/core/road-graph.ts`'s field-by-field reconstruction (the pre-existing prototype-pollution control, T-04-01) stays the only parse boundary |
| T-04.1-02 | Tampering | removal of the DEM network fetch path | accept (net risk reduction) | Deleting the DEM stage removes a network-fetched, third-party-parsed binary input. No replacement network or file input is introduced by this plan — the flat baseline is a literal constant |
| T-04.1-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages. RESEARCH.md's Package Legitimacy Audit records "not applicable — zero external packages"; if any executor reaches for a noise/geometry library instead of the hand-rolled code specified here, STOP and run the Package Legitimacy Gate with a blocking human checkpoint first |
</threat_model>

<verification>
1. `npx vitest run tools/map-compiler/graph/elevation.test.ts` — green.
2. `npx tsc --noEmit` — errors confined to `tools/map-compiler/cli.ts` only (that file is plan 04.1-08's).
3. `npx biome check tools/map-compiler/graph/elevation.ts tools/map-compiler/graph/elevation.test.ts` — clean.
</verification>

<success_criteria>
- `applyFlatElevation(graph)` returns a new graph whose every node `y` and every edge point `y` is exactly `0`.
- `lengthM` is the exact 2D polyline length.
- Zero references to DEM sampling, projection, smoothing, densification or gradient reporting remain in `tools/map-compiler/graph/elevation.ts`.
</success_criteria>

<output>
Create `.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-01-SUMMARY.md` when done
</output>
