---
phase: 04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der
plan: 11
type: execute
wave: 6
depends_on: ["04.1-10"]
files_modified:
  - docs/adr/0001-map-data-source.md
  - docs/adr/0004-first-area-and-compiler-decisions.md
  - tests/docs-present.test.ts
autonomous: true
requirements: [D-08, D-05, D-04, D-01]

must_haves:
  truths:
    - "ADR 0001 records that elevation is no longer DEM-sourced, without deleting why DEM was chosen originally"
    - "ADR 0001 states the condition under which DEM elevation could return"
    - "ADR 0004's DEM-derived heightfield decision is marked superseded rather than left reading as current"
    - "OpenStreetMap remains the frozen, unchanged road and building topology source"
  artifacts:
    - path: "docs/adr/0001-map-data-source.md"
      provides: "the in-place amendment retiring DEM elevation for v1"
      contains: "Amendment"
    - path: "docs/adr/0004-first-area-and-compiler-decisions.md"
      provides: "decision 7 marked superseded by the phase 04.1 amendment"
      contains: "04.1"
  key_links:
    - from: "docs/adr/0001-map-data-source.md"
      to: "tools/map-compiler/author/heightfield.ts"
      via: "the amendment naming the code that replaced the DEM stage"
      pattern: "heightfield"
---

<objective>
Amend `docs/adr/0001-map-data-source.md` IN PLACE (D-08) so the project's normative map-data
record says what the code actually does, and mark ADR 0004's superseded decision.

Purpose: ADR 0001 currently states elevation comes from USGS 3DEP or Copernicus GLO-30 via an
explicit selection rule, and ADR 0004 decision 7 describes a DEM-derived heightfield sunk 4.5m
beneath the roads. Neither is true after this phase. ADR 0001 is the document
`tests/no-google-pipeline.test.ts` and the compiled artifacts' attribution both point at; leaving
it stale makes every downstream provenance claim unverifiable.

This plan runs LAST, after the D-06 session, so the amendment records the values actually signed
off rather than the values proposed.

Output: an amended ADR 0001, a superseded-marked ADR 0004 decision 7, and doc-presence coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-CONTEXT.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-PATTERNS.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-09-SUMMARY.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-10-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

docs/adr/0001-map-data-source.md section map (current line numbers):
    1  # ADR 0001 — Map data comes from OpenStreetMap plus an open DEM
   13  ## Context
   29  ## Decision
   31  ### Road network — OpenStreetMap under ODbL v1.0            <- UNCHANGED, still frozen
   52  ### Elevation — an open DEM, chosen by an explicit selection rule   <- the sub-decision being retired
   57-60  the USGS 3DEP / Copernicus GLO-30 source table          <- KEEP as history, do not delete
   83  ### Prohibition — no Google-derived bytes, anywhere         <- UNCHANGED
  103  ## Consequences
  123  ## Supersedes
  148  ## Open Questions
  165  ## Enforcement  (table at 167-172; row 171 cites source.demSource — requirement unchanged, only its value changed)
  174  ## References

docs/adr/0004-first-area-and-compiler-decisions.md section map:
  101  ### 7. Off-road ground is a DEM-derived, uniform-`grass` heightfield sunk beneath the roads (plan 04-10 D-P28)
  182  ## Supersedes
  188  ## Open Questions   (#1 is the oblique-junction artifact; #4 is the "what would justify revisiting" pattern to copy)
  215  ## Enforcement

tests/docs-present.test.ts:
  line 45  { path: "docs/adr/0001-map-data-source.md", minChars: 1500 }
  line 50  { path: "docs/adr/0004-first-area-and-compiler-decisions.md", minChars: 1500 }
  line 83  expect(read("docs/adr/0001-map-data-source.md")).toContain("OpenStreetMap")

House style to match (CONTEXT.md D-08 names ADR 0004 explicitly as the template):
  Context / Decision / Consequences / Supersedes / Open Questions / Enforcement, with a
  "what would justify revisiting" note per retired decision.

Values to cite (take the FINAL post-session values from 04.1-10-SUMMARY.md, not the proposals):
  FLAT_Y 0 ; RELIEF_AMPLITUDE_M ; RELIEF_FALLOFF_DISTANCE_M ; HEIGHTFIELD_SINK_M ;
  TARGET_SHOULDER_WIDTH_M (was 20) ; the three JULIETTE_GA_CRESTS ; source.demSource
  "none-flat-authored" ; COMPILER_VERSION "0.6.0"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ADR 0001 in place</name>
  <files>docs/adr/0001-map-data-source.md</files>
  <read_first>
    - docs/adr/0001-map-data-source.md (the whole document)
    - docs/adr/0004-first-area-and-compiler-decisions.md (the whole document — this is the house style D-08 names as the template, especially its Supersedes and Open Questions sections)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-10-SUMMARY.md (the FINAL signed-off constant values to cite)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-CONTEXT.md (D-01 through D-05 and D-08 — the decisions this amendment records)
  </read_first>
  <action>
Amend `docs/adr/0001-map-data-source.md` in place. This is an amendment, not a rewrite and not a
new ADR document — D-08 is explicit on both counts.

1. **Title and status line.** Leave the title's `plus an open DEM` wording but append a dated
   amendment marker near the top (immediately under the title, before `## Context`) reading that
   the elevation sub-decision was amended in phase 04.1 on 2026-09-19 and pointing at the new
   `### Elevation — amended` subsection below. Do not retitle the document; the road-network
   half of this ADR is unchanged and still frozen.

2. **Keep `### Elevation — an open DEM, chosen by an explicit selection rule` and its source
   table intact as HISTORY.** Do not delete the USGS 3DEP / Copernicus rows, the selection rule,
   the Copernicus mandatory-notice text or the citations. Add a short lead-in sentence marking
   the whole subsection as the original, now-superseded decision, retained because it records
   why DEM was chosen and remains the ready-made answer if DEM elevation ever returns.

3. **Add a new subsection `### Elevation — amended (phase 04.1): flat, hand-authored terrain`**
   immediately after it, covering:
   - WHAT changed: elevation is no longer sampled from any DEM. Every road node and every edge
     centreline point is authored at `y = 0` (D-01/D-03). Off-road terrain gets a capped,
     deterministic, seeded procedural relief that fades to exactly zero at every road's paved
     edge (D-02/D-02a/D-02b). Jumps come from Phase 2's hand-placed ramp mechanic plus a small
     number of explicitly authored off-road terrain crests (D-04), never from real-world
     elevation data.
   - WHY: cite the developer's own framing verbatim from CONTEXT.md — "adding skirts to the
     edges of the roads to meet the terrain results in 20m extra road width in places which just
     looks ridiculous" — and the measured numbers behind it (average road-to-terrain gap 5.09m,
     shoulder ramp median 12.9 degrees and p95 26.9 degrees at a 20m target width), against the
     post-amendment measurements recorded in `04.1-09-SUMMARY.md` / `04.1-10-SUMMARY.md`.
   - WHAT IS UNCHANGED: OpenStreetMap under ODbL 1.0 remains the frozen, sole source for road and
     building topology; the Google prohibition is untouched; `source.demSource` and
     `attribution.dem` remain mandatory fields — only their VALUES changed, to
     `none-flat-authored` and the flat/hand-authored sentence.
   - WHERE THE CODE LIVES: name `tools/map-compiler/graph/elevation.ts` (`applyFlatElevation`),
     `tools/map-compiler/author/heightfield.ts` (`buildOffRoadRelief`) and
     `src/core/crest-geometry.ts` (the authored crests), and record the final signed-off constant
     values from `04.1-10-SUMMARY.md`.
   - D-05's retirement note: the original "favour real towns with genuine rolling terrain when
     picking a target area" selection criterion is retired as the operative rule for v1's area
     but is explicitly NOT permanently ruled out.
   - A **"What would justify revisiting this"** paragraph, matching ADR 0004's Open Questions #4
     pattern: a future v2 area whose terrain character is itself the point, a heightfield
     resolution budget that could represent real relief without the coarse-grid shoulder problem,
     or a decision that real-world vertical fidelity matters more than shoulder geometry. Note
     that the retained source table above is the ready-made answer if so.

4. **`## Consequences`:** add entries for what the amendment costs and buys — no network
   dependency and no `geotiff` parser in the build (one fewer fetched-and-third-party-parsed
   input), fully reproducible compiles, and the loss of real-world vertical fidelity as the
   accepted trade.

5. **`## Supersedes`:** add a line recording that this ADR's own elevation sub-decision is
   superseded in place by the phase 04.1 amendment, and that `docs/adr/0004` decision 7's
   DEM-derived heightfield is likewise superseded.

6. **`## Enforcement`:** leave the existing table rows intact — the `source.demSource` mandatory
   field requirement is unchanged. Add ONE row recording that the compiled artifact can no longer
   claim DEM provenance, enforced by `tools/map-compiler/graph/build-graph.test.ts`'s
   provenance-string test and `tests/compiled-map.test.ts`'s exact-flatness assertions.

7. **`## References`:** add the phase 04.1 planning artifacts (CONTEXT, RESEARCH) and
   `docs/schemas/road-graph.v1.md`.

Do not touch `### Road network — OpenStreetMap under ODbL v1.0` or
`### Prohibition — no Google-derived bytes, anywhere`. `tests/no-google-pipeline.test.ts`
requires an ADR pointer within six lines of any matching term — keep every existing pointer
intact and do not introduce a new bare mention of a retired pipeline without one.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npx vitest run tests/docs-present.test.ts tests/no-google-pipeline.test.ts tests/road-graph-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/docs-present.test.ts tests/no-google-pipeline.test.ts` exits 0 with 0 failures
    - `grep -c "Elevation — amended (phase 04.1)" docs/adr/0001-map-data-source.md` returns 1
    - `grep -c "USGS 3DEP" docs/adr/0001-map-data-source.md` returns at least 2 (the original source table is retained as history, not deleted)
    - `grep -c "What would justify revisiting" docs/adr/0001-map-data-source.md` returns at least 1
    - `grep -c "none-flat-authored" docs/adr/0001-map-data-source.md` returns at least 1
    - `grep -c "20m extra road width" docs/adr/0001-map-data-source.md` returns 1 (the developer's own framing is quoted)
    - `grep -c "ODbL" docs/adr/0001-map-data-source.md` is unchanged from before this plan (the road-network half is untouched)
    - `grep -c "OpenStreetMap" docs/adr/0001-map-data-source.md` returns at least 1 (the docs-present content assertion still holds)
  </acceptance_criteria>
  <done>ADR 0001 records the flat/hand-authored elevation decision with its measured justification, retains the DEM reasoning as history, states its revisit condition, and leaves the OSM and Google-prohibition sections untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mark ADR 0004 decision 7 superseded and extend doc-presence coverage</name>
  <files>docs/adr/0004-first-area-and-compiler-decisions.md, tests/docs-present.test.ts</files>
  <read_first>
    - docs/adr/0004-first-area-and-compiler-decisions.md (decision 7 at line 101, the Supersedes section at 182, Open Questions at 188, Enforcement at 215)
    - docs/adr/0001-map-data-source.md (as amended in Task 1 — the two documents must agree)
    - tests/docs-present.test.ts (the `minChars` table around lines 45-50 and the content assertion at line 83)
  </read_first>
  <action>
`docs/adr/0004-first-area-and-compiler-decisions.md`:

1. Decision 7 (`### 7. Off-road ground is a DEM-derived, uniform-grass heightfield sunk beneath
   the roads`): prepend a superseded marker sentence naming phase 04.1 and pointing at ADR
   0001's amendment. Keep the decision's body intact as history — its `HEIGHTFIELD_SINK_M`
   reasoning, the shoulder-grounding fix and the measured numbers are the record of WHY the
   phase 04.1 change was needed. Append a short paragraph stating precisely what survived and
   what did not: the `HeightfieldGrid` shape, the verified Rapier `heights[row + col * (rows + 1)]`
   storage order, the `.collision.json` sidecar and the shared `buildRoadShoulders` algorithm all
   survive unchanged; the DEM source, the 4.5m sink and the 20m shoulder width do not.
2. `## Supersedes`: add a line recording decision 7's elevation source as superseded by ADR
   0001's phase 04.1 amendment.
3. `## Open Questions`: leave #1 (the oblique-junction artifact) open — plan 04.1-10's session
   records whether it still presents on flat terrain, and that verdict should be appended to the
   existing question rather than closing it unless the session says it is gone.

`tests/docs-present.test.ts`:
4. Add a content assertion for `docs/adr/0001-map-data-source.md` asserting it contains
   `"none-flat-authored"`, so a future edit cannot silently revert the amendment and leave the
   ADR claiming DEM provenance while the compiler emits the flat value. Follow the existing
   `expect(read(...)).toContain(...)` style at line 83 exactly.
5. Raise ADR 0001's `minChars` floor to reflect the amended length (read the amended file's real
   character count and set the floor comfortably below it, matching how the existing 1500 floor
   relates to the other listed documents). Do not raise it so close to the real length that a
   routine wording edit trips it.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (typecheck, lint and the full suite)
    - `grep -ci "supersed" docs/adr/0004-first-area-and-compiler-decisions.md` increased by at least 2 versus before this plan
    - `grep -c "04.1" docs/adr/0004-first-area-and-compiler-decisions.md` returns at least 2
    - `grep -c "none-flat-authored" tests/docs-present.test.ts` returns 1
    - Temporarily removing the amendment subsection from ADR 0001 makes `tests/docs-present.test.ts` fail (verify, then revert)
  </acceptance_criteria>
  <done>ADR 0004 decision 7 reads as superseded history rather than current practice, and a doc-presence test guards ADR 0001's amendment against silent reversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR documents → the project's provenance and licensing claims | ADR 0001 is what the compiled artifacts' attribution block and `tests/no-google-pipeline.test.ts` both point at; a stale ADR makes every downstream provenance claim unverifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-30 | Repudiation | a normative ADR describing a pipeline that no longer exists | mitigate | The amendment is written in place with the retired decision retained as explicitly-marked history, and `tests/docs-present.test.ts` gains a `none-flat-authored` content assertion so the amendment cannot be silently reverted |
| T-04.1-31 | Tampering | weakening the Google-derived-bytes prohibition while editing around it | mitigate | Task 1 forbids touching the prohibition and the road-network subsection; `tests/no-google-pipeline.test.ts` (a scoped comment-stripped grep requiring an ADR pointer within six lines) is in the verify command, and the `ODbL` occurrence count is pinned in acceptance criteria |
| T-04.1-32 | Repudiation | losing the reasoning that would be needed to reinstate DEM elevation | mitigate | The original source table, selection rule, Copernicus mandatory-notice text and citations are retained verbatim, and a "What would justify revisiting this" paragraph records D-05's condition explicitly |
| T-04.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; this plan edits documentation and one test file only |
</threat_model>

<verification>
1. `npx vitest run tests/docs-present.test.ts tests/no-google-pipeline.test.ts tests/road-graph-schema.test.ts` — green.
2. `npm run check` — green.
3. ADR 0001 contains both the retained DEM source table and the new amendment subsection; ADR 0004 decision 7 is marked superseded.
</verification>

<success_criteria>
- `docs/adr/0001-map-data-source.md` is amended in place (not superseded by a new document), records the flat/hand-authored decision with its measured justification and the final signed-off constant values, retains the DEM reasoning as history, and states the revisit condition.
- The OSM road-network decision and the Google prohibition are byte-for-byte untouched.
- ADR 0004 decision 7 reads as superseded history, naming exactly what survived.
- A doc-presence content assertion guards the amendment.
</success_criteria>

<output>
Create `.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-11-SUMMARY.md` when done
</output>
