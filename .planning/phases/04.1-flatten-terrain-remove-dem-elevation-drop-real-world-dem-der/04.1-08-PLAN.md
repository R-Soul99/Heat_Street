---
phase: 04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der
plan: 08
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-04", "04.1-05", "04.1-06", "04.1-07"]
files_modified:
  - tools/map-compiler/cli.ts
  - tools/map-compiler/sources/dem.ts
  - tools/map-compiler/sources/dem.test.ts
  - tools/map-compiler/areas/juliette-ga.dem.tif
  - tools/map-compiler/areas/juliette-ga.config.ts
  - package.json
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-08]

must_haves:
  truths:
    - "npm run compile-map completes with no network call for elevation and no DEM cache read"
    - "The repository contains no DEM fetch, parse, cache or sampler code"
    - "npm run check is green across typecheck, lint and the full suite"
  artifacts:
    - path: "tools/map-compiler/cli.ts"
      provides: "the flat-terrain compiler pipeline end to end"
      contains: "applyFlatElevation"
    - path: "package.json"
      provides: "dependency list with geotiff removed"
  key_links:
    - from: "tools/map-compiler/cli.ts"
      to: "tools/map-compiler/author/heightfield.ts"
      via: "buildOffRoadRelief(elevatedGraph, elevatedGraph.bounds) before the building stage"
      pattern: "buildOffRoadRelief"
    - from: "tools/map-compiler/cli.ts"
      to: "src/core/crest-geometry.ts"
      via: "buildCrestGeometry over crestsForArea(config.areaId) into the .glb"
      pattern: "buildCrestGeometry"
---

<objective>
Rewire the compiler's composition root onto the flat-terrain stage set and delete every remaining
trace of the DEM pipeline.

Purpose: this is the integration plan. Plans 04.1-01 through 04.1-07 each deliberately left
`tools/map-compiler/cli.ts` uncompilable (file ownership); this plan restores a green
`npm run check` and makes `npm run compile-map` runnable again.

Output: a DEM-free compiler CLI, four deleted files, and a green full-repo gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-CONTEXT.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-RESEARCH.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-PATTERNS.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-01-SUMMARY.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-04-SUMMARY.md
@.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-07-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

New/changed signatures this plan wires together:
  applyFlatElevation(graph: RoadGraph): { graph: RoadGraph; report: FlatElevationReport }        // 04.1-01
  FlatElevationReport = { nodeCount, edgeCount, pointCount, maxAbsNodeY, maxAbsPointY, totalEdgeLengthM }
  buildOffRoadRelief(graph: RoadGraph, bounds: RoadGraphBounds, resolution?): HeightfieldGrid     // 04.1-04
  buildingBoxes(envelope, projector, groundHeightAt: GroundHeightSampler, config?)                // 04.1-05
  buildCrestGeometry(crests, heightSampler: (x, z) => number): readonly CrestGeometryEntry[]      // 04.1-03
  crestsForArea(areaId: string): readonly AuthoredCrest[]                                         // 04.1-03
  buildGltfDocument(geometry, boxes, heightfield, shoulders?, crests?)                            // 04.1-07

Unchanged and still required in cli.ts:
  makeProjector({ lat: graph.origin.lat, lon: graph.origin.lon })   // still needed by buildingBoxes' projectRing
  sampleHeightfieldBilinear(heightfield, x, z)                      // src/core/heightfield-sample.ts
  buildRoadGeometry / buildRoadShoulders / TARGET_SHOULDER_WIDTH_M / MIN_SHOULDER_WIDTH_M / SHOULDER_BUILDING_SAFETY_MARGIN_M
  resolvePointShoulderWidth / buildingBoundingRadius / ClearanceBuilding
  parseRoadGraph / parseMapCollision self-checks, validateGraph gate, --no-validate, --refresh

Being deleted from cli.ts:
  import { DemRaster, LoadOrFetchDemResult, loadOrFetchDem, makeElevationSampler, parseDemRaster } from "./sources/dem.ts"
  printElevationReport(demResult, raster, report)
  const demResult / raster / sampler
  the `demSource "${config.demSource}"` fragment in the resolved-area log line

Current stage order (cli.ts lines ~396-559), for reference:
  buildGraph -> applyElevation -> parseRoadGraph self-check -> buildRoadGeometry -> validate
  -> write .map.json -> buildingBoxes -> buildHeightfield -> buildMapCollision -> write .collision.json
  -> buildRoadShoulders -> buildGltfDocument -> write .glb
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the compiler pipeline onto the flat-terrain stages</name>
  <files>tools/map-compiler/cli.ts</files>
  <read_first>
    - tools/map-compiler/cli.ts (the whole file — the import block, `printElevationReport`, `printHeightfieldSummary`, and `main`'s stage sequence from line ~396 to ~560)
    - tools/map-compiler/graph/elevation.ts (as rewritten in plan 04.1-01)
    - tools/map-compiler/author/heightfield.ts (as rewritten in plan 04.1-04)
    - tools/map-compiler/geometry/building-box.ts (as changed in plan 04.1-05)
    - tools/map-compiler/author/gltf.ts (as changed in plan 04.1-07)
    - src/core/crest-geometry.ts (plan 04.1-03)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-PATTERNS.md (the "print, don't just log a boolean" and "self-check-before-write" conventions that must survive unchanged)
  </read_first>
  <action>
Rewrite `main`'s stage sequence in `tools/map-compiler/cli.ts` to this exact order, keeping every
existing gate, self-check, summary print, `--refresh`/`--no-validate` behaviour and abort path
intact:

1. `--area` resolution against `KNOWN_AREAS` (unchanged — this registry is T-04-08's mitigation
   and must not become argv-driven).
2. The resolved-area log line: drop the `demSource "${config.demSource}"` fragment and replace it
   with `terrain "flat-authored"` so the line still says something true about elevation.
3. `loadOrFetchArea` for roads and buildings (unchanged), `printFetchSummary` (unchanged).
4. `buildGraph` + `printBuildReport` + the two surface-fallback-ratio gates (unchanged).
5. `const projector = makeProjector({ lat: graph.origin.lat, lon: graph.origin.lon });` — keep it,
   `buildingBoxes` still needs it for `projectRing`.
6. `const { graph: flatGraph, report: elevationReport } = applyFlatElevation(graph);`
   then `printFlatElevationReport(elevationReport)` (new, see below). Delete the `loadOrFetchDem`
   / `parseDemRaster` / `makeElevationSampler` / `applyElevation` block entirely.
7. `parseRoadGraph(JSON.stringify(flatGraph), ...)` self-check — keep, with its label updated to
   name `applyFlatElevation`.
8. `buildRoadGeometry(flatGraph)` (unchanged), the `--no-validate` warning and the `validateGraph`
   abort gate (unchanged), then write `<areaId>.map.json` (unchanged).
9. **Moved earlier:** `const heightfield = buildOffRoadRelief(flatGraph, flatGraph.bounds);`
   followed by `printHeightfieldSummary(heightfield)` (unchanged function). Add a block comment
   explaining the move: the building stage now seats buildings on this grid, so the grid must
   exist first — previously buildings came first because they sampled the DEM independently.
10. `const { boxes, report: buildingReport } = buildingBoxes(buildingsResult.envelope, projector, (x, z) => sampleHeightfieldBilinear(heightfield, x, z));`
11. `buildMapCollision` + `parseMapCollision` self-check + write `<areaId>.collision.json` +
    `printCollisionSummary` (all unchanged).
12. `buildRoadShoulders(flatGraph, ...)` with the existing per-point `resolvePointShoulderWidth`
    resolver and the existing three shoulder constants (unchanged code; the constants themselves
    changed in plan 04.1-02).
13. New: `const crests = buildCrestGeometry(crestsForArea(config.areaId), (x, z) => sampleHeightfieldBilinear(heightfield, x, z));`
    with a block comment stating that `src/physics/map-scene.ts` makes the identical call over the
    identical table and sampler, so the shipped mesh and the runtime collider cannot diverge.
14. `buildGltfDocument(geometry, boxes, heightfield, shoulders, crests)` + `writeGlb` +
    `printGltfSummary` (extend `printGltfSummary` to print the new crest stats).

Replace `printElevationReport` with `printFlatElevationReport(report: FlatElevationReport): void`,
following this file's "print concrete numbers, never a bare boolean" convention. It must print:
node/edge/point counts, `maxAbsNodeY` and `maxAbsPointY` both formatted to 3 decimals, and
`totalEdgeLengthM`. Add an explicit warning branch: if either max-abs value is greater than 0,
`console.warn` a named warning that D-01 requires exact flatness and something authored a
non-zero road y — this is the regression signal that replaces the old gradient-threshold report.

Extend `printGltfSummary` to print `crests: <crestCount> crest(s), <crestTriangleCount> triangles`.

Delete the entire `./sources/dem.ts` import statement and every reference to `demResult`,
`raster`, `sampler`, `DemRaster`, `LoadOrFetchDemResult`, `loadOrFetchDem`, `makeElevationSampler`
and `parseDemRaster`.

Update the file header's numbered pipeline description (stages 1-8) so stage 4 describes the flat
elevation pass instead of the DEM fetch/sample/smooth stage, and so the heightfield/building
ordering matches the new sequence.

Security (T-04.1-19): do not introduce any new argv-derived path, dynamic import or file read.
`KNOWN_AREAS` stays the only area-resolution mechanism, and `crestsForArea` is keyed by the
already-resolved `config.areaId`, never by raw argv.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with zero errors repo-wide (the transient `cli.ts` errors from waves 1-2 are resolved)
    - `grep -c "applyFlatElevation" tools/map-compiler/cli.ts` returns at least 2
    - `grep -c "buildOffRoadRelief" tools/map-compiler/cli.ts` returns 1
    - `grep -c "buildCrestGeometry" tools/map-compiler/cli.ts` returns 1
    - `grep -E "loadOrFetchDem|parseDemRaster|makeElevationSampler|DemRaster|sources/dem" tools/map-compiler/cli.ts` returns no matches
    - `grep -c "printFlatElevationReport" tools/map-compiler/cli.ts` returns 2
    - `grep -c "parseRoadGraph" tools/map-compiler/cli.ts` returns at least 2 and `grep -c "parseMapCollision" tools/map-compiler/cli.ts` returns at least 2 (both self-checks survived the rewrite)
    - `grep -c "KNOWN_AREAS" tools/map-compiler/cli.ts` returns at least 3 (the registry mechanism is untouched)
  </acceptance_criteria>
  <done>`tools/map-compiler/cli.ts` compiles, runs the flat-terrain stage set in the new order, prints a flatness report with a non-zero-y warning branch, and contains no DEM references.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the DEM source module, its cache and the orphaned geotiff dependency</name>
  <files>tools/map-compiler/sources/dem.ts, tools/map-compiler/sources/dem.test.ts, tools/map-compiler/areas/juliette-ga.dem.tif, tools/map-compiler/areas/juliette-ga.config.ts, package.json</files>
  <read_first>
    - tools/map-compiler/sources/dem.ts (confirm what else it exports before deleting, and that `geotiff` is imported only here)
    - tools/map-compiler/areas/juliette-ga.config.ts (the `demSizePx` field declaration near line 69 and its value near line 95; `demSource` was already changed in plan 04.1-06 and stays)
    - package.json (the `devDependencies` block containing `geotiff`)
    - .planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-RESEARCH.md (Pitfall 2 — the orphaned `.dem.tif` cache; and the `geotiff` sole-importer verification)
  </read_first>
  <action>
Before deleting anything, re-verify the import graph with these two greps and record their output
in the SUMMARY:
`grep -rn "sources/dem" --include=*.ts src/ tools/ tests/` and
`grep -rn "geotiff" --include=*.ts --include=*.json src/ tools/ tests/ package.json`.
If `sources/dem` still has any importer other than `dem.test.ts`, STOP and report it rather than
deleting — that would mean an earlier plan's decoupling is incomplete.

Then:
1. Delete `tools/map-compiler/sources/dem.ts` and `tools/map-compiler/sources/dem.test.ts`.
2. Delete `tools/map-compiler/areas/juliette-ga.dem.tif` (the cached raw GeoTIFF). Keep
   `tools/map-compiler/areas/juliette-ga.raw-osm.json` and
   `tools/map-compiler/areas/juliette-ga.raw-buildings.json` — roads and buildings still come
   from OSM and those caches stay live. Use `git rm` so the deletion is staged, and confirm with
   `git status` that the `.tif` no longer appears as an untracked or unchanged file
   (RESEARCH.md Pitfall 2's named warning sign).
3. Remove the `demSizePx` field from the `AreaConfig` interface and from `julietteGaConfig` in
   `tools/map-compiler/areas/juliette-ga.config.ts` — it existed solely as the per-area cap on
   the DEM `exportImage` request. Do not remove `demSource`; plan 04.1-06 repurposed it and it is
   a required schema field.
4. Remove `"geotiff"` from `package.json`'s `devDependencies` and run `npm install` so
   `package-lock.json` is regenerated consistently. Do not add any dependency. Do not change any
   other pinned version — STATE.md records that exact version literals are load-bearing because a
   `three` or Rapier patch bump can alter solver behaviour and invalidate recorded medal times.
5. If `tools/map-compiler/sources/` is now empty except for `overpass.ts`/`overpass.test.ts`,
   leave the directory as is — do not restructure.

Finally run `npm run check` and confirm it is green end to end. If the CRLF-vs-LF Biome issue
recorded in STATE.md's Blockers section surfaces on files this phase never touched, note it in the
SUMMARY as pre-existing rather than fixing it here.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (typecheck + lint + full suite all green)
    - `test ! -f tools/map-compiler/sources/dem.ts && test ! -f tools/map-compiler/sources/dem.test.ts && test ! -f tools/map-compiler/areas/juliette-ga.dem.tif` succeeds
    - `test -f tools/map-compiler/areas/juliette-ga.raw-osm.json && test -f tools/map-compiler/areas/juliette-ga.raw-buildings.json` succeeds (the OSM caches are retained)
    - `grep -rn "geotiff" package.json` returns no matches
    - `grep -rn "sources/dem\|ElevationSampler\|demSizePx" --include=*.ts src/ tools/ tests/` returns no matches
    - `git status --porcelain tools/map-compiler/areas/juliette-ga.dem.tif` shows a staged deletion (a `D` status), not an untracked or unmodified file
  </acceptance_criteria>
  <done>No DEM fetch, parse, cache, sampler, config field or npm dependency remains anywhere in the repository, and `npm run check` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process.argv` → area resolution | Mitigated by the `KNOWN_AREAS` object literal; this plan must not weaken it |
| (removed) USGS 3DEP HTTPS endpoint → GeoTIFF parser | Deleted entirely by this plan |
| Overpass JSON cache → `buildGraph` | Retained, unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-19 | Tampering | argv-driven dynamic import / path traversal in `cli.ts` | mitigate | `KNOWN_AREAS` stays the sole area-resolution mechanism (T-04-08); no new argv-derived path, dynamic import or file read is introduced, and `crestsForArea` is keyed by the already-resolved `config.areaId`. Acceptance criteria grep for the registry's continued presence |
| T-04.1-20 | Tampering | build tool trusting network-fetched, third-party-parsed bytes | accept (net risk reduction) | Deleting `dem.ts` removes the HTTPS fetch, the on-disk `.tif` cache, the TIFF-magic sniff (T-04-17) and the `geotiff` parser dependency. No replacement input is added, so nothing needs equivalent validation |
| T-04.1-21 | Tampering | supply chain via dependency churn | mitigate | The only `package.json` change is a REMOVAL (`geotiff`); every remaining version stays an exact pinned literal per STATE.md's Phase 01-01 decision. No `npm install <pkg>` is performed, so the Package Legitimacy Gate is not triggered |
| T-04.1-22 | Tampering | compiler emitting an artifact its own runtime parser would reject | mitigate | Both `parseRoadGraph` and `parseMapCollision` self-checks survive the rewrite unchanged and are grepped for in acceptance criteria — the codebase's "never ship what the runtime would reject" guarantee |
| T-04.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; one removed. Any executor adding a package must STOP and run the Package Legitimacy Gate with a blocking human checkpoint first |
</threat_model>

<verification>
1. `npm run check` — green (typecheck, lint, full suite).
2. `grep -rn "sources/dem\|ElevationSampler\|geotiff\|demSizePx" --include=*.ts --include=*.json src/ tools/ tests/ package.json` — no matches.
3. `git status --porcelain` — shows the `.dem.tif` staged for deletion.
</verification>

<success_criteria>
- `tools/map-compiler/cli.ts` compiles and runs the flat-terrain stage set in the new order.
- Zero DEM code, cache files, config fields or dependencies remain in the repository.
- `npm run check` is green across the whole repo for the first time since wave 1.
</success_criteria>

<output>
Create `.planning/phases/04.1-flatten-terrain-remove-dem-elevation-drop-real-world-dem-der/04.1-08-SUMMARY.md` when done
</output>
