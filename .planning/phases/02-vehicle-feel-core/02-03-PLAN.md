---
phase: 02-vehicle-feel-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hud/speedometer.ts
  - tests/speedometer.test.ts
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "A retro analog needle gauge with a digital inset exists in the bottom-right corner"
    - "The needle angle is a pure, clamped function of mph and never sweeps past 0 or 160"
    - "The digital readout settles at the same rate at 60 Hz and at 144 Hz"
    - "The gauge is built without innerHTML and without importing three or Rapier"
  artifacts:
    - path: "src/hud/speedometer.ts"
      provides: "createSpeedometer() DOM/SVG half plus the pure mph/angle/damping half"
      exports: ["createSpeedometer", "Speedometer", "mphFromGroundSpeed", "needleAngleDeg", "dampStep", "readoutColour", "SWEEP_START_DEG", "SWEEP_DEG", "MAX_MPH", "DIGIT_TAU_SEC", "REDLINE_MPH"]
      min_lines: 200
    - path: "tests/speedometer.test.ts"
      provides: "NAV-01 pure-half coverage: conversion, needle mapping, clamping, damping"
  key_links:
    - from: "src/hud/speedometer.ts"
      to: "the render callback"
      via: "update(groundSpeedMs, dtMs)"
      pattern: "update\\(groundSpeedMs"
---

<objective>
Create `src/hud/` — a new player-facing HUD tier — and its first inhabitant: the retro
analog speedometer (D-10 to D-13, NAV-01). Pure functions for the maths, an SVG DOM half
for the pixels, mirroring exactly the `formatHudText` / `createHud` split that
`src/debug/profiler-hud.ts` established.

Purpose: NAV-01, ROADMAP SC4. `src/hud/` is deliberately a new directory rather than a
corner of `src/render/` (02-RESEARCH.md Open Question 5, decided in 02-UI-SPEC.md Call 2) —
`src/render/` means "the 3D scene", and Phases 5/6/8 add a minimap, run timer, split
deltas, medal grid, damage indicator and pursuit HUD that must not land in the renderer.

Output: `src/hud/speedometer.ts` and `tests/speedometer.test.ts`. The layering rule that
polices `src/hud/**` is added in plan 02-05, which owns `tests/layering.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vehicle-feel-core/02-UI-SPEC.md
@.planning/phases/02-vehicle-feel-core/02-RESEARCH.md
@.planning/phases/02-vehicle-feel-core/02-PATTERNS.md
@src/debug/profiler-hud.ts
@tests/profiler-hud.test.ts
@tests/layering.test.ts
</context>

<interfaces>
<!-- Contract the composition root (plan 02-08) compiles against. -->
interface Speedometer {
  /** Called once per rAF. `groundSpeedMs` is hypot(linvel.x, linvel.z); `dtMs` comes from the loop. */
  update(groundSpeedMs: number, dtMs: number): void;
  dispose(): void;
}
function createSpeedometer(): Speedometer

<!-- Pure half, imported by tests/speedometer.test.ts only -->
function mphFromGroundSpeed(groundSpeedMs: number): number
function needleAngleDeg(mph: number): number
function dampStep(damped: number, targetMph: number, dtMs: number): number
function readoutColour(dampedMph: number): string
const SWEEP_START_DEG = -120, SWEEP_DEG = 240, MAX_MPH = 160, REDLINE_MPH = 120, DIGIT_TAU_SEC = 0.12
const MS_TO_MPH = 2.2369362920544
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pure speedometer maths and its Node test suite</name>
  <files>src/hud/speedometer.ts, tests/speedometer.test.ts</files>
  <read_first>
    - src/debug/profiler-hud.ts (lines 9-13 the pure/impure split doc comment; lines 36-63 `formatHudText` and the `over()` marker helper; lines 65-73 the `Hud` handle interface)
    - tests/profiler-hud.test.ts (the `makeX(overrides)` builder pattern; the threshold-pair style — one `it` under, one `it` over; the "assert the constant alongside the behaviour" style at lines 73-77; the "purity-is-the-proof" case at lines 135-140)
    - .planning/phases/02-vehicle-feel-core/02-UI-SPEC.md ("Speedometer Geometry Contract" needle sweep mapping and its four check values; "Interaction & Motion Contract" damping row; Color section's 120 mph amber threshold)
    - .planning/phases/02-vehicle-feel-core/02-RESEARCH.md (Pattern 6; Pitfall 1 — why `currentVehicleSpeed()` must never be the speed source)
  </read_first>
  <action>
Create `src/hud/speedometer.ts` with a module doc comment ending in the new `src/hud/`
layering paragraph: "receives plain numbers; imports neither `three` nor Rapier; never
writes simulation state; never reads a clock." Add the `profiler-hud.ts:9-13` style
paragraph explaining that the two halves are deliberately separate — the pure half is
Node-testable, the `createSpeedometer` half needs a DOM and is covered only by the human
browser checkpoint in plan 02-10.

Export these constants with doc comments naming their source:
  `MS_TO_MPH = 2.2369362920544`, `MAX_MPH = 160` (D-12), `REDLINE_MPH = 120`,
  `SWEEP_START_DEG = -120`, `SWEEP_DEG = 240`, `DIGIT_TAU_SEC = 0.12`.
Comment `REDLINE_MPH` with 02-UI-SPEC.md's rationale: the redline starts at exactly the
120 mph ramp-jump target so entering amber is the legible "fast enough to jump" signal, and
BOTH colour switches (band start, digits amber) use this same threshold so the instrument
tells one story.

Export the pure functions:
  - `mphFromGroundSpeed(groundSpeedMs)` = `groundSpeedMs * MS_TO_MPH`. Doc-comment it with
    Pitfall 1: `currentVehicleSpeed()` must never be used — it returns the full 3D
    velocity magnitude including vertical (measured -23.32 where ground speed was 20.0) and
    its sign is `linvel . forwardAxis` where the forward axis defaults to X, which is
    numerical noise for a -Z-forward car (measured -19.135 while true forward speed was
    +19.185). The composition root passes `Math.hypot(linvel.x, linvel.z)`.
  - `needleAngleDeg(mph)` = `SWEEP_START_DEG + clamp(mph / MAX_MPH, 0, 1) * SWEEP_DEG`.
  - `dampStep(damped, targetMph, dtMs)` = `damped + (targetMph - damped) * (1 - Math.exp(-(dtMs / 1000) / DIGIT_TAU_SEC))`. Doc-comment why the
    `1 - exp(-dt/tau)` form and not `damped += (mph - damped) * 0.2`: the fixed-lerp form
    smooths ~2.4x harder at 144 Hz than at 60 Hz and would reintroduce the
    framerate-dependence Phase 1 spent seven plans eliminating.
  - `readoutColour(dampedMph)` returning `"#E8A33D"` at `>= REDLINE_MPH` and `"#F2EFE6"`
    below.

Create `tests/speedometer.test.ts` importing ONLY the pure half. Header restates
`tests/profiler-hud.test.ts:6-11`'s scope-limiting comment: `createSpeedometer` needs a DOM
that does not exist in Vitest's `node` environment, and the browser checkpoint in plan
02-10 covers it. Cases:
  - mph conversion at 0, at 26.8224 m/s (asserts 60.0 to within 1e-6), at 53.6448 m/s (120.0).
  - needle angle at the four 02-UI-SPEC.md check values: 0 mph -> -120, 80 mph -> 0,
    120 mph -> +60, 160 mph -> +120. Assert each exactly.
  - needle clamps: 200 mph -> +120 (not beyond), -5 mph -> -120.
  - "assert the constant alongside the behaviour": one case per constant asserting
    `SWEEP_START_DEG === -120`, `SWEEP_DEG === 240`, `MAX_MPH === 160`,
    `DIGIT_TAU_SEC === 0.12`, `REDLINE_MPH === 120` in the same `it` as the behaviour that
    depends on it, so a silent constant edit fails loudly instead of moving the goalposts.
  - "damping": step `dampStep` from 0 toward 100 mph in 16.7 ms increments and in 6.9 ms
    increments over the SAME total elapsed sim time, and assert both reach within 0.5 mph of
    each other — the framerate-independence proof 02-VALIDATION.md pins as `-t damping`.
  - damping settles within 95% of the target in ~0.36 s (assert the value at 360 ms of
    accumulated dt is >= 95 mph when driving 0 -> 100).
  - damping is applied before rounding: assert `dampStep` returns a non-integer mid-transit,
    so `String(Math.round(...))` cannot flicker between neighbours.
  - `readoutColour` threshold pair: 119.9 -> off-white, 120 -> amber.
  - a "purity-is-the-proof" case in `tests/profiler-hud.test.ts:135-140`'s exact shape:
    the fact this suite runs at all in Node with no `document` is itself the proof the pure
    half touches no DOM.
  </action>
  <verify>
    <automated>npx vitest run tests/speedometer.test.ts</automated>
    <automated>npx vitest run tests/speedometer.test.ts -t damping</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/speedometer.test.ts` exits 0 with at least 12 passing cases.
    - `npx vitest run tests/speedometer.test.ts -t damping` matches at least one case and passes.
    - `needleAngleDeg(0) === -120`, `needleAngleDeg(80) === 0`, `needleAngleDeg(120) === 60`, `needleAngleDeg(160) === 120`, `needleAngleDeg(200) === 120`, `needleAngleDeg(-5) === -120`.
    - `src/hud/speedometer.ts` contains no `currentVehicleSpeed` reference on any line.
    - `tests/speedometer.test.ts` imports nothing from `three` or `@dimforge/rapier3d`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Every number the gauge displays is produced by a pure function with a Node-verified contract.</done>
</task>

<task type="auto">
  <name>Task 2: SVG gauge construction and the per-frame update path</name>
  <files>src/hud/speedometer.ts</files>
  <read_first>
    - src/hud/speedometer.ts (task 1's output)
    - src/debug/profiler-hud.ts (lines 75-97 the `createHud` factory and the gate-free construction doc comment; lines 81-88 the `style.cssText` overlay pattern; line 125 the `textContent`-only write; lines 33-34 and 108-110 the THROTTLE_MS block that must NOT be copied; lines 144-147 `dispose`)
    - .planning/phases/02-vehicle-feel-core/02-UI-SPEC.md ("Speedometer Geometry Contract" element table — every cx/cy/r/x/y/stroke-width/colour; the container `style.cssText` string; "Interaction & Motion Contract"; "Accessibility" role/aria-label row; "Layer / z-index contract")
    - tests/layering.test.ts (lines 151-155: any non-comment `innerHTML` under `src/` fails the suite)
    - .planning/phases/02-vehicle-feel-core/02-PATTERNS.md (section `src/hud/speedometer.ts` — the three deviations from `profiler-hud.ts` and the mandatory counter-comment at the throttle site)
  </read_first>
  <action>
Add the DOM half to `src/hud/speedometer.ts`: `export interface Speedometer { update(groundSpeedMs:
number, dtMs: number): void; dispose(): void }` and `export function createSpeedometer():
Speedometer`.

`createSpeedometer` is GATE-FREE — it does not check `DEBUG_ENABLED`, because the
speedometer is player-facing and always on. This mirrors `createHud`'s
"this module never checks `DEBUG_ENABLED` itself so it stays independently testable"
comment; restate it with the player-facing reason.

Build every element with `document.createElementNS("http://www.w3.org/2000/svg", tag)` and
`appendChild`. No `innerHTML`, no template strings into the DOM, no stylesheet, no edit to
`index.html`. Root `<svg>` gets `viewBox="0 0 200 200"`, `role="img"`,
`aria-label="Speedometer"`, and this exact `style.cssText` (a `+`-concatenated string
literal, following `profiler-hud.ts:83-86`):
`position:fixed;right:16px;bottom:16px;width:200px;height:200px;z-index:5;pointer-events:none;font-family:"Arial Narrow","Helvetica Neue",Helvetica,Arial,sans-serif;font-stretch:condensed;font-variant-numeric:tabular-nums`
Comment `font-variant-numeric:tabular-nums` as non-negotiable: the readout changes up to
60x/sec and proportional digits make the number visibly shimmer as `1` swaps for `8`.

Children, in draw order, with the exact geometry from 02-UI-SPEC.md's element table:
dial face circle (100,100,r92, fill `#0B0B0C`); bezel circle (r94, stroke `#2A2A2E`,
stroke-width 4, fill none); redline `<path>` arc at r84 spanning 120 -> 160 mph with
stroke `#E8A33D`, stroke-width 6, `stroke-linecap:butt`, fill none — compute its endpoints
from `needleAngleDeg`, do not hardcode a `d` string, so the band can never drift from the
needle mapping; 9 major tick `<line>`s at 0/20/.../160 from r92 to r78 stroke `#F2EFE6`
width 3; minor tick `<line>`s every 5 mph skipping majors, r92 to r85, stroke `#6E6E73`
width 1.5; 9 dial numeral `<text>`s centred at r62, `font-size:14px`, `font-weight:600`,
`text-anchor:middle`, `dominant-baseline:central`, fill `#F2EFE6` except `120`/`140`/`160`
which are `#E8A33D`; readout `<rect>` x69 y124 w62 h32 rx3 fill `#16161A` stroke `#2A2A2E`
width 1; digital readout `<text>` at (100,140) `font-size:28px` weight 600 anchored middle,
central baseline; `MPH` `<text>` at (100,168) `font-size:11px` weight 600
`letter-spacing:1px` fill `#9A9AA0`; needle CASING `<line>` (100,112)->(100,26)
stroke `#0B0B0C` width 7 `stroke-linecap:round`; needle `<line>` same endpoints stroke
`#C8402F` width 4 round cap; needle hub `<circle>` (100,100,r6) fill `#2A2A2E` stroke
`#F2EFE6` width 1.5. The casing-under-needle pair is a mandatory legibility mitigation, not
decoration — comment it: red-on-amber contrast is poor and the dark casing is what
separates the needle from the redline band. Append the root to `document.body`.

`update(groundSpeedMs, dtMs)`:
  - `const mph = mphFromGroundSpeed(groundSpeedMs)`
  - rotate BOTH needle elements with `setAttribute("transform", "rotate(" + needleAngleDeg(mph) + " 100 100)")` — instant, unsmoothed (D-13)
  - `damped = dampStep(damped, mph, dtMs)`; write `digits.textContent = String(Math.round(damped))`
  - set the digits' `fill` from `readoutColour(damped)`
  - runs on EVERY call. Do NOT copy `profiler-hud.ts`'s `THROTTLE_MS` block. Place an
    explicit counter-comment at that spot stating that a needle updating 7x/sec reads as
    broken hardware, and that the cost is 3 `setAttribute` calls plus 1 `textContent` write
    per frame — negligible against the 16.6 ms budget in `docs/frame-budget.md`. Without
    that comment the next reader will "fix" it back.
  - Never set a CSS `transition` on the needle: 02-UI-SPEC.md forbids it because it would
    reintroduce refresh-rate-dependent lag between physics and display.

`dispose()` removes the root SVG from the DOM (`svg.remove()`), matching
`profiler-hud.ts:144-147`.
  </action>
  <verify>
    <automated>npx vitest run tests/speedometer.test.ts tests/layering.test.ts</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (typecheck + Biome + full suite).
    - `npx vitest run tests/layering.test.ts` exits 0 — in particular the `no innerHTML anywhere under src/` case stays green with `src/hud/speedometer.ts` present.
    - `grep -c "innerHTML" src/hud/speedometer.ts` returns 0.
    - `grep -c "createElementNS" src/hud/speedometer.ts` returns at least 8.
    - `grep -c "THROTTLE_MS\|accMs" src/hud/speedometer.ts` returns 0.
    - `grep -c "transition" src/hud/speedometer.ts` returns 0.
    - `src/hud/speedometer.ts` contains the literal substrings `z-index:5`, `right:16px`, `bottom:16px`, `pointer-events:none`, `role`, `aria-label`, `tabular-nums`, `#C8402F`, `#E8A33D`, `#F2EFE6`, `#0B0B0C`.
    - `src/hud/speedometer.ts` contains no `from "three"` and no `from "@dimforge/rapier3d"`.
  </acceptance_criteria>
  <done>A retro analog gauge renders bottom-right from pure TypeScript, its needle tracks `needleAngleDeg` exactly, and no DOM-injection or framerate-dependent path exists in it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physics numbers -> DOM text | Speed values become on-screen text every frame |
| module -> `document.body` | The gauge injects nodes into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering (DOM injection) | `createSpeedometer` element construction and per-frame writes | mitigate | Elements built exclusively with `createElementNS` + `appendChild`; text written only with `textContent`; styling only via `style.cssText` and `setAttribute`. `tests/layering.test.ts`'s repo-wide `innerHTML` scan is the automated gate, asserted in this plan's acceptance criteria. |
| T-02-10 | Denial of Service | per-frame DOM writes | accept | 3 `setAttribute` calls plus 1 `textContent` write per rAF is far inside the 16.6 ms frame budget; the profiler HUD from Phase 1 measures the frame cost and will surface a regression. The throttle is deliberately omitted (02-UI-SPEC.md). |
| T-02-11 | Information disclosure | HUD present in production build | accept | The speedometer is intentionally player-facing and always on. It exposes only the player's own vehicle speed. Unlike `src/debug/**` it is not gated, by design (NAV-01). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No install task in this plan; zero webfonts, zero icon library, zero UI framework (02-UI-SPEC.md Registry Safety: net third-party UI dependency is zero). |
</threat_model>

<verification>
1. `npx vitest run tests/speedometer.test.ts` — green, at least 12 cases.
2. `npx vitest run tests/speedometer.test.ts -t damping` — the framerate-independence case passes (02-VALIDATION.md pinned filter).
3. `npx vitest run tests/layering.test.ts` — `innerHTML` scan green with the new file present.
4. `npm run check` — green.
5. Manual (deferred to plan 02-10, SC4): the gauge's visual appearance and that the needle
   tracks real vehicle speed on screen is verified in the browser checkpoint, once the
   composition root wires it in plan 02-08.
</verification>

<success_criteria>
- `src/hud/` exists with `speedometer.ts` and imports neither `three` nor Rapier.
- The pure half (`mphFromGroundSpeed`, `needleAngleDeg`, `dampStep`, `readoutColour`) is fully Node-tested including all four 02-UI-SPEC.md needle check values and both clamp ends.
- The DOM half builds the full 02-UI-SPEC.md geometry with `createElementNS` only.
- Digit damping is framerate-independent and proven by test; the needle is unsmoothed and untransitioned.
</success_criteria>

<output>
Create `.planning/phases/02-vehicle-feel-core/02-03-SUMMARY.md` when done.
</output>
