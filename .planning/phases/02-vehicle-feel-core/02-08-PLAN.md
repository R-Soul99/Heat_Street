---
phase: 02-vehicle-feel-core
plan: 08
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-05", "02-06"]
files_modified:
  - src/loop.ts
  - tests/loop.test.ts
  - src/main.ts
autonomous: false
requirements: [VEH-01, VEH-02, VEH-04, NAV-01]

must_haves:
  truths:
    - "Opening the dev server shows a car on a plane that the player can drive with the keyboard"
    - "The speedometer needle and digits track the car's actual ground speed"
    - "The wheels visibly steer, spin and compress on their suspension"
    - "The Phase 1 debug scene files are still present and their regression suites still pass"
  artifacts:
    - path: "src/main.ts"
      provides: "Composition root wired to the vehicle scene, live input, vehicle view and speedometer"
    - path: "src/loop.ts"
      provides: "render(alpha, dtMs) — dtMs reaches the render callback so the HUD damping stays framerate-independent"
  key_links:
    - from: "src/main.ts"
      to: "src/hud/speedometer.ts"
      via: "speedo.update(Math.hypot(v.x, v.z), dtMs) inside render"
      pattern: "speedo\\.update"
    - from: "src/main.ts"
      to: "src/physics/vehicle-scene.ts"
      via: "applyInput: scene.applyInput wired into startLoop"
      pattern: "createVehicleScene"
    - from: "src/main.ts"
      to: "src/input/live-input.ts"
      via: "input: new LiveInputSource() replaces the NEUTRAL stub"
      pattern: "LiveInputSource"
---

<objective>
Wire the phase's first end-to-end user-visible slice: open the dev server, press a key, and
a muscle car moves on a plane with a live speedometer tracking it.

Purpose: this is the MVP vertical slice for the phase goal — "the player can drive one
muscle car". Everything before this plan is contracts and headless proofs; this is where a
human first sees the thing. It deliberately lands before the dev tooling (plan 02-09),
because discovering "the car does not render" during the tuning session would be far more
expensive than discovering it here.

Output: a modified `src/loop.ts`, `tests/loop.test.ts` and `src/main.ts`, plus a browser
checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vehicle-feel-core/02-PATTERNS.md
@.planning/phases/02-vehicle-feel-core/02-UI-SPEC.md
@src/main.ts
@src/loop.ts
@src/physics/vehicle-scene.ts
@src/render/vehicle-view.ts
@src/hud/speedometer.ts
@src/input/live-input.ts
@src/core/vehicle-tuning.ts
@tests/loop.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the dtMs signature gap — render(alpha, dtMs)</name>
  <files>src/loop.ts, tests/loop.test.ts</files>
  <read_first>
    - src/loop.ts (read lines 107-250 — the `LoopDeps` interface at 112-133, the frame function's `dtMs` computation at 175, the single `deps.render(clock.alpha(nowMs))` call at 220, and the `deps.hud?.(stats, dtMs)` call at 226-236)
    - tests/loop.test.ts (the `makeDeps` helper and every `render` spy — confirm none of them assert on the argument list before changing the signature)
    - src/debug/profiler-hud.ts (lines 65-73 and 100-110 — how `dtMs` already flows into `Hud.update` and why the HUD is called after render returns)
    - .planning/phases/02-vehicle-feel-core/02-PATTERNS.md (section `src/main.ts (MODIFY)` — the flagged signature gap between 02-UI-SPEC.md's `speedo.update(groundSpeedMs, dtMs)` contract and the shipped `LoopDeps.render(alpha)`)
    - .planning/phases/02-vehicle-feel-core/02-UI-SPEC.md ("Call 2" signature contract)
  </read_first>
  <action>
This is the one decision 02-PATTERNS.md flagged as ambiguous, and it is decided here rather
than left to the executor. 02-UI-SPEC.md contracts `speedo.update(groundSpeedMs, dtMs)`
called once per rAF from the render callback, but the shipped `LoopDeps.render(alpha)`
receives only `alpha`; `dtMs` currently reaches only the separate `hud` callback.

DECISION: extend `LoopDeps.render` to `render(alpha: number, dtMs: number): void` and pass
the already-computed `dtMs` at the single call site. Rejected alternative: routing the
speedometer through the existing `hud` callback — `hud` is constructed only when
`DEBUG_ENABLED` is true, and the speedometer is player-facing and always on, so that
routing would silently make the gauge a debug-only feature. Record this rejection as a
comment at the `render` field in `LoopDeps`.

Change exactly two things in `src/loop.ts`:
  1. `LoopDeps.render(alpha: number, dtMs: number): void`, with its doc comment extended to
     say `dtMs` is the wall-clock delta since the previous frame, supplied so a HUD can do
     framerate-independent smoothing without reading a clock itself — which is what keeps
     `performance.now(` confined to this file and `src/debug/` under
     `tests/layering.test.ts`'s existing rule.
  2. line 220: `deps.render(clock.alpha(nowMs), dtMs)`.
Nothing else in `src/loop.ts` changes. Do NOT touch the step loop, the clamp/rebaseline
logic, or the `hud` call — Phase 1 spent seven plans on those and `tests/determinism.test.ts`
depends on them.

Add one case to `tests/loop.test.ts` asserting `render` receives two arguments and that the
second is a finite number equal to the wall-clock delta the fake scheduler produced for that
frame. Also assert it is 0 or absent on the very first (start-only) frame, matching the
existing "only starts the clock: zero steps, zero renders" behaviour.
  </action>
  <verify>
    <automated>npx vitest run tests/loop.test.ts</automated>
    <automated>npx vitest run tests/determinism.test.ts tests/sim-clock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/loop.test.ts` exits 0 with every pre-existing case still passing plus the new `dtMs` case.
    - `npx vitest run tests/determinism.test.ts tests/sim-clock.test.ts` exits 0 — the Phase 1 fixed-timestep proofs are unaffected.
    - `git diff src/loop.ts` shows exactly two changed regions: the `render` field in `LoopDeps` and the `deps.render(...)` call.
    - `src/loop.ts` contains a comment naming the rejected `hud`-callback routing and why it was rejected.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>`dtMs` reaches the render callback, so the speedometer's damping can be framerate-independent without reading a clock.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the composition root at the vehicle scene</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (read in full — the module doc explaining this file is the trust boundary; the explicit-contract-at-the-call-site comment at 40-47; the Phase 2 hook comment at 49-57; the `DEBUG_ENABLED ? createHud(...) : null` gating at 59-64; the `startLoop` deps wiring at 66-80 including the load-bearing ordering comment at 73-75)
    - src/physics/vehicle-scene.ts (plan 02-06 — `createVehicleScene`, `VehicleScene.bodies`, `applyInput`, `preTick`, `setTuning`)
    - src/render/vehicle-view.ts (plan 02-06 — `createVehicleView(wheelRadius, halfTrack, halfWheelbase, chassisHalfExtents)`, `meshes`, `updateWheels`)
    - src/hud/speedometer.ts (plan 02-03 — `createSpeedometer()` and `update(groundSpeedMs, dtMs)`)
    - src/input/live-input.ts (plan 02-02 — `LiveInputSource`)
    - src/core/vehicle-tuning.ts (plan 02-01 — `defaultTuning()`, `parseSavedTuning`, `TUNING_STORAGE_KEY`)
    - src/loop.ts (task 1's output — the new `render(alpha, dtMs)` signature)
    - .planning/phases/02-vehicle-feel-core/02-PATTERNS.md (section `src/main.ts (MODIFY)`, including the load-bearing ordering constraint on `speedo.update`)
  </read_first>
  <action>
Rewire `src/main.ts`. Keep its existing module doc comment (this file is the trust boundary
where a layering violation is easiest to introduce and hardest to see) and extend it with a
sentence recording that Phase 2 supersedes the Phase 1 debug scene HERE and only here.

Add a prominent comment stating that `src/physics/debug-scene.ts` and
`src/render/debug-scene.ts` are deliberately LEFT IN PLACE and are no longer imported by
this file. They are now regression fixtures: `tests/determinism.test.ts` (7 sites),
`tests/transform-cache.test.ts` and `tests/loop.test.ts` all import `createDebugScene`, and
`tests/determinism.test.ts` is the VEH-03 proof CLAUDE.md project constraint 3 says must not
regress. Deleting them is a separately planned task, never a side effect of this phase.

Wiring, replacing the Phase 1 body:
  - `const tuning = parseSavedTuning(typeof localStorage !== "undefined" ? localStorage.getItem(TUNING_STORAGE_KEY) : null) ?? defaultTuning();`
    Persisted values are restored at boot so an HMR reload does not lose a tuning session
    (D-17); `parseSavedTuning` is what makes the untrusted blob safe (threat T-02-01).
  - `const world = createWorld(); const scene = createVehicleScene(world, tuning);`
  - `const transforms = new TransformCache(scene.bodies);`
  - `const { renderer, camera } = createRenderer(canvas);`
  - `const view = createVehicleView(tuning.wheels.radius, tuning.wheels.halfTrack, tuning.wheels.halfWheelbase, tuning.chassis.halfExtents);`
    Pass the geometry EXPLICITLY at this call site rather than letting the render module
    import `VehicleTuning`, preserving the "explicit contract at the call site" convention
    the current file already uses for `scene.bodies.length` / `scene.spinnerIndex`
    (threat T-01-16).
  - `const input = new LiveInputSource();` — this replaces the `NEUTRAL` stub the Phase 1
    comment at lines 49-57 anticipated. Delete that stub and its now-stale comment.
  - `const speedo = createSpeedometer();` — always constructed, NOT gated on
    `DEBUG_ENABLED`. It is player-facing (NAV-01).
  - Keep the existing profiler-HUD gating block exactly as it is.
  - Camera: position it so the car is clearly visible and the ramp is in frame. This phase
    has no helicopter camera (that is Phase 3) — use a simple fixed or follow chase camera
    and comment that it is a temporary placeholder for the SC5 playtest, per 02-RESEARCH.md
    Open Question 1's recommendation that the feel session not be judged through an unusable
    view.

`startLoop` deps:
  - `applyInput: scene.applyInput`, `onTickBegin: scene.preTick` — unchanged shape, so
    `src/loop.ts:196-197` still guarantees `applyInput` runs immediately before
    `world.step()`, which is the ordering 02-RESEARCH.md Pattern 2 requires.
  - `render(alpha, dtMs)` body, in THIS order, and preserve the existing load-bearing
    ordering comment verbatim in substance:
      1. `const v = scene.vehicle.body.linvel(); speedo.update(Math.hypot(v.x, v.z), dtMs);`
         Ground speed computed here, never `currentVehicleSpeed()` — that returns the full
         3D magnitude including vertical velocity and its sign is numerical noise for a
         -Z-forward car (Pitfall 1). The speedometer update MUST NOT sit between
         `applyAllInterpolated` and `renderer.render`: the profiler HUD reads
         `renderer.info` after `render()` returns and that read is valid only because there
         is exactly one draw call per frame. Put it first.
      2. `view.updateWheels(scene.vehicle.controller);`
      3. `applyAllInterpolated(view.meshes, transforms, alpha);`
      4. `renderer.render(view.scene, camera);`
  - `hud:` unchanged.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>npx vitest run tests/layering.test.ts tests/determinism.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (typecheck + Biome + full suite).
    - `npm run build` exits 0 and emits a bundle.
    - `src/main.ts` contains `createVehicleScene`, `createVehicleView`, `LiveInputSource`, `createSpeedometer` and `parseSavedTuning`, and contains no `createDebugScene` or `createDebugRenderScene` import.
    - `git status --porcelain src/physics/debug-scene.ts src/render/debug-scene.ts` is empty and both files still exist.
    - `npx vitest run tests/determinism.test.ts tests/transform-cache.test.ts tests/loop.test.ts` exits 0.
    - In `src/main.ts`'s `render` body, `speedo.update` appears BEFORE `applyAllInterpolated`, and `renderer.render` is the last statement.
    - `src/main.ts` contains no `currentVehicleSpeed`.
  </acceptance_criteria>
  <done>The dev server boots into the vehicle scene with live keyboard input and a live speedometer, and every Phase 1 regression suite is still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser check — the car drives and the gauge tracks it</name>
  <files>none</files>
  <read_first>
    - src/main.ts (task 2's output — so the reported behaviour can be traced to the wiring)
    - .planning/phases/02-vehicle-feel-core/02-UI-SPEC.md ("Speedometer Geometry Contract" and the Color section — the visual contract being checked)
  </read_first>
  <action>
Pause and hand control to the developer. Start the dev server with `npm run dev`, print the
URL, then present the `<how-to-verify>` steps below verbatim and wait. Do not proceed, do not
self-approve, and do not substitute a screenshot or a headless check -- this task exists
because the previous two tasks are only provably correct on a real screen. If the developer
reports a defect, fix it and re-present the same steps for a second pass; record both passes
in the SUMMARY.
  </action>
  <what-built>
The composition root now builds a Rapier vehicle on a flat plane with a ramp, renders a
chassis and four wheels, feeds it live keyboard and gamepad input through the fixed tick,
and drives a retro analog speedometer from the chassis ground speed once per animation frame.
`npm run check` and `npm run build` both pass, and every Phase 1 regression suite is green.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open the printed URL (no query parameters).
2. Confirm a car-shaped chassis with four visible wheels sits on a ground plane, and the ramp
   is visible in frame.
3. Hold W (or Up arrow). The car should accelerate away smoothly and the rear wheels should
   visibly spin.
4. Hold A then D (or Left/Right). The FRONT wheels should visibly turn, and the car should
   turn LEFT on A and RIGHT on D. A wrong direction here means the `AXLE` sign or the steer
   negation is inverted.
5. Watch the speedometer in the bottom-right: the needle must sweep clockwise from its
   lower-left rest position as you accelerate, and the digital number must rise with it and
   settle rather than jitter. At around 120 mph the needle enters the amber band and the
   digits turn amber.
6. Release the throttle and hold S (or Down arrow). The car should slow and the nose should
   visibly DIP (weight transfer under braking).
7. Press and hold Space while turning at speed. The rear should step out into a slide.
   Release Space and steer into the slide — it should recover.
8. Drive up the ramp at speed. The car should CLIMB it and launch, not stop dead against its
   leading edge, and should land on its wheels rather than its roof.
9. Press the backtick key — nothing should happen, because `?debug` is absent. Then reload
   with `?debug` appended and press backtick: the Phase 1 profiler HUD should appear.
10. Report any of these that fail, with what you saw instead.

Not being judged yet: whether the car FEELS like a heavy muscle car. That is SC1/SC5 and is
the tuning session in plan 02-10, after the tuning panel exists.
  </how-to-verify>
  <verify>
    <automated>npm run check</automated>
    <human-check>Steps 1-10 above, performed in a desktop browser.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer confirms the car is visible, drives, steers in the correct direction, slides on handbrake, climbs the ramp and lands upright.
    - The developer confirms the needle sweeps with speed, the digits track it without jitter, and both turn amber at 120 mph.
    - The developer confirms the profiler HUD is absent without `?debug` and present with it.
    - Any failure is reported with the observed behaviour; the SUMMARY records both the failures found and the fixes applied.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what you saw instead.</resume-signal>
  <done>A human has confirmed the phase's core vertical slice works on screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` -> boot-time tuning | The composition root reads a user-editable blob before building the vehicle |
| `?debug` -> debug surface existence | Decides whether any debug overlay or listener is created |
| composition root -> layering | `src/main.ts` is where a layering violation is easiest to introduce and hardest to see |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / DoS | boot-time `localStorage.getItem(TUNING_STORAGE_KEY)` | mitigate | Piped through `parseSavedTuning` (plan 02-01), which try/catches the parse, rejects non-objects, and range-clamps every leaf; falls back to `defaultTuning()` on any failure. Never `JSON.parse` directly at this call site. |
| T-02-03 | Information disclosure | debug surface in a production build | mitigate | The `DEBUG_ENABLED ? createHud(...) : null` gate is preserved unchanged; the speedometer is deliberately ungated because it is player-facing (NAV-01), and it exposes only the player's own speed. |
| T-01-15 / T-01-16 | Tampering | render/sim boundary and mesh-to-body index contract | mitigate | Wheel geometry and mesh counts are passed explicitly at this call site rather than imported, keeping the contract visible; `tests/layering.test.ts` polices `src/render/`, `src/hud/` and `src/input/` automatically. |
| T-02-20 | Tampering | Phase 1 regression fixtures deleted as a side effect | mitigate | `src/physics/debug-scene.ts` and `src/render/debug-scene.ts` are explicitly NOT deleted; an acceptance criterion asserts `git status --porcelain` shows them unmodified and the three importing suites green. |
| T-02-02 | Tampering | DOM injection | mitigate | No `innerHTML` anywhere; the pre-existing `src/main.ts:21` occurrence is inside a comment documenting the prohibition and is exempt from `tests/layering.test.ts`'s comment-skipping scan (recorded in STATE.md under [01-06]). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No install task; `package.json` must be unchanged. |
</threat_model>

<verification>
1. `npm run check` — typecheck, Biome and the full suite green.
2. `npm run build` — production bundle builds.
3. `npx vitest run tests/determinism.test.ts tests/transform-cache.test.ts tests/loop.test.ts` — the three suites importing the Phase 1 debug scene are green, and both `debug-scene.ts` files show zero diff.
4. `npx vitest run tests/layering.test.ts` — all tiers green with the composition root rewired.
5. Human browser checkpoint (task 3), steps 1-10.
</verification>

<success_criteria>
- `src/loop.ts` passes `dtMs` to `render`, with the rejected alternative recorded in a comment.
- `src/main.ts` builds the vehicle scene, the vehicle view, the live input source and the speedometer, with the speedometer update placed outside the interpolate-then-draw pair.
- Both `debug-scene.ts` files are untouched and all three Phase 1 suites that import them pass.
- A human has driven the car in a browser and confirmed steering direction, braking dive, handbrake slide, ramp launch and gauge tracking.
</success_criteria>

<output>
Create `.planning/phases/02-vehicle-feel-core/02-08-SUMMARY.md` when done.
</output>
