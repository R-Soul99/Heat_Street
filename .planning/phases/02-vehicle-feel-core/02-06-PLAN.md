---
phase: 02-vehicle-feel-core
plan: 06
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/physics/vehicle-scene.ts
  - src/render/vehicle-view.ts
  - tests/vehicle-scene.test.ts
autonomous: true
requirements: [VEH-01, VEH-04]

must_haves:
  truths:
    - "A flat plane with a drivable ramp exists and the car can be driven onto it, not into its leading edge"
    - "Hitting the ramp at speed launches the car airborne and it lands driveable"
    - "The car and its four wheels are visible, with wheels that steer, spin and travel on their suspension"
  artifacts:
    - path: "src/physics/vehicle-scene.ts"
      provides: "createVehicleScene(world, tuning) -> flat ground + drivable ramp + one Vehicle, with preTick/applyInput"
      exports: ["createVehicleScene", "VehicleScene", "RAMP_APPROACH_Z", "SPAWN"]
      min_lines: 130
    - path: "src/render/vehicle-view.ts"
      provides: "chassis mesh + 4 wheel meshes + ground/ramp visuals; per-frame wheel rig"
      exports: ["createVehicleView", "VehicleView"]
      min_lines: 150
    - path: "tests/vehicle-scene.test.ts"
      provides: "Ramp geometry proof — the car climbs it and goes airborne rather than hitting a wall"
  key_links:
    - from: "src/physics/vehicle-scene.ts"
      to: "src/physics/vehicle.ts"
      via: "createVehicle(world, tuning, SPAWN)"
      pattern: "createVehicle"
    - from: "src/render/vehicle-view.ts"
      to: "the Rapier vehicle controller"
      via: "read-only wheelSuspensionLength / wheelSteering / wheelRotation / wheelAxleCs"
      pattern: "wheelSuspensionLength"
---

<objective>
Build the world the car drives in and the meshes that show it: a flat plane with one
drivable ramp on the physics side, and a chassis + four-wheel visual rig on the render side.

Purpose: VEH-04 and ROADMAP SC3 ("hit a ramp at 120mph and land driveable"). 02-RESEARCH.md
Open Question 2 is explicitly UNSOLVED — the researcher's probe built the ramp as a rotated
cuboid and the car hit its vertical leading edge instead of driving up it. That makes ramp
geometry its own task with its own verification, not an assumption baked into a larger plan.

Output: `src/physics/vehicle-scene.ts`, `src/render/vehicle-view.ts`,
`tests/vehicle-scene.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vehicle-feel-core/02-RESEARCH.md
@.planning/phases/02-vehicle-feel-core/02-PATTERNS.md
@src/physics/vehicle.ts
@src/physics/debug-scene.ts
@src/physics/world.ts
@src/physics/transform-cache.ts
@src/render/debug-scene.ts
@src/render/interpolator.ts
@docs/frame-budget.md
</context>

<interfaces>
<!-- Mirrors the shipped DebugScene contract so src/loop.ts needs no change. -->
export interface VehicleScene {
  /** Every body the render layer draws, in dense index order. Chassis is index 0. Excludes ground and ramp. */
  readonly bodies: readonly RAPIER.RigidBody[];
  readonly vehicle: Vehicle;
  preTick(tick: number): void;
  applyInput(frame: InputFrame): void;      // calls vehicle.tick(frame, tuning); loop.ts then steps
  setTuning(t: VehicleTuning): void;        // the tuning panel's write path
  dispose(): void;
}
export function createVehicleScene(world: RAPIER.World, tuning: VehicleTuning): VehicleScene

export interface VehicleView {
  readonly scene: THREE.Scene;
  /** Index-aligned with VehicleScene.bodies — index 0 is the chassis. Fed to applyAllInterpolated. */
  readonly meshes: readonly THREE.Object3D[];
  /** Per-frame wheel rig: suspension travel, steer angle, roll. Reads the controller, writes only meshes. */
  updateWheels(vc: RAPIER.DynamicRayCastVehicleController): void;
  dispose(): void;
}
export function createVehicleView(wheelRadius: number, halfTrack: number, halfWheelbase: number,
                                  chassisHalfExtents: {x,y,z}): VehicleView
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Vehicle scene — flat plane plus a ramp the car can actually climb</name>
  <files>src/physics/vehicle-scene.ts, tests/vehicle-scene.test.ts</files>
  <read_first>
    - src/physics/debug-scene.ts (read in full — the `Layering:` header, the `bodies` dense-index contract doc comment, the `create*(world)` factory returning an object literal with `preTick`/`applyInput`, the `GROUND_HALF_EXTENTS` constant, and the ground construction)
    - src/physics/vehicle.ts (plan 02-04's output — `createVehicle`, `Vehicle.tick`, `sampleVehicle`)
    - src/physics/world.ts (how `createWorld()` sets the timestep from `DT`)
    - tests/determinism.test.ts (lines 33-45 `stepScene`; lines 129-134 and 158-166 the anti-trivially-green convention)
    - .planning/phases/02-vehicle-feel-core/02-RESEARCH.md ("Open Questions" item 2 — the three candidate ramp geometries and the recorded failure; Pitfall 6 CCD and collider thickness; CLAUDE.md constraint 10 on `TriMeshFlags.FIX_INTERNAL_EDGES` value 144)
  </read_first>
  <action>
Create `src/physics/vehicle-scene.ts` with the `src/physics/` layering header (may import
`@dimforge/rapier3d` and `src/core/`; must not import `three`; no DOM, no wall clock).

Add a prominent doc comment stating this file SUPERSEDES `src/physics/debug-scene.ts` at
the composition root ONLY. `src/physics/debug-scene.ts` and `src/render/debug-scene.ts` must
NOT be deleted or edited: `tests/determinism.test.ts` (7 import sites),
`tests/transform-cache.test.ts` and `tests/loop.test.ts` all import `createDebugScene`, and
`tests/determinism.test.ts` is the VEH-03 proof that CLAUDE.md project constraint 3 says
must not regress. They are regression fixtures now, and retiring them is a separately
planned task, never a side effect of this phase.

Ground: a fixed cuboid at least 1 m thick (Pitfall 6 — 120 mph is 0.894 m of travel per
fixed tick, so anything thinner is a tunnelling candidate), large enough for the telemetry
routines: half-extents at least `{x: 200, y: 1, z: 400}`, top surface at y = 0, friction 1.0.

Ramp: the unsolved piece. Requirements: the car driving along -Z at speed must ROLL UP the
incline, never strike a vertical leading edge. Author it as a `ColliderDesc.convexHull` wedge
whose leading edge is a knife edge at exactly y = 0 where it meets the ground, rising to a
crest. Target geometry: 12 m long along Z, 8 m wide along X, 1.6 m tall at the crest
(approximately a 7.6 degree incline), placed so its foot is at `RAMP_APPROACH_Z` and it
rises in the -Z direction. Export `RAMP_APPROACH_Z` and `SPAWN` as named constants so the
telemetry routines in plan 02-07 and the tests here can position runs against them without
duplicating a literal.

If `convexHull` returns null or the wedge still catches, fall back IN ORDER and record which
option shipped in a `DEVIATION`-style comment naming the evidence: (a) a rotated cuboid sunk
deep enough into the ground that its leading edge is BELOW y = 0; (b)
`ColliderDesc.trimesh(verts, indices, TriMeshFlags.FIX_INTERNAL_EDGES)` — the flag value is
144 and CLAUDE.md forbids trimesh without it, because internal-edge bumps read as "the
physics is broken" and are very hard to diagnose after the fact. Do not ship a ramp that
merely "usually works": the test below is the gate.

`createVehicleScene(world, tuning)` builds the ground, the ramp, and exactly one
`createVehicle(world, tuning, SPAWN)` where `SPAWN` places the chassis about 0.6 m above the
ground, well clear of the ramp, facing -Z. It returns the `VehicleScene` object literal from
this plan's `<interfaces>` block. `bodies` contains the chassis body and NOTHING else —
comment the dense-index contract exactly as `debug-scene.ts` does, since
`src/render/vehicle-view.ts` and `TransformCache` both key on this order (threat T-01-16).
`preTick` is a no-op this phase but must exist so the loop wiring shape is unchanged;
comment that it is where Phase 3's surface lookup will live. `applyInput(frame)` calls
`vehicle.tick(frame, currentTuning)` and nothing else — `src/loop.ts:196-197` guarantees
`applyInput` runs immediately before `world.step()`, which is exactly the ordering
02-RESEARCH.md Pattern 2 requires, so NO change to `src/loop.ts` is needed for the tick path.
`setTuning(t)` stores the reference and calls `vehicle.applyTuning(t)`.

Create `tests/vehicle-scene.test.ts` in `tests/determinism.test.ts`'s style, using a
`stepScene`-shaped helper that builds a fresh world, a fresh scene, and drives scripted
`InputFrame`s. Cases:
  - "builds ground, ramp and exactly one chassis body": `world.bodies.len()` and
    `scene.bodies.length === 1`, and `world.colliders.len()` counts ground + ramp + chassis.
  - "the car rests on the ground at spawn": after 120 ticks of NEUTRAL input the chassis
    y-position is stable within 1 cm across the last 30 ticks and all four wheels report
    contact.
  - "ramp is climbable, not a wall": drive full throttle straight at the ramp from the spawn
    until the chassis passes `RAMP_APPROACH_Z`, and assert the chassis y-position INCREASED
    monotonically over the climb while forward speed retained at least 60% of its
    pre-ramp value. A car that hit a vertical edge loses nearly all forward speed — this
    assertion is what distinguishes a working ramp from the researcher's failed probe.
  - "ramp launches the car airborne": continuing the same run, assert at least one tick where
    zero wheels are in contact and the chassis y-position exceeds the crest height.
  - "lands driveable off the ramp": 0.5 s of sim time after touchdown, chassis tilt is under
    20 degrees and forward ground speed exceeds 40 mph.
  - anti-trivially-green companion, in `determinism.test.ts:158-166`'s shape: the same
    approach run with `throttle: 0` never leaves the ground, so the airborne assertion above
    cannot be passing for a reason unrelated to the ramp.
  </action>
  <verify>
    <automated>npx vitest run tests/vehicle-scene.test.ts</automated>
    <automated>npx vitest run tests/determinism.test.ts tests/transform-cache.test.ts tests/loop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/vehicle-scene.test.ts` exits 0 with at least 6 passing cases including the "ramp is climbable, not a wall" case.
    - `git status --porcelain src/physics/debug-scene.ts src/render/debug-scene.ts` shows NO modification and both files still exist.
    - `npx vitest run tests/determinism.test.ts tests/transform-cache.test.ts tests/loop.test.ts` exits 0 — the Phase 1 VEH-03 proof is untouched.
    - `src/physics/vehicle-scene.ts` exports `RAMP_APPROACH_Z` and `SPAWN` and contains no `from "three"`.
    - The shipped ramp collider approach is named in a comment together with the evidence for choosing it over the other two candidates.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>A car spawned in this scene rests on the ground, drives up the ramp without catching, and lands driveable — proven headlessly, not by eye.</done>
</task>

<task type="auto">
  <name>Task 2: Chassis and wheel meshes with the per-frame wheel rig</name>
  <files>src/render/vehicle-view.ts</files>
  <read_first>
    - src/render/debug-scene.ts (read in full — the `Layering:` header at 17-19, the THE INDEX CONTRACT doc block at 9-16, the validate-parameters-and-throw pattern at 67-74 and 146-148, the shared-geometry/material pattern at 96-106 with its draw-call budget comment, and the do-not-seed-a-pose rule at 117-120)
    - src/render/interpolator.ts (the `applyAllInterpolated(targets, transforms, alpha)` contract: target array index `i` maps to buffer offset `i * XFORM_STRIDE`)
    - src/physics/vehicle.ts (the `FL/FR/RL/RR = 0/1/2/3` index constants this file's wheel array must mirror)
    - .planning/phases/02-vehicle-feel-core/02-RESEARCH.md ("Code Examples > Wheel visual rig" — the one half of the three.js example that IS worth copying, including the `T | null` return of every wheel getter)
    - .planning/phases/02-vehicle-feel-core/02-PATTERNS.md (section `src/render/vehicle-view.ts` — the weakened layering clause, `import type * as RAPIER`, and the do-not-seed-a-pose exception for wheels)
    - docs/frame-budget.md (the draw-call and triangle targets the scene must stay inside)
  </read_first>
  <action>
Create `src/render/vehicle-view.ts`.

Layering header, adapted from `src/render/debug-scene.ts:17-19`: `src/render/` reads
simulation state and never writes it. State the deviation explicitly — unlike
`debug-scene.ts`, this file DOES read the Rapier vehicle controller
(`wheelSuspensionLength`, `wheelSteering`, `wheelRotation`, `wheelAxleCs`,
`wheelChassisConnectionPointCs`), so the clause weakens from "touches Rapier not at all" to
"reads Rapier, never writes". Note that `tests/layering.test.ts:119` enforces only the write
ban (`world.step|applyImpulse|setTranslation|setRotation|setNextKinematic`) and none of the
wheel getters match it. Use `import type * as RAPIER` (the `transform-cache.ts:19`
precedent), since this file only reads.

Restate the INDEX CONTRACT doc block for this file: `meshes` index 0 is the chassis and
maps onto `VehicleScene.bodies[0]`, and the wheel mesh array index order is
`FL/FR/RL/RR = 0/1/2/3` — a mismatch draws each wheel at another wheel's transform, which
reads as "the physics is broken" and is painful to diagnose after the fact.

`createVehicleView(wheelRadius, halfTrack, halfWheelbase, chassisHalfExtents)` takes wheel
geometry as PARAMETERS rather than importing `VehicleTuning`, following
`createDebugRenderScene`'s reasoning — this keeps the module a pure render concern and puts
the geometry contract at the composition-root call site. Validate every parameter with the
`RangeError` pattern from `debug-scene.ts:67-74` (positive finite numbers).

Build:
  - a `THREE.Scene` with the same lighting setup `src/render/debug-scene.ts` uses, so the
    two scenes read consistently.
  - a ground plane visual and a ramp visual matching the physics geometry from task 1.
    These are static and are NOT in the `meshes` array.
  - a chassis `THREE.Mesh` box from `chassisHalfExtents`, added to the scene and returned as
    `meshes[0]`.
  - four wheel meshes from ONE shared `CylinderGeometry` and ONE shared material, rotated so
    the cylinder axis lies along X. Copy the shared-geometry/material comment from
    `debug-scene.ts:96-106` including its draw-call justification. Each wheel is added as a
    CHILD of the chassis mesh, positioned at `{+/-halfTrack, connectionY, +/-halfWheelbase}`
    in FL/FR/RL/RR order — so the chassis interpolation from `applyAllInterpolated` carries
    them and only their LOCAL transform needs updating.
  - copy the do-not-seed-a-pose comment from `debug-scene.ts:117-120` for the CHASSIS mesh,
    and immediately note the exception: the wheel meshes' local transforms ARE owned by this
    module and written every frame by `updateWheels`, so the rule does not apply to them.

`updateWheels(vc)` — per wheel index i in 0..3:
  `wheelMeshes[i].position.y = (vc.wheelChassisConnectionPointCs(i)?.y ?? 0) - (vc.wheelSuspensionLength(i) ?? 0)`,
  then compose `quaternion` from a steer rotation about world up by `vc.wheelSteering(i) ?? 0`
  times a roll rotation about `vc.wheelAxleCs(i)` by `vc.wheelRotation(i) ?? 0`, using
  `multiplyQuaternions(steerQ, rollQ)` with module-scope scratch objects reused every call
  (the `interpolator.ts` no-per-frame-allocation convention). Every getter returns `T | null`
  because Rapier returns null for an out-of-range index — handle that with `?? 0` rather than
  a non-null assertion, and comment why.

`dispose()` disposes the geometries and materials this module created.
  </action>
  <verify>
    <automated>npx vitest run tests/layering.test.ts</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0.
    - `npx vitest run tests/layering.test.ts` exits 0 — in particular the `src/render never writes simulation state` block stays green with `vehicle-view.ts` present.
    - `src/render/vehicle-view.ts` contains `import type * as RAPIER` and contains none of `world.step`, `applyImpulse`, `setTranslation`, `setRotation`, `setNextKinematic` on a non-comment line.
    - `grep -c "new THREE.CylinderGeometry" src/render/vehicle-view.ts` returns 1 — one shared wheel geometry, not four.
    - `grep -c "!\." src/render/vehicle-view.ts` shows no non-null assertions on the Rapier wheel getters; `?? 0` appears at least 4 times.
    - `createVehicleView` throws a `RangeError` for a zero or negative `wheelRadius` (assert by a one-line check in the SUMMARY or an added case in `tests/vehicle-scene.test.ts` — the executor may add it there since that file is owned by this plan).
  </acceptance_criteria>
  <done>The car is drawable: a chassis mesh the interpolator poses, plus four wheels that steer, spin and travel on their suspension, built from one shared geometry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physics state -> render meshes | The render layer reads simulation state; a write here would make render cadence affect medal times |
| high-speed chassis -> static colliders | 120 mph is 0.894 m of travel per fixed tick |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Denial of Service | ramp / ground collider tunnelling at 120 mph | mitigate | Ground and ramp colliders at least 1 m thick; `setCcdEnabled(true)` on the chassis (plan 02-04). The `ramp launches the car airborne` and `lands driveable off the ramp` cases would fail if the car tunnelled. |
| T-01-15 | Tampering | `src/render/vehicle-view.ts` writing simulation state | mitigate | Read-only access via wheel getters; `import type * as RAPIER`; enforced by the existing `src/render/` block in `tests/layering.test.ts` and asserted in this plan's acceptance criteria. |
| T-01-16 | Tampering | mesh-to-body index mismatch | mitigate | The dense-index contract is documented in both `vehicle-scene.ts` and `vehicle-view.ts`, geometry is passed as explicit parameters at the composition root, and `createVehicleView` range-validates them with `RangeError`. |
| T-02-16 | Denial of Service | per-frame allocation in `updateWheels` | mitigate | Module-scope scratch quaternions and vectors reused every call, following `src/render/interpolator.ts`'s stated convention; one shared wheel geometry and material keeps the scene inside `docs/frame-budget.md`'s draw-call target. |
| T-02-02 | Tampering | DOM injection | accept | Neither file touches the DOM. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No install task in this plan; `three@0.185.1` is already pinned. |
</threat_model>

<verification>
1. `npx vitest run tests/vehicle-scene.test.ts` — green, ramp-climb and ramp-launch cases pass.
2. `npx vitest run tests/determinism.test.ts tests/transform-cache.test.ts tests/loop.test.ts` — the Phase 1 fixtures are untouched and green. `src/physics/debug-scene.ts` and `src/render/debug-scene.ts` must show zero diff.
3. `npx vitest run tests/layering.test.ts` — render write-ban green.
4. `npm run check` — green.
5. Manual (deferred to plan 02-08's browser checkpoint): that the car and its wheels are
   actually VISIBLE and that the wheels visibly steer and compress is a browser check, not
   an automated one.
</verification>

<success_criteria>
- A flat plane plus a ramp the car climbs rather than collides with, with the chosen collider approach and its evidence recorded in a comment.
- `VehicleScene` mirrors the shipped `DebugScene` contract so `src/loop.ts` needs no change.
- `src/physics/debug-scene.ts` and `src/render/debug-scene.ts` are neither edited nor deleted, and all three Phase 1 suites that import them stay green.
- Chassis and four wheels render from one shared geometry, index-aligned with the physics bodies, with the wheel rig reading Rapier and writing only meshes.
</success_criteria>

<output>
Create `.planning/phases/02-vehicle-feel-core/02-06-SUMMARY.md` when done.
</output>
