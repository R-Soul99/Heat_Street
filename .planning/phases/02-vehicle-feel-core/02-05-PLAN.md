---
phase: 02-vehicle-feel-core
plan: 05
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - src/debug/debug-gate.ts
  - tests/debug-gate.test.ts
  - tests/layering.test.ts
autonomous: true
requirements: [VEH-02, NAV-01]

must_haves:
  truths:
    - "Three debug overlays can be toggled independently on three different keys"
    - "A debug hotkey does not fire while the developer is typing into a numeric field"
    - "src/input/ and src/hud/ are policed by automated layering rules, not by convention"
  artifacts:
    - path: "src/debug/debug-gate.ts"
      provides: "onDebugKey(code, fn) general form with an activeElement focus guard; onDebugToggle kept as an alias"
      exports: ["parseDebugFlag", "DEBUG_ENABLED", "onDebugKey", "onDebugToggle", "isTextEntryFocused"]
    - path: "tests/layering.test.ts"
      provides: "src/input/** and src/hud/** rule blocks, each with a scanned-at-least-one guard"
    - path: "tests/debug-gate.test.ts"
      provides: "Coverage of the keyed variant and the focus guard"
  key_links:
    - from: "src/debug/debug-gate.ts"
      to: "src/main.ts and src/debug/tuning-panel.ts"
      via: "onDebugKey(\"KeyG\", …) / onDebugKey(\"KeyT\", …)"
      pattern: "onDebugKey"
---

<objective>
Close 02-RESEARCH.md Open Question 4 in code: extend `src/debug/debug-gate.ts` with a keyed
hotkey variant plus the focus guard the file's own standing NOTE has been waiting for, and
add the automated layering rules that police the two new source tiers `src/input/` and
`src/hud/`.

Purpose: the tuning panel (plan 02-09) and the telemetry panel need their own toggles, and
every lil-gui numeric control is a real `<input>` element — so without a focus guard,
typing a `g` into a tuning field would toggle the panel mid-edit. That is a live defect, not
speculative hardening. The layering rules are the mechanism that stops `src/input/` and
`src/hud/` from silently acquiring a `three` import or a wall-clock read in Phase 3.

Output: three modified files. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vehicle-feel-core/02-UI-SPEC.md
@.planning/phases/02-vehicle-feel-core/02-PATTERNS.md
@src/debug/debug-gate.ts
@src/main.ts
@tests/debug-gate.test.ts
@tests/layering.test.ts
</context>

<interfaces>
<!-- Existing, unchanged -->
export function parseDebugFlag(search: string): boolean
export const DEBUG_ENABLED: boolean
export function onDebugToggle(fn: () => void): void      // becomes onDebugKey("Backquote", fn)

<!-- New this plan -->
export function onDebugKey(code: string, fn: () => void): void
export function isTextEntryFocused(el: Element | null): boolean    // pure, Node-testable

<!-- Key assignment fixed by 02-UI-SPEC.md Architecture Call 1 -->
Backquote -> profiler HUD (Phase 1, unchanged)
KeyG      -> vehicle tuning panel
KeyT      -> telemetry results panel
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: onDebugKey with an activeElement focus guard (closes the standing NOTE)</name>
  <files>src/debug/debug-gate.ts, tests/debug-gate.test.ts</files>
  <read_first>
    - src/debug/debug-gate.ts (read in full — lines 36-56 are the function being generalised; lines 44-46 carry the standing NOTE this task closes; lines 28-34 are the `typeof location !== "undefined"` Node-import guard)
    - tests/debug-gate.test.ts (read in full — lines 4-11 explain why the module must import cleanly in Node; the hostile-input case style is the template for the focus-guard cases)
    - src/main.ts (line 63 — the existing `onDebugToggle(() => hud.toggle())` call site that must keep compiling with no change)
    - .planning/phases/02-vehicle-feel-core/02-UI-SPEC.md ("Architecture Call 1" — the key table, the rationale for three separate keys, and the exact focus-guard condition)
    - .planning/phases/02-vehicle-feel-core/02-PATTERNS.md (section `src/debug/debug-gate.ts (MODIFY)` — extend, do not replace)
  </read_first>
  <action>
Extend, do not rewrite, `src/debug/debug-gate.ts`.

Add `export function isTextEntryFocused(el: Element | null): boolean` returning true when
`el` is an `HTMLInputElement`, an `HTMLTextAreaElement`, or an `HTMLElement` with
`isContentEditable` true. Keep it PURE and parameterised on the element rather than reading
`document.activeElement` internally, following `parseDebugFlag`'s precedent — that is what
lets `tests/debug-gate.test.ts` hammer it in Vitest's `node` environment with hand-built
fakes. Guard against a missing DOM: when `HTMLInputElement` is not defined (Node), fall back
to a duck-typed check on `tagName` (`INPUT`, `TEXTAREA`) and an `isContentEditable`
property, so the function is meaningful in both environments. Document that fallback.

Add `export function onDebugKey(code: string, fn: () => void): void` carrying forward every
guard `onDebugToggle` already has — `if (!DEBUG_ENABLED) return;`, bare `addEventListener`
(not `window.addEventListener`), `!e.repeat`, `!e.metaKey && !e.ctrlKey`,
`e.preventDefault()` — and adding one new guard before invoking `fn`:
bail when `isTextEntryFocused(document.activeElement)` is true.

Rewrite `onDebugToggle` as a thin alias: `onDebugKey("Backquote", fn)`. This keeps
`src/main.ts:63` and the existing `tests/debug-gate.test.ts` cases compiling and passing
with zero edits at those sites.

Update the standing NOTE at lines 44-46 — do not leave a stale TODO in the file. The new
wording must state that the guard IS now handled, that the later phase it anticipated is
Phase 2's lil-gui tuning panel (every lil-gui numeric control is a real `<input>`), and that
`KeyG` and `KeyT` are the panel toggles the guard protects.

Add a doc comment recording 02-UI-SPEC.md Architecture Call 1's decision and its reason:
three separate keys rather than one shared toggle, because the three overlays have
genuinely different duty cycles — the profiler HUD is glanceable and often left on, the
tuning panel is a large opaque rectangle you want gone the moment you start a feel run, and
the telemetry panel is transient. Binding them together forces the developer to accept all
three or none precisely during the long tuning session this phase exists to enable.

Append to `tests/debug-gate.test.ts` (do not modify or delete any existing case):
  - `isTextEntryFocused(null)` is false.
  - a fake `{ tagName: "INPUT" }` is true; `{ tagName: "TEXTAREA" }` is true;
    `{ tagName: "DIV", isContentEditable: true }` is true;
    `{ tagName: "DIV", isContentEditable: false }` is false; `{ tagName: "CANVAS" }` is false.
  - each case asserts `typeof result === "boolean"`, matching the file's existing convention
    of asserting both the value and the type.
  - a case asserting `onDebugKey` and `onDebugToggle` are both exported functions and that
    calling them with `DEBUG_ENABLED === false` (the Node default) registers nothing and
    does not throw — the same "registers zero listeners in a normal build" property the
    existing suite already relies on.
  </action>
  <verify>
    <automated>npx vitest run tests/debug-gate.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/debug-gate.test.ts` exits 0 with every pre-existing case still present and passing, plus at least 7 new cases.
    - `src/debug/debug-gate.ts` exports `onDebugKey`, `onDebugToggle` and `isTextEntryFocused`.
    - `grep -n "onDebugToggle" src/main.ts` still shows the unchanged Phase 1 call site and `src/main.ts` is NOT modified by this plan (`git diff --name-only` does not list it).
    - `grep -c "will need extending" src/debug/debug-gate.ts` returns 0 — the stale NOTE is gone.
    - `src/debug/debug-gate.ts` contains `activeElement` on a non-comment line.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Three debug overlays can own three keys, and no debug hotkey can fire while the developer is typing a tuning value.</done>
</task>

<task type="auto">
  <name>Task 2: Layering rules for src/input/** and src/hud/**</name>
  <files>tests/layering.test.ts</files>
  <read_first>
    - tests/layering.test.ts (read in full — the `import.meta.glob` INLINE `?raw` query at lines 18-22, the `isCommentLine`/`findHits`/`format` helpers at 44-74, the file-count floor at 76-80, and the `src/render/` three-part block at 117-130 which is the template)
    - src/input/keyboard.ts, src/input/gamepad.ts, src/input/live-input.ts (plan 02-02's output — the rules must pass against what actually shipped)
    - src/hud/speedometer.ts (plan 02-03's output)
    - .planning/phases/02-vehicle-feel-core/02-UI-SPEC.md ("Call 2" — the exact four-row `src/hud/**` forbidden table)
    - .planning/phases/02-vehicle-feel-core/02-PATTERNS.md (section `tests/layering.test.ts (MODIFY)` — copy the whole three-part shape including the non-optional scanned-at-least-one guard)
  </read_first>
  <action>
Add two new `describe` blocks to `tests/layering.test.ts`, each copying the exact three-part
shape of the existing `src/render/` block: (1) a `FILES.filter` by path prefix, (2) a
`scanned at least one file` guard, (3) a per-file `it()` inside a `for` loop. The guard is
NOT optional — `src/hud/` and `src/input/` each contain few files and the entire point is to
go red if the glob misses them.

Block A: `layering — src/hud never imports an engine and never writes simulation state (T-02-14)`.
Prefix `src/hud/`. Forbidden pattern combines two halves: the engine-import half from the
`src/core/` rule (`from\s+["'](three|@dimforge\/rapier3d)["']`) and the write-ban half from
the `src/render/` rule (`\b(world\.step|applyImpulse|setTranslation|setRotation|setNextKinematic)\b`).
Deliberately DROP `\bdocument\.` — the HUD legitimately calls `document.createElementNS`.
Do NOT duplicate a `performance.` check; lines 140-149 already cover it repo-wide. Put a
comment stating both of those omissions so a later reader does not "tighten" the rule and
break the gauge.

Block B: `layering — src/input never imports three and never writes simulation state (T-02-15)`.
Prefix `src/input/`. Forbidden pattern: `from\s+["']three["']` plus the same write-ban set.
Comment that the repo-wide `performance.now(`/`Date.now(` rule at lines 140-149 is what
mechanically prevents a wall-clock steering ramp in this tier — a rule that only works
because `src/input/` is not on that exception list.

Raise the file-count floor at line 78 from 10 to 20, with a comment noting Phase 2 adds
roughly ten source files, so the floor stays a meaningful guard against a broken glob rather
than a value that was true once in Phase 1.

Do not touch any existing describe block.
  </action>
  <verify>
    <automated>npx vitest run tests/layering.test.ts</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/layering.test.ts` exits 0.
    - The suite reports at least one `src/hud/` case and at least one `src/input/` case by name (visible in `--reporter=verbose` output).
    - Deliberately breaking either rule makes the suite red: verify once by temporarily adding `import * as THREE from "three";` to `src/hud/speedometer.ts`, confirming `npx vitest run tests/layering.test.ts` FAILS, then reverting. Record the observed failure line in the SUMMARY. This is the anti-trivially-green check `tests/determinism.test.ts:129-134` establishes as house convention.
    - `tests/layering.test.ts` line asserting the file-count floor reads `toBeGreaterThanOrEqual(20)`.
    - All pre-existing describe blocks are unchanged (`git diff tests/layering.test.ts` shows additions plus the single floor edit only).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>The two new source tiers are policed automatically, and the policing has been proven to fail when violated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard event stream -> debug overlay state | Keypresses reach debug toggles; a lil-gui numeric `<input>` competes for the same keys |
| `?debug` query parameter -> debug surface existence | An attacker-influenced URL decides whether any debug surface exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | `onDebugKey` firing during text entry | mitigate | `isTextEntryFocused(document.activeElement)` bail before invoking the callback, covering `HTMLInputElement`, `HTMLTextAreaElement` and `isContentEditable`. Closes the standing NOTE at `debug-gate.ts:44-46`. Unit-tested with duck-typed fakes in `tests/debug-gate.test.ts`. |
| T-02-04 | Tampering | `?debug` parameter handling | accept | Unchanged from Phase 1: presence check only via `URLSearchParams.has("debug")`; the value is never read and never rendered (ASVS V5). No new surface added here. |
| T-02-03 | Information disclosure | debug listeners in a production build | mitigate | `onDebugKey` keeps `if (!DEBUG_ENABLED) return;` as its first statement, so a normal build registers zero listeners for all three keys. Asserted by a `tests/debug-gate.test.ts` case. |
| T-02-14 / T-02-15 | Tampering | layering drift in `src/hud/` and `src/input/` | mitigate | Automated source-scan rules with a scanned-at-least-one guard, plus a one-off proof that a deliberate violation turns the suite red. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No install task in this plan; net dependency change zero. |
</threat_model>

<verification>
1. `npx vitest run tests/debug-gate.test.ts` — green, all pre-existing cases intact.
2. `npx vitest run tests/layering.test.ts` — green, with `src/hud/` and `src/input/` blocks reporting scanned files.
3. One-off negative proof: a temporary `three` import in `src/hud/speedometer.ts` makes the layering suite red; reverted afterwards and recorded in the SUMMARY.
4. `npm run check` — green.
5. Manual (deferred to plan 02-10): actually pressing `KeyG` / `KeyT` in the browser is
   covered by the browser checkpoint once the panels exist in plan 02-09.
</verification>

<success_criteria>
- `onDebugKey(code, fn)` exists, `onDebugToggle` is a thin alias, and `src/main.ts` needed no edit.
- The focus guard bails on `<input>`, `<textarea>` and contenteditable, tested in Node.
- The stale NOTE in `debug-gate.ts` is replaced with a statement that the guard is handled.
- `tests/layering.test.ts` polices `src/input/**` and `src/hud/**` with a scanned-at-least-one guard, and the file-count floor is raised to 20.
</success_criteria>

<output>
Create `.planning/phases/02-vehicle-feel-core/02-05-SUMMARY.md` when done.
</output>
