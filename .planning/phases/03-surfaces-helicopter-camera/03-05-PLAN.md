---
phase: 03-surfaces-helicopter-camera
plan: 05
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/physics/surface-scene.ts
  - src/render/surface-view.ts
  - src/render/vehicle-view.ts
  - tests/surface-scene.test.ts
autonomous: true
requirements: [SURF-01, CAM-04]

must_haves:
  truths:
    - "A single straight drive from the spawn point crosses all six surfaces in sequence, proven headlessly"
    - "Placeholder buildings exist in two deliberately different densities — one sparse cluster and one dense urban-canyon corridor — so both halves of CAM-04's steepen mitigation can be exercised in one drive"
    - "The scene has visual counterparts for every physics zone and building, built without cross-importing geometry constants between the physics and render tiers"
    - "Phase 2's ramp scene survives untouched as a regression fixture"
    - "D-01: the six surface zones sit adjacent on one flat plane, patchwork-style, so one straight drive crosses all of them"
    - "D-02: this is a fresh layout, not a reuse of Phase 2's ramp scene"
    - "D-03: this scene is a dev/test fixture only, matching the debug-scene.ts/vehicle-scene.ts precedent, explicitly superseded by Phase 4's real map pipeline"
    - "D-06: placeholder box/cuboid buildings make SC6's occlusion mitigations genuinely testable this phase, per the roadmap's 'not a paper decision' requirement"
    - "D-07: building layout is a few clustered zones of varying density (one sparse, one dense/urban-canyon), not a random scatter"
  artifacts:
    - path: "src/physics/surface-scene.ts"
      provides: "Six contiguous surface zones, two building clusters, one Vehicle, and the SurfaceContext the tick consumes"
      exports: ["SurfaceScene", "createSurfaceScene", "SURFACE_SCENE_SPAWN", "SURFACE_ZONE_ORDER"]
      min_lines: 150
    - path: "src/render/surface-view.ts"
      provides: "Coloured zone planes and building meshes as one disposable THREE.Group, plus the buildingMeshes array the occlusion probe consumes"
      exports: ["SurfaceWorldView", "createSurfaceWorld"]
    - path: "tests/surface-scene.test.ts"
      provides: "Headless proof that a straight drive crosses all six surfaces, and that the zone/building layout matches its contract"
  key_links:
    - from: "src/physics/surface-scene.ts"
      to: "src/physics/surface.ts"
      via: "surfaceMap.register(collider.handle, surface) at zone construction"
      pattern: "\\.register\\("
    - from: "src/physics/surface-scene.ts"
      to: "src/physics/vehicle.ts"
      via: "vehicle.tick(frame, tuning, surfaceContext) from applyInput"
      pattern: "vehicle\\.tick\\("
    - from: "src/render/surface-view.ts"
      to: "src/physics/surface-scene.ts"
      via: "duplicated literal dimensions with a MUST MATCH comment — never an import"
      pattern: "MUST MATCH"
---

<objective>
Build the placeholder world SURF-01 and CAM-04 are proven on: six surface zones laid
end to end so one straight drive crosses all of them, and two deliberately-different
building clusters so occlusion can be judged in a dense canyon and in the open, in the
same session.

Purpose: after this plan, `tests/surface-scene.test.ts` can drive a car headlessly
from tarmac through to mud and assert every surface was encountered — SC1's premise
made mechanical, before anything is on screen. The building clusters exist now, not
in Phase 4, because CONTEXT.md D-06 is explicit that SC6's playtest is not deferred.

Output: `src/physics/surface-scene.ts`, `src/render/surface-view.ts`, a small
options extension to `src/render/vehicle-view.ts`, and `tests/surface-scene.test.ts`.
</objective>

## Phase Goal

**Note for the executor:** `.planning/ROADMAP.md`'s Phase 3 `**Goal:**` line is not in
the three-slot user-story form MVP mode expects. Reproduced verbatim rather than invented:

> The world under the tires changes how the car drives, and the game is viewed
> through its signature camera without losing the sense of speed

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-surfaces-helicopter-camera/03-CONTEXT.md
@.planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md
@.planning/phases/03-surfaces-helicopter-camera/03-PATTERNS.md
@.planning/phases/03-surfaces-helicopter-camera/03-VALIDATION.md
@.planning/phases/03-surfaces-helicopter-camera/03-03-SUMMARY.md
</context>

<interfaces>
<!-- Contracts extracted from the existing codebase and prior plans. -->

`src/physics/vehicle-scene.ts` — the exact structural template (CONTEXT.md names it as the pattern to follow):
- `const GROUND_HALF_EXTENTS = { x: 200, y: 1, z: 400 }` with a doc comment explaining Y >= 1 m is a
  tunnelling guard: 120 mph is 0.894 m of travel per fixed tick (02-RESEARCH.md Pitfall 6)
- `function buildGround(world: RAPIER.World): void` — a `RigidBodyDesc.fixed()` translated to
  `(0, -halfY, 0)` so the top surface sits at y = 0, plus `ColliderDesc.cuboid(...).setFriction(1.0)`
- `export const SPAWN = { x: 0, y: 0.6, z: RAMP_APPROACH_Z + 30 }` — 0.6 m above the ground,
  the suspension settles the rest
- `export interface VehicleScene { readonly bodies: readonly RAPIER.RigidBody[]; readonly vehicle: Vehicle; preTick(tick: number): void; applyInput(frame: InputFrame): void; setTuning(t: VehicleTuning): void; dispose(): void }`
  `bodies` is chassis-only; the ground and ramp are fixed bodies with NO mesh counterpart
  (`TransformCache` and `vehicle-view.ts` both key on this dense order — threat T-01-16)
- `createVehicleScene(world, tuning)` builds ground, builds ramp, `createVehicle(world, tuning, SPAWN)`,
  `applyInput` calls `vehicle.tick(frame, currentTuning)`

`src/physics/vehicle-scene.ts:219-222` — `preTick` is currently a documented no-op whose comment says
"Phase 3's per-wheel surface lookup (`wheelGroundObject`) is the reason this hook exists at all".
That turned out to belong inside `vehicle.tick` (plan 03-03), so `SurfaceScene.preTick` stays a no-op —
carry the corrected note forward rather than repeating the outdated prediction.

Landed by plan 03-03 (`src/physics/surface.ts`):
- `createSurfaceMap(defaultSurface?: SurfaceType): SurfaceMap`, `SurfaceMap.register(handle, surface)`
- `interface SurfaceContext { readonly map: SurfaceMap; readonly profiles: SurfaceProfiles }`
- `Vehicle.tick(frame, tuning, surfaces?)` and `Vehicle.wheelSurfaces: readonly SurfaceType[]`

Landed by plan 03-01: `src/core/surface-types.ts` (`SURFACE_TYPES`, `SurfaceType`),
`src/core/surface-tuning.ts` (`defaultSurfaceProfiles()`).

`src/render/vehicle-view.ts` — the render-side template:
- `export function createVehicleView(radius, halfTrack, halfWheelbase, halfExtents): VehicleView`
- `export interface VehicleView { readonly scene: THREE.Scene; readonly meshes: readonly THREE.Object3D[]; ... }`
- Colour constants at lines 56-62: `COLOUR_BACKGROUND 0x141820`, `COLOUR_GROUND 0x2b2b33`,
  `COLOUR_RAMP 0x4a4a55`, `COLOUR_CHASSIS 0xb5321f`, `COLOUR_WHEEL 0x151515`,
  `COLOUR_GRID 0x50505c`, `COLOUR_GRID_CENTER 0x6a6a78`
- It builds the ground mesh (line ~207), a `THREE.GridHelper` (line 216), the ramp mesh (line ~222),
  chassis and wheels, and disposes every geometry/material it creates (lines 338-349)
- Its "MUST MATCH `src/physics/vehicle-scene.ts`" convention: geometry literals are duplicated,
  never cross-imported between tiers
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The six-zone surface scene with two building clusters</name>
  <files>src/physics/surface-scene.ts, tests/surface-scene.test.ts</files>
  <read_first>
    - src/physics/vehicle-scene.ts (the whole file — the structural template, the ground-builder shape, the `VehicleScene` contract and its `bodies` exclusion rule)
    - src/physics/surface.ts (plan 03-03's `createSurfaceMap` / `SurfaceContext`)
    - src/physics/vehicle.ts (the `tick(frame, tuning, surfaces?)` signature and `wheelSurfaces` array plan 03-03 added)
    - src/core/surface-types.ts and src/core/surface-tuning.ts (plan 03-01)
    - tests/vehicle-scene.test.ts (the existing scene-test style: build a real world, drive scripted input, assert on sampled state)
    - .planning/phases/03-surfaces-helicopter-camera/03-CONTEXT.md (D-01, D-02, D-03, D-06, D-07 — the layout requirements this task implements literally)
  </read_first>
  <behavior>
    Write `tests/surface-scene.test.ts` FIRST; it must fail before the module exists.
    - Registration: every one of the six `SURFACE_TYPES` values is registered in the scene's
      `surfaceMap` exactly once; looking up each zone collider's handle returns its own surface.
    - Contiguity: the six zones tile the Z axis with no gap and no overlap — for each adjacent
      pair, the far edge of one equals the near edge of the next to within 1e-9.
    - Order: `SURFACE_ZONE_ORDER` starts with `"tarmac"` at the highest Z (the spawn end) so the
      car begins on the Phase 2 baseline surface and drives into progressively looser ones.
    - Spawn is on tarmac: `surfaceMap.lookup` of the collider directly beneath
      `SURFACE_SCENE_SPAWN` resolves to `"tarmac"`, and the spawn sits at least 40 m from the
      first zone boundary so there is room to reach speed before the first transition.
    - THE SC1 PROOF, headless: build the scene, then drive full throttle with zero steering for
      up to 1800 ticks, calling `scene.applyInput` then `world.step()` each tick and recording
      `scene.vehicle.wheelSurfaces[0]` after each. Assert that the recorded sequence, with
      consecutive duplicates collapsed, equals `SURFACE_ZONE_ORDER` — all six surfaces, in order,
      from one straight drive. This is D-01's stated purpose made mechanical.
    - Buildings: the scene exposes its building placements; there are at least 12 of them, they
      form exactly two clusters, and the two clusters' mean positions are at least 40 m apart.
    - Canyon geometry: the dense cluster's two rows leave a drivable gap of between 8 m and 14 m —
      wide enough for a 1.9 m-wide car to thread, narrow enough to be a genuine urban canyon.
    - Building height: every building's top is above 20 m, i.e. taller than the helicopter rig's
      maximum altitude default of 26 m minus its follow distance — the geometry can actually
      occlude the camera, otherwise the whole SC6 playtest would be vacuous.
    - `bodies` contains exactly one entry, the chassis — no zone or building has a mesh counterpart
      in the interpolated-transform array (threat T-01-16).
    - `dispose()` does not throw and can be called once safely.
  </behavior>
  <action>
    Create `src/physics/surface-scene.ts` with `vehicle-scene.ts`'s doc-comment header adapted:
    state that this SUPERSEDES `src/physics/vehicle-scene.ts` at the composition root ONLY, that
    `vehicle-scene.ts` and `debug-scene.ts` both stay in place as regression fixtures and must not
    be deleted or edited (`tests/vehicle-scene.test.ts`, `tests/determinism.test.ts`,
    `tests/transform-cache.test.ts` and `tests/loop.test.ts` import them), and that per CONTEXT.md
    D-03 this scene is itself a dev/test fixture superseded by Phase 4's real map pipeline — not
    shipped content. Same layering rules: `@dimforge/rapier3d` and `src/core/` only, no `three`,
    no DOM, no wall clock.

    Geometry — D-01's patchwork, laid out as bands ACROSS the driving direction so one straight
    run crosses every band. The car's forward is local `-Z`, so the bands stack along Z:
    - `ZONE_HALF_X = 60`, `ZONE_HALF_Y = 1`, `ZONE_HALF_Z = 40` (each band 120 m wide, 80 m long,
      >= 1 m thick — carry `vehicle-scene.ts`'s tunnelling-guard comment across verbatim)
    - `export const SURFACE_ZONE_ORDER` = `["tarmac", "gravel", "dirt_road", "grass", "sand", "mud"]`,
      band `i` centred at `z = 200 - i * 80`, i.e. +200, +120, +40, -40, -120, -200. Bands are
      therefore contiguous from z = +240 down to z = -240.
    - `export const SURFACE_SCENE_SPAWN = { x: 0, y: 0.6, z: 205 }` — on tarmac, 45 m of clear
      run-up before the gravel boundary at z = 160, facing -Z.
    - D-02: this scene contains NO ramp. Do not import `RAMP_APPROACH_Z` or copy `buildRamp`.
      Say so in a comment so the omission reads as deliberate.

    `function buildSurfaceZone(world, surfaceMap, surface, centerZ)` mirroring `buildGround`:
    a `RigidBodyDesc.fixed()` translated to `(0, -ZONE_HALF_Y, centerZ)` so every band's top
    surface is exactly y = 0, a `ColliderDesc.cuboid(ZONE_HALF_X, ZONE_HALF_Y, ZONE_HALF_Z)` with
    `.setFriction(1.0)` — the SAME value on every band. Comment that this is deliberate and is
    SURF-01's literal requirement: grip differences come from per-wheel friction, never from the
    ground collider's coefficient, so a reader cannot mistake a uniform value for an oversight.
    Then `surfaceMap.register(collider.handle, surface)`.

    Buildings — D-06/D-07, two hand-placed clusters, one function per cluster so the deliberate
    layout is legible rather than hidden in a loop:
    - `buildSparseCluster(world)`: 4 boxes, half-extents `{ x: 6, y: 10, z: 6 }` (12 x 20 x 12 m),
      bodies translated to y = 10 so they sit on the ground, at
      `(34, 170)`, `(48, 110)`, `(30, 55)`, `(50, 0)` in (x, z). Widely spaced, on the +X side,
      clear of the centreline run — this is the "relaxes in open areas" half of CAM-04.
    - `buildDenseCluster(world)`: 10 boxes, half-extents `{ x: 5, y: 14, z: 9 }` (10 x 28 x 18 m),
      bodies translated to y = 14, in two rows at x = -41 and x = -21, each row at
      z = 30, 10, -10, -30, -50. The rows' inner faces sit at x = -36 and x = -26, leaving a 10 m
      drivable corridor the car must thread — the urban canyon. At 28 m tall these are taller than
      the helicopter rig's default high-speed altitude, which is what makes them occlude at all.
    - Each building is a `RigidBodyDesc.fixed()` plus a `ColliderDesc.cuboid(...)` with
      `.setFriction(0.8)` so a collision reads as a wall, not ice. Buildings are deliberately NOT
      registered in the `SurfaceMap` — a wheel that somehow ends up on a roof resolves to the map's
      default surface; comment that this is accepted for a placeholder fixture.
    - Export the placements as a readonly data table (e.g.
      `export const SPARSE_BUILDINGS` / `export const DENSE_BUILDINGS`, each an array of
      `{ x: number; z: number; halfExtents: { x: number; y: number; z: number } }`) so
      `tests/surface-scene.test.ts` can assert the layout contract and `src/render/surface-view.ts`
      can be checked against the same numbers by a human reading both files.

    `export interface SurfaceScene` copying `VehicleScene`'s shape verbatim and adding
    `readonly surfaces: SurfaceContext`. `bodies` stays chassis-only.

    `export function createSurfaceScene(world: RAPIER.World, tuning: VehicleTuning, profiles: SurfaceProfiles): SurfaceScene`:
    creates the surface map, builds the six bands in `SURFACE_ZONE_ORDER`, builds both clusters,
    creates one `createVehicle(world, tuning, SURFACE_SCENE_SPAWN)`, and returns the scene.
    `applyInput` calls `vehicle.tick(frame, currentTuning, surfaces)` — passing the surface
    context is the entire point of this scene. `setTuning` keeps `vehicle-scene.ts`'s behaviour
    and additionally accepts a profiles update so plan 03-06's tuning panel can sweep grip live:
    give the scene a `setSurfaceProfiles(p: SurfaceProfiles): void` that swaps the object the
    context holds. `preTick` stays a documented no-op.
  </action>
  <verify>
    <automated>npx vitest run tests/surface-scene.test.ts tests/vehicle-scene.test.ts tests/layering.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/surface-scene.test.ts` passes, including the straight-drive test whose collapsed surface sequence equals `SURFACE_ZONE_ORDER`
    - `npx vitest run tests/vehicle-scene.test.ts tests/determinism.test.ts` passes unchanged — the Phase 2 fixture is untouched
    - `grep -c "RAMP\|buildRamp\|convexHull" src/physics/surface-scene.ts` returns 0 — D-02's no-ramp requirement
    - `grep -c "setFriction(1.0)" src/physics/surface-scene.ts` returns 6 — every band shares one collider friction value
    - `grep -c "\.register(" src/physics/surface-scene.ts` returns at least 1, inside the zone builder only
    - `grep -c "from \"three\"" src/physics/surface-scene.ts` returns 0
    - `npx vitest run tests/layering.test.ts` passes
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>A headless test drives the car in a straight line from the spawn and observes tarmac, gravel, dirt_road, grass, sand and mud in that order, and two building clusters of genuinely different density exist to drive through.</done>
</task>

<task type="auto">
  <name>Task 2: Visual zone planes and building meshes matching the physics scene</name>
  <files>src/render/surface-view.ts, src/render/vehicle-view.ts</files>
  <read_first>
    - src/render/vehicle-view.ts (the whole file — the MUST MATCH comment convention at lines 39-54, the colour constant table at lines 56-62, the shared-geometry/material discipline at lines 246-254, and the `dispose()` structure at lines 338-349)
    - src/physics/surface-scene.ts (Task 1's output — every literal dimension this file must mirror)
    - .planning/phases/03-surfaces-helicopter-camera/03-PATTERNS.md (section "src/render/surface-view.ts", including its per-surface colour constants)
    - .planning/phases/03-surfaces-helicopter-camera/03-CONTEXT.md (D-08's per-surface colour language: grey smoke/tarmac, tan dust/gravel-sand, brown spray/mud-dirt, green-tinted/grass)
    - docs/frame-budget.md (the draw-call and triangle targets the shared-geometry discipline protects)
  </read_first>
  <action>
    Create `src/render/surface-view.ts`.

    Header doc comment, copying `vehicle-view.ts`'s convention at lines 39-54: this file has NO
    import from `src/physics/surface-scene.ts`; every zone half-extent, band centre, building
    half-extent and building position is duplicated here as a literal with a
    "MUST MATCH `src/physics/surface-scene.ts`" comment naming the specific constant it mirrors.
    Cross-importing geometry constants between the physics and render tiers is the convention this
    project deliberately does not use.

    Per-surface colours (03-PATTERNS.md's table, use these exact values so the zone colour reads
    as the same material family the particle FX in plan 03-09 will use):
    `COLOUR_TARMAC 0x2b2b33` (deliberately identical to `vehicle-view.ts`'s existing
    `COLOUR_GROUND` — tarmac is the unchanged Phase 2 baseline), `COLOUR_GRAVEL 0xa89a78`,
    `COLOUR_DIRT_ROAD 0x8a6a4a`, `COLOUR_GRASS 0x3f6b3a`, `COLOUR_SAND 0xd9c48f`,
    `COLOUR_MUD 0x4a3626`. Plus `COLOUR_BUILDING 0x6b6f7a`.

    Export:
    - `export interface SurfaceWorldView { readonly group: THREE.Group; readonly buildingMeshes: readonly THREE.Mesh[]; dispose(): void }`
    - `export function createSurfaceWorld(): SurfaceWorldView`

    `createSurfaceWorld` builds:
    - Six zone meshes. Use ONE shared `THREE.BoxGeometry` sized to a band and one
      `MeshStandardMaterial` PER SURFACE (six materials, one geometry) — the shared-geometry
      discipline `vehicle-view.ts` lines 246-254 states for the wheels, for the same
      `docs/frame-budget.md` reason. Position each band to match its physics counterpart with the
      top face at y = 0. Boxes rather than planes so the band edges are visible from the
      helicopter camera's high angle, which is what makes a surface transition legible on screen.
    - Building meshes from the duplicated placement tables. ONE shared `THREE.BoxGeometry` per
      cluster (the two clusters have different half-extents) and ONE shared
      `MeshStandardMaterial` across all buildings — plan 03-08's fade mitigation will need
      per-building opacity, so build each mesh with `material.clone()` and record in a comment
      that the clone is deliberate and exists for that reason, so a future "optimisation" back to
      a single shared material does not silently break the fade. Set `transparent: true` and
      `depthWrite: false` is NOT set yet — plan 03-08 owns the fade's material flags and will
      state its own reasoning.
    - `buildingMeshes` is the flat array of every building mesh in both clusters, in
      sparse-then-dense order — this is exactly the array plan 03-08's `THREE.Raycaster`
      probe passes to `intersectObjects`.
    - `dispose()` disposes every geometry and every material this function created, one line each,
      copying `vehicle-view.ts` lines 338-349's structure verbatim.

    Then make a SMALL extension to `src/render/vehicle-view.ts`: add an optional final parameter
    `options?: { readonly includePhase2Ground?: boolean }` to `createVehicleView`, defaulting to
    `true`. When `false`, skip creating the ground mesh and the ramp mesh (and skip disposing
    what was not created), but STILL create the `THREE.GridHelper` — the grid was added after the
    plan 02-10 feel session specifically because a featureless ground made speed and slip
    impossible to judge, and SC4's speed-legibility gate needs it more than that session did.
    Every existing call site keeps working with no change. Do not restructure anything else in
    that file.

    Layering: `src/render/surface-view.ts` must contain none of `world.step`, `applyImpulse`,
    `setTranslation`, `setRotation`, `setNextKinematic` on a non-comment line, and must not import
    `@dimforge/rapier3d`.
  </action>
  <verify>
    <automated>npx vitest run tests/layering.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and every existing `createVehicleView` call site compiles unchanged
    - `grep -c "MUST MATCH" src/render/surface-view.ts` returns at least 1
    - `grep -v '^\s*[*/]' src/render/surface-view.ts | grep -c "from \"../physics/"` returns 0 — no cross-tier geometry import
    - `grep -c "0x2b2b33" src/render/surface-view.ts` returns 1 — tarmac reuses the existing ground colour
    - `grep -c "dispose()" src/render/surface-view.ts` returns at least 7 — six zone materials plus the shared geometries and building resources are each released
    - `grep -c "GridHelper" src/render/vehicle-view.ts` returns at least 1 and the grid is created on both branches of the new option
    - `npx vitest run tests/layering.test.ts` passes with `src/render/surface-view.ts` in its glob
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Six differently-coloured surface bands and two building clusters can be added to the render scene as one disposable group, and the composition root can ask `createVehicleView` to omit Phase 2's ground and ramp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene construction → `SurfaceMap` | Collider handles are registered exactly once at build time; a missed registration silently degrades a whole band to the default surface |
| `src/physics/surface-scene.ts` ↔ `src/render/surface-view.ts` | Two independently-maintained copies of the same geometry; drift produces a car that grips a surface it is not visually standing on |
| Building colliders → the chassis | Static geometry the car can hit at 120 mph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Spoofing (data integrity) | A zone whose visual colour and registered `SurfaceType` disagree — the car slides on what looks like tarmac | mitigate | `SURFACE_ZONE_ORDER` is exported from the physics module and drives both the registration loop and the test's expected sequence; the render file carries a `MUST MATCH` comment naming the specific physics constant each literal mirrors, the same control `vehicle-view.ts`/`vehicle-scene.ts` already use |
| T-03-15 | Denial of Service | A 120 mph chassis tunnelling through a band collider thinner than its 0.894 m per-tick travel, dropping the car out of the world | mitigate | `ZONE_HALF_Y = 1`, carrying `vehicle-scene.ts`'s existing Pitfall-6 tunnelling-guard comment; the chassis already has `setCcdEnabled(true)` |
| T-03-16 | Tampering | Registering a building collider as a ground surface, so a roof silently becomes drivable tarmac | accept | Buildings are deliberately not registered; a wheel on a roof resolves to the map's default surface. Documented in place. Low impact for a fixture explicitly superseded by Phase 4 (D-03) |
| T-03-17 | Tampering (regression) | Deleting or editing `vehicle-scene.ts`/`debug-scene.ts` as a side effect of adding their successor, breaking the VEH-03 determinism proof CLAUDE.md constraint 3 protects | mitigate | Header comment naming the four importing test files, plus an acceptance gate that `tests/vehicle-scene.test.ts` and `tests/determinism.test.ts` pass unchanged |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; all geometry is procedural `THREE.BoxGeometry`, no glTF asset pipeline is triggered |
</threat_model>

<verification>
1. `npx vitest run tests/surface-scene.test.ts` — the straight-drive test observes all six surfaces in order.
2. `npx vitest run tests/vehicle-scene.test.ts tests/determinism.test.ts tests/transform-cache.test.ts tests/loop.test.ts` passes unchanged — every Phase 1/2 fixture survives.
3. `npx vitest run tests/layering.test.ts` passes.
4. `npm run check` exits 0.
</verification>

<success_criteria>
- D-01 is literally satisfied: one straight drive crosses all six surfaces, and a headless test proves it.
- D-02 is satisfied: no ramp, fresh layout, asserted by grep.
- D-06 and D-07 are satisfied: buildings exist this phase, in one sparse cluster and one dense canyon, tall enough to actually occlude the helicopter camera.
- The physics and render tiers stay decoupled, with the project's existing MUST MATCH convention as the only link.
</success_criteria>

<output>
Create `.planning/phases/03-surfaces-helicopter-camera/03-05-SUMMARY.md` when done.
</output>
