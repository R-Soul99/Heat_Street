---
phase: 03-surfaces-helicopter-camera
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/core/camera-tuning.ts
  - src/render/camera/helicopter-camera.ts
  - src/render/camera/camera-skin.ts
  - tests/camera-tuning.test.ts
  - tests/camera-skin.test.ts
  - index.html
autonomous: true
requirements: [CAM-01, CAM-02, CAM-03]

must_haves:
  truths:
    - "A camera rig follows a generic target's velocity heading with a tunable, frame-rate-independent lag — nothing in its signature names 'the player car'"
    - "Camera altitude, follow distance and FOV all shift with speed, damped so a brake-to-accel produces no pop"
    - "A low chase-cam rig satisfying the identical CameraRig interface is selectable, so D-12's fallback is a real shipped option rather than a dev escape hatch"
    - "Switching camera skin changes presentation only — the skin module cannot touch distance, damping or targeting because it holds no reference to any of them"
    - "D-11: stable through a full 40-degree drift means no shake or jitter, with a smooth lag as the camera catches up to the car's changing velocity heading — reads like a chopper pilot tracking, not a rigid instant lock"
    - "D-13: the camera's target-tracking mechanism is generic/swappable ('what am I following'), not hardcoded to the one player car, mirroring Phase 2's D-09 vehicle-controller precedent"
    - "D-15: visual differentiation between the police/news and sports-broadcast skins is research-informed (colour grade, overlay chrome, spotlight/vignette conventions), refined via playtest"
  artifacts:
    - path: "src/core/camera-tuning.ts"
      provides: "CameraTuning defaults/ranges/clamp/parse/serialize + storage key, delegating to tuning-utils"
      exports: ["CameraTuning", "defaultCameraTuning", "CAMERA_TUNING_RANGES", "clampCameraTuning", "parseSavedCameraTuning", "serializeCameraTuning", "CAMERA_TUNING_STORAGE_KEY"]
    - path: "src/render/camera/helicopter-camera.ts"
      provides: "CameraTarget, CameraRig, createHelicopterCameraRig, createChaseCameraRig"
      exports: ["CameraTarget", "CameraRig", "createHelicopterCameraRig", "createChaseCameraRig"]
      min_lines: 120
    - path: "src/render/camera/camera-skin.ts"
      provides: "CameraSkin union, applySkinClasses, createCameraSkin DOM switcher"
      exports: ["CameraSkin", "CAMERA_SKINS", "applySkinClasses", "createCameraSkin"]
  key_links:
    - from: "src/render/camera/helicopter-camera.ts"
      to: "src/render/camera/camera-math.ts"
      via: "blendedHeadingRad / framingForSpeed / dampFactor"
      pattern: "blendedHeadingRad|framingForSpeed|dampFactor"
    - from: "src/core/camera-tuning.ts"
      to: "src/core/tuning-utils.ts"
      via: "clampNode / copyLeaves — the one shared ASVS V5 pipeline"
      pattern: "from \"./tuning-utils\""
    - from: "src/render/camera/camera-skin.ts"
      to: "index.html"
      via: "CSS class names .skin-police / .skin-sports applied to the canvas"
      pattern: "skin-police|skin-sports"
---

<objective>
Build the actual helicopter camera rig, its tuning contract, its D-12 fallback, and
its two presentation skins — everything CAM-01, CAM-02 and CAM-03 need, short of
being wired at the composition root.

Purpose: this is the camera half of the phase's first vertical slice. Plan 03-02
proved the maths in isolation; this plan turns it into a rig that moves a real
`THREE.PerspectiveCamera`, and gives that rig a tuning object the lil-gui panel can
sweep live in plan 03-06 — because the SC4 go/no-go gate is found by feel, and a
camera you cannot retune mid-drive cannot be genuinely tuned before it is judged.

Output: `src/core/camera-tuning.ts`, `src/render/camera/helicopter-camera.ts`,
`src/render/camera/camera-skin.ts`, two test files, and the skin CSS in `index.html`.
</objective>

## Phase Goal

**Note for the executor:** `.planning/ROADMAP.md`'s Phase 3 `**Goal:**` line is not in
the three-slot user-story form MVP mode expects. Reproduced verbatim rather than invented:

> The world under the tires changes how the car drives, and the game is viewed
> through its signature camera without losing the sense of speed

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-surfaces-helicopter-camera/03-CONTEXT.md
@.planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md
@.planning/phases/03-surfaces-helicopter-camera/03-PATTERNS.md
@.planning/phases/03-surfaces-helicopter-camera/03-VALIDATION.md
@.planning/phases/03-surfaces-helicopter-camera/03-01-SUMMARY.md
@.planning/phases/03-surfaces-helicopter-camera/03-02-SUMMARY.md
</context>

<interfaces>
<!-- Contracts extracted from the existing codebase and prior plans. No exploration required. -->

Landed by plan 03-01:
- `src/core/tuning-utils.ts`: `TuningRange`, `isPlainObject`, `isTuningRange`, `clampNode`, `copyLeaves`
- `src/core/surface-tuning.ts`: the exact defaults/ranges/clamp/parse/serialize/storage-key file shape to mirror

Landed by plan 03-02 (`src/render/camera/camera-math.ts`, imports nothing):
- `smoothstep01(t: number): number`
- `dampFactor(lambda: number, dtSec: number): number` — `1 - exp(-lambda*dtSec)`
- `blendedHeadingRad(velocityXZ, chassisForwardXZ, groundSpeedMs, blendSpeedMs): number` — `atan2(x, z)` order
- `interface CameraFraming { altitudeM; distanceM; fovDeg }`
- `interface CameraSpeedCurve { lowSpeedMs; highSpeedMs; low: CameraFraming; high: CameraFraming }`
- `speedFactor01(speedMs, lowMs, highMs): number`
- `framingForSpeed(speedMs, curve): CameraFraming`

Existing in `src/render/renderer.ts`:
- `const FOV_DEG = 55` (line 21), `NEAR = 0.1`, `FAR = 500`, `MAX_PIXEL_RATIO = 2`
- `export interface RenderContext { readonly renderer: THREE.WebGLRenderer; readonly camera: THREE.PerspectiveCamera }`
- `createRenderer` already calls `camera.updateProjectionMatrix()` inside its resize handler (line 94)

Existing placeholder to be replaced at the composition root by plan 03-06 (`src/main.ts:117-135, 154-160`):
`const CHASE_OFFSET = { x: 0, y: 8, z: 16 }` — a fixed offset behind/above the chassis with `camera.lookAt`.
These numbers are the starting point for `chaseFallback`'s defaults.

Existing "config-in / controller-out generic factory" precedent (D-09, cited by this phase's D-13):
`src/physics/vehicle.ts:135-139` — `createVehicle(world, tuning, spawn): Vehicle` names nothing "the player".

Existing DOM-building convention: `src/hud/speedometer.ts` builds every element with a direct
`document.createElementNS`/`createElement` call and appends to `document.body`.
`innerHTML` is banned repo-wide and asserted at zero by `tests/layering.test.ts:198-202`.

Layering rule (`tests/layering.test.ts:121-134`): no `src/render/` file may contain `world.step`,
`applyImpulse`, `setTranslation`, `setRotation` or `setNextKinematic` on a non-comment line.
Note `setRotationFromQuaternion` does not match that pattern (no word boundary after `setRotation`),
but prefer `camera.quaternion.slerp` regardless.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CameraTuning contract with the shared clamp/persist pipeline</name>
  <files>src/core/camera-tuning.ts, tests/camera-tuning.test.ts</files>
  <read_first>
    - src/core/surface-tuning.ts (plan 03-01's output — the file shape to mirror exactly)
    - src/core/tuning-utils.ts (the five helpers to delegate to)
    - src/core/vehicle-tuning.ts (the `TUNING_RANGES` nesting style and the per-field provenance doc-comment convention)
    - src/render/camera/camera-math.ts (plan 03-02 — `CameraSpeedCurve`'s field names, which this tuning object must be able to produce)
    - src/render/renderer.ts (lines 18-46 — `FOV_DEG = 55`, the value `chaseFallback.fovDeg` matches)
    - src/main.ts (lines 117-135 — `CHASE_OFFSET = { x: 0, y: 8, z: 16 }`, the source of `chaseFallback`'s altitude/distance defaults)
    - tests/surface-tuning.test.ts (plan 03-01's hostile-blob case list to mirror)
  </read_first>
  <behavior>
    Write `tests/camera-tuning.test.ts` FIRST; it must fail before the module exists.
    - `defaultCameraTuning()` returns a fresh object each call (mutating one does not affect the next).
    - Every numeric leaf of `defaultCameraTuning()` is finite and lies inside its `CAMERA_TUNING_RANGES` bound.
    - `CAMERA_TUNING_RANGES` has the identical key shape as `defaultCameraTuning()` at every level.
    - SC4 separation against the SHIPPED defaults (the assertion plan 03-02 deferred here):
      feeding `defaultCameraTuning()`'s framing block into `framingForSpeed` at 26.82 m/s
      (60 mph) and 49.17 m/s (110 mph) yields at least 6 degrees of FOV difference AND at
      least 5 m of altitude difference.
    - Near-constant pitch: `atan(lowAltitudeM / lowDistanceM)` and `atan(highAltitudeM / highDistanceM)`
      differ by less than 3 degrees — the rig is a high ANGLE at every speed, not a camera that
      flattens toward the horizon as it pulls back.
    - `clampCameraTuning` replaces `NaN`, `Infinity`, `-Infinity`, `null`, a string and a
      missing key with the matching default, and clamps an out-of-range finite number.
    - `fadeFloorOpacity`'s range minimum is strictly greater than 0 — a fully-invisible
      building removes spatial context (03-RESEARCH.md's explicit "low but non-zero floor").
    - Every damping lambda's range minimum is strictly greater than 0 — a lambda of 0 makes
      `dampFactor` return 0 forever and the camera never moves at all.
    - `parseSavedCameraTuning` returns `null` for `null`, for non-JSON text, for a JSON array,
      for a JSON scalar, and for an object missing any required top-level group; it never throws.
    - Round trip: `parseSavedCameraTuning(serializeCameraTuning(defaultCameraTuning()))`
      deep-equals `defaultCameraTuning()`.
    - A blob with `framing.highFovDeg` of `1e9` comes back clamped to the range max, never `1e9`
      — the value that would otherwise reach `camera.fov` (T-03-05).
  </behavior>
  <action>
    Create `src/core/camera-tuning.ts` mirroring `src/core/surface-tuning.ts`'s structure
    exactly (same `src/core/` layering header: no `three`, no `@dimforge/rapier3d`, no DOM,
    no wall clock).

    `export interface CameraTuning` with five nested groups, every leaf a plain number:
    - `framing`: `lowSpeedMs`, `highSpeedMs`, `lowAltitudeM`, `lowDistanceM`, `lowFovDeg`,
      `highAltitudeM`, `highDistanceM`, `highFovDeg`
    - `damping`: `positionLambda`, `headingLambda`, `framingLambda`
    - `heading`: `blendSpeedMs`
    - `occlusion`: `nearTargetMarginM`, `fadeFloorOpacity`, `fadeLambda`, `basePitchDeg`,
      `maxPitchDeg`, `fanRayCount`
    - `chaseFallback`: `altitudeM`, `distanceM`, `fovDeg`

    `export function defaultCameraTuning(): CameraTuning` returning these exact starting values,
    each with a doc comment carrying its rationale and an `[ASSUMED]` provenance tag plus the note
    that plan 03-11's go/no-go playtest is where they get corrected, exactly like Phase 2's
    plan 02-10 corrected `rearSideFriction`/`powerOversteerGain`/`bodyRollGain`:
    - `framing.lowSpeedMs` 8.94 (20 mph), `highSpeedMs` 53.64 (120 mph — aligned to the
      speedometer's existing amber transition so the camera stops changing where the gauge
      changes colour)
    - `framing.lowAltitudeM` 14, `lowDistanceM` 16, `lowFovDeg` 48
    - `framing.highAltitudeM` 26, `highDistanceM` 30, `highFovDeg` 62
      (comment the arithmetic: this gives a 7.0 degree FOV and 6.0 m altitude separation
      between 60 and 110 mph, and holds the pitch at roughly 41 degrees at both ends)
    - `damping.positionLambda` 6.0, `headingLambda` 2.5, `framingLambda` 1.5
      (comment `headingLambda` as the literal implementation of D-11: the camera's lag behind a
      changing velocity heading IS this one number, a deliberate output rather than an accident —
      "like a real chopper pilot tracking a car, not a rigid instant lock")
    - `heading.blendSpeedMs` 1.5
    - `occlusion.nearTargetMarginM` 2.0, `fadeFloorOpacity` 0.15, `fadeLambda` 8.0,
      `basePitchDeg` 41, `maxPitchDeg` 78, `fanRayCount` 5
    - `chaseFallback.altitudeM` 8, `distanceM` 16, `fovDeg` 55
      (comment: MUST MATCH `src/main.ts`'s existing `CHASE_OFFSET` and `src/render/renderer.ts`'s
      `FOV_DEG`, so selecting the fallback reproduces the exact view plan 02-10's feel session
      was signed off through)

    `export const CAMERA_TUNING_RANGES` mirroring that shape, every leaf a `TuningRange`.
    Bounds must satisfy the `<behavior>` constraints: `fadeFloorOpacity` min 0.05 max 0.6 step 0.01;
    every `*Lambda` min 0.2 max 20 step 0.1; `fanRayCount` min 1 max 9 step 1; FOV leaves
    min 25 max 100 step 1; altitude/distance leaves min 3 max 80 step 0.5; speed leaves
    min 0 max 80 step 0.5; `blendSpeedMs` min 0.1 max 10 step 0.1; pitch leaves min 10 max 89 step 1;
    `nearTargetMarginM` min 0 max 10 step 0.1.

    `clampCameraTuning`, `parseSavedCameraTuning`, `serializeCameraTuning` and
    `CAMERA_TUNING_STORAGE_KEY = "heat-street.camera-tuning.v1"` all mirror the surface-tuning
    equivalents, delegating to `clampNode`/`copyLeaves` from `./tuning-utils`. The required-group
    guard checks the five top-level keys. Do not re-implement any clamp helper here.
  </action>
  <verify>
    <automated>npx vitest run tests/camera-tuning.test.ts tests/layering.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/camera-tuning.test.ts` passes, covering every bullet in `<behavior>`
    - `grep -n "from \"./tuning-utils\"" src/core/camera-tuning.ts` returns at least one line
    - `grep -v '^\s*[*/]' src/core/camera-tuning.ts | grep -c "function clampNode\|function copyLeaves"` returns 0
    - `grep -c "heat-street.camera-tuning.v1" src/core/camera-tuning.ts` returns 1
    - `npx vitest run tests/layering.test.ts` passes with `src/core/camera-tuning.ts` in its glob
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The camera's every tunable number lives in one clamped, persistable, Node-testable object, and the shipped defaults provably separate 60 mph from 110 mph.</done>
</task>

<task type="auto">
  <name>Task 2: The helicopter rig and its D-12 chase-cam fallback</name>
  <files>src/render/camera/helicopter-camera.ts</files>
  <read_first>
    - src/render/camera/camera-math.ts (plan 03-02 — every function this rig calls)
    - src/core/camera-tuning.ts (Task 1's output — the tuning shape consumed here)
    - src/physics/vehicle.ts (lines 112-139 and 325-363 — the D-09 generic-factory precedent D-13 mirrors, and `sampleVehicle`'s derivation of world forward as `rotateVec(q, 0, 0, -1)`)
    - src/render/interpolator.ts (the project's stated rule that rotation is ALWAYS `THREE.Quaternion.slerp`, never a hand-rolled angle lerp)
    - src/render/renderer.ts (the `RenderContext` shape and the existing `updateProjectionMatrix` call site)
    - src/main.ts (lines 117-166 — where the rig's `update` will be called from in plan 03-06, and the placeholder it replaces)
    - .planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md (sections "Helicopter Camera Rig" and "Pitfall 3: Camera math leaking into the fixed physics tick")
    - .planning/phases/03-surfaces-helicopter-camera/03-PATTERNS.md (section "src/render/camera/helicopter-camera.ts")
  </read_first>
  <action>
    Create `src/render/camera/helicopter-camera.ts`.

    Export the generic target contract (D-13 — nothing here may name "the player car", so a
    Phase 7/8 pursuer satisfies it with zero change):
    - `export interface CameraTarget { position(): { x: number; y: number; z: number }; velocity(): { x: number; y: number; z: number }; forward(): { x: number; y: number; z: number } }`
      `forward()` is the target's own chassis-forward world vector, needed for the low-speed
      heading blend; document that it is only consulted below `heading.blendSpeedMs` and has no
      influence at speed, which is CAM-01's "velocity heading, not chassis yaw" requirement.

    Export the rig contract, satisfied identically by both implementations:
    - `export interface CameraRig { readonly kind: "helicopter" | "chase"; update(dtMs: number): void; snap(): void; setPitchBiasRad(rad: number): void; dispose(): void }`
      `snap()` places the camera at its fully-converged pose instantly with no damping — called
      once at boot so frame 1 is not a swoop in from the origin. `setPitchBiasRad` exists now,
      no-op on the chase rig, so plan 03-08's steepen mitigation needs no interface change.

    `export function createHelicopterCameraRig(camera: THREE.PerspectiveCamera, target: CameraTarget, tuning: CameraTuning): CameraRig`:
    - Holds module- or closure-scope reusable `THREE.Vector3`/`THREE.Quaternion` scratch objects,
      allocated once, so `update` allocates nothing per frame — `src/render/vehicle-view.ts`'s
      `SCRATCH_AXLE` convention (lines 160-164).
    - Keeps its own damped internal state: `headingRad`, and a `CameraFraming` triple. Do not
      re-derive them from the camera's current transform each frame.
    - Per `update(dtMs)`: convert to `dtSec`; read `target.position()`, `target.velocity()`,
      `target.forward()`; compute `groundSpeedMs` as `Math.hypot(velocity.x, velocity.z)` — never
      a vehicle-controller speed getter, per the project's existing Pitfall 1 discipline; compute
      the desired heading with `blendedHeadingRad(...)` and the desired framing with
      `framingForSpeed(groundSpeedMs, curve)` where `curve` is assembled from `tuning.framing`;
      damp `headingRad` toward the desired heading using a SHORTEST-ARC step (wrap the difference
      into [-PI, PI] before applying `dampFactor(tuning.damping.headingLambda, dtSec)`, or
      equivalently slerp two `THREE.Quaternion`s built from the two yaw angles — either is correct,
      a raw subtraction is not, and the file must say why); damp each framing field toward its
      desired value with `THREE.MathUtils.damp(current, desired, tuning.damping.framingLambda, dtSec)`.
    - Compute the desired camera position: the target position, offset BACKWARD along the damped
      heading by the damped `distanceM` and UP by the damped `altitudeM`, then apply
      `setPitchBiasRad`'s current bias by rotating that offset toward vertical about the target
      (a bias of 0 is the baseline pose). Damp `camera.position` toward it with
      `THREE.MathUtils.damp` per component at `tuning.damping.positionLambda`.
    - Aim with `camera.lookAt(targetPos)`. Set `camera.fov` to the damped `fovDeg` and call
      `camera.updateProjectionMatrix()` only when the value actually changed since last frame
      (compare against the previous number) — the matrix rebuild is cheap but not free and this
      runs every frame.
    - `snap()` runs the same computation with every damp factor forced to 1.
    - `dispose()` is a no-op with a comment saying so: the rig owns no listeners and no GPU
      resources; it exists so the composition root's teardown shape is uniform.

    `export function createChaseCameraRig(camera, target, tuning): CameraRig` — D-12's fallback,
    and per CONTEXT.md D-12 this is a genuine candidate for the SHIPPED camera if the helicopter
    rig fails plan 03-11's go/no-go gate, not a dev escape hatch. Same interface, `kind: "chase"`.
    It reproduces `src/main.ts`'s existing placeholder behaviour from `tuning.chaseFallback`:
    a fixed offset of `distanceM` behind and `altitudeM` above the target along the damped
    heading, a fixed `fovDeg`, `setPitchBiasRad` a documented no-op. Damp position and heading
    with the same lambdas so the two rigs are compared on framing, not on smoothing quality.

    Layering: this file may import `three`. It must not contain `world.step`, `applyImpulse`,
    `setTranslation`, `setRotation` or `setNextKinematic` on a non-comment line, and it must not
    import anything from `src/physics/`. It reads only what `CameraTarget` exposes.

    Add a header comment stating Pitfall 3 explicitly: `update` takes `dtMs` (a variable
    render-frame delta) and is called from `src/main.ts`'s `render(alpha, dtMs)` callback ONLY,
    never from `applyInput`/`onTickBegin`/the fixed-tick loop body, and no smoothing constant in
    this file may ever be expressed in ticks.
  </action>
  <verify>
    <automated>npx vitest run tests/layering.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and both factories compile against the `CameraRig` interface
    - `grep -v '^\s*[*/]' src/render/camera/helicopter-camera.ts | grep -c "from \"../../physics/"` returns 0 — the rig imports nothing from the physics tier
    - `grep -v '^\s*[*/]' src/render/camera/helicopter-camera.ts | grep -c "currentVehicleSpeed"` returns 0 — ground speed comes from the XZ components of `velocity()`
    - `grep -c "MathUtils.damp\|dampFactor" src/render/camera/helicopter-camera.ts` returns at least 3 — position, heading and framing are all frame-rate-independent
    - `grep -v '^\s*[*/]' src/render/camera/helicopter-camera.ts | grep -c "0\.1)\|, 0\.05)\|lerp(.*0\.[0-9]*)$"` returns 0 — no fixed-fraction per-frame lerp constant survives
    - `grep -c "kind: \"chase\"" src/render/camera/helicopter-camera.ts` returns 1 — the D-12 fallback exists as a real rig
    - `grep -v '^\s*[*/]' src/render/camera/helicopter-camera.ts | grep -c "player"` returns 0 — D-13's generic-target requirement, asserted the same way D-09 was
    - `npx vitest run tests/layering.test.ts` passes
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Two interchangeable rigs exist behind one interface: a velocity-heading helicopter cam with speed-driven framing, and the low chase cam D-12 keeps alive as a shippable alternative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Police/news and sports-broadcast camera skins, presentation only</name>
  <files>src/render/camera/camera-skin.ts, tests/camera-skin.test.ts, index.html</files>
  <read_first>
    - src/hud/speedometer.ts (lines 128-160 — the `document.createElement*` + `appendChild` DOM-building convention, and the `innerHTML` prohibition)
    - src/debug/debug-gate.ts (the `DEBUG_ENABLED` / `onDebugKey` convention plan 03-06 will use to drive the toggle)
    - tests/debug-gate.test.ts (the hand-built-fake, Node-safe test style for DOM-shaped inputs)
    - tests/layering.test.ts (lines 44-74 — the `isCommentLine` / `findHits` / `?raw` inline-glob idiom this test reuses for its source-level assertion)
    - index.html (the existing inline `<style>` block the skin CSS is added to)
    - .planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md (Pattern 4 and the "Camera Skin Presentation (CAM-03)" subsection — the recommended filter values and chrome treatments)
  </read_first>
  <behavior>
    Write `tests/camera-skin.test.ts` FIRST; it must fail before the module exists. It runs under
    `environment: "node"` and must never construct a real DOM element.
    - `CAMERA_SKINS` is `["police", "sports"]`.
    - `applySkinClasses("police", fakeClassList)` leaves exactly `skin-police` present and
      `skin-sports` absent; `applySkinClasses("sports", ...)` the mirror. The fake `classList`
      is a hand-built object with `add`/`remove`/`contains` backed by a `Set`, mirroring
      `tests/debug-gate.test.ts`'s fake style.
    - Calling `applySkinClasses` twice with the same skin is idempotent — exactly one skin class
      remains, never two.
    - Switching police to sports to police leaves exactly one skin class at every step.
    - CAM-03's "presentation only — never distance, damping or targeting", asserted at the SOURCE
      level using `tests/layering.test.ts`'s `?raw` inline-glob + `isCommentLine` idiom: no
      non-comment line of `src/render/camera/camera-skin.ts` matches
      `/\b(camera|fov|updateProjectionMatrix|quaternion|lookAt|position|distance|Lambda|CameraRig|CameraTarget)\b/`.
      This is the mechanical proof that a skin switch cannot reach camera-rig state — the module
      holds no reference capable of it. Comment the test to say the doc comment is deliberately
      excluded because it must be free to NAME the things it may not touch.
    - `createCameraSkin` is exercised only through an injected fake element pair (an object with
      a `classList` fake and a `textContent` field), never `document` — the test asserts
      `cycle()` returns the next skin and wraps around, and that `current()` tracks it.
  </behavior>
  <action>
    Create `src/render/camera/camera-skin.ts`:
    - `export const CAMERA_SKINS = ["police", "sports"] as const` and
      `export type CameraSkin = (typeof CAMERA_SKINS)[number]`.
    - `export interface SkinClassList { add(token: string): void; remove(token: string): void }`
      — the minimal structural subset of `DOMTokenList` this module needs, so the test can
      supply a hand-built fake with no jsdom.
    - `export function applySkinClasses(skin: CameraSkin, classList: SkinClassList): void` —
      removes every token in `CAMERA_SKINS.map(s => "skin-" + s)` then adds the one for `skin`.
      Remove-all-then-add, never a toggle, so repeated calls cannot leave two classes on.
    - `export interface CameraSkinSwitcher { current(): CameraSkin; set(skin: CameraSkin): void; cycle(): CameraSkin; dispose(): void }`
    - `export function createCameraSkin(canvasClassList: SkinClassList, chrome: { textContent: string | null }, initial?: CameraSkin): CameraSkinSwitcher`
      — takes the two things it writes to as PARAMETERS rather than reaching for `document`
      itself, which is what makes the whole module Node-testable. `set` calls
      `applySkinClasses` and writes the skin's chrome label into `chrome.textContent`
      (`"● LIVE"` for police, `"◆ BROADCAST"` for sports — `textContent` only, never `innerHTML`).
      `cycle()` advances through `CAMERA_SKINS` with wraparound and returns the new skin.
      `dispose()` removes both skin classes and blanks the chrome label.
    - Also export a thin `export function createCameraSkinChrome(): HTMLElement` that builds the
      chrome overlay element with `document.createElement("div")`, sets `id = "camera-chrome"`
      and appends it to `document.body`, matching `createSpeedometer`'s own append-to-body
      convention. Keep it a SEPARATE export from `createCameraSkin` so the Node test can exercise
      all the logic without it. Plan 03-06 calls both.
    - Module doc comment: state CAM-03's requirement verbatim, state that the mechanism is a CSS
      class swap plus a DOM chrome label and nothing else, and state why — a second Three.js
      post-processing pass would break the single-draw-call-per-frame assumption documented in
      `src/render/renderer.ts` that the profiler HUD's `renderer.info` read depends on
      (03-RESEARCH.md's "Anti-Patterns to Avoid"). Also record 03-RESEARCH.md Pitfall 4: a CSS
      filter is a browser-compositor operation the in-game profiler HUD cannot see, so the skins'
      frame-time cost must be spot-checked in the browser's own performance panel during plan
      03-11's playtest, not trusted to the HUD numbers.

    Edit `index.html`'s existing inline `<style>` block, adding (D-15, values from
    03-RESEARCH.md's "Camera Skin Presentation" subsection, each with a CSS comment naming it as
    a starting point to be refined in plan 03-11's playtest):
    - `#game.skin-police { filter: saturate(0.85) contrast(1.05) hue-rotate(-4deg); }` — the cooler,
      desaturated, higher-contrast ENG news-gathering look
    - `#game.skin-sports { filter: saturate(1.25) contrast(1.1) brightness(1.05); }` — the warmer,
      more saturated sports-aerial look
    - `#camera-chrome` positioned absolutely, `pointer-events: none`, a monospace font, top-left
      or top-right placement that does not collide with the speedometer, plus
      `#game.skin-police ~ #camera-chrome { color: #e8eef5; }` and
      `#game.skin-sports ~ #camera-chrome { color: #ffd97a; }` so the chrome differs per skin too
    - A `#game.skin-police` vignette via an additional absolutely-positioned pseudo-element or a
      second overlay div is optional; if added it must also be `pointer-events: none`

    Do NOT add any `EffectComposer`, render pass, or `ShaderMaterial`.
  </action>
  <verify>
    <automated>npx vitest run tests/camera-skin.test.ts tests/layering.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/camera-skin.test.ts` passes, covering every bullet in `<behavior>` including the source-level presentation-only assertion
    - `grep -c "skin-police" index.html` returns at least 1 and `grep -c "skin-sports" index.html` returns at least 1
    - `grep -c "pointer-events: none" index.html` returns at least 1
    - `grep -c "innerHTML" src/render/camera/camera-skin.ts` returns 0
    - `grep -c "EffectComposer\|ShaderMaterial\|RenderPass" src/render/camera/camera-skin.ts index.html` returns 0 for both files
    - `grep -c "from \"three\"" src/render/camera/camera-skin.ts` returns 0 — the skin module does not import the engine at all
    - `npx vitest run tests/layering.test.ts` passes
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Two visually distinct camera presentations can be swapped by changing one CSS class and one text label, and the module is structurally incapable of touching the rig.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → `src/core/camera-tuning.ts` | A devtools-editable blob under `heat-street.camera-tuning.v1` crosses into values that drive `camera.fov` and `camera.position` |
| `CameraTuning` → `THREE.PerspectiveCamera` | Numbers leaving the tuning module reach `camera.fov` + `updateProjectionMatrix()` every render frame |
| Sampled vehicle state → `CameraTarget` | The rig reads position/velocity/forward across the physics-to-render boundary; it must never write back |
| `camera-skin.ts` → `<canvas>` / DOM | The only DOM writes are a class token swap and a `textContent` assignment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `parseSavedCameraTuning` reading `heat-street.camera-tuning.v1` | mitigate | Mirrors `parseSavedTuning` exactly and delegates to the shared `clampNode`/`copyLeaves`; never throws, never returns a partially-validated object. Asserted by `tests/camera-tuning.test.ts`'s hostile-blob cases |
| T-03-05 | Denial of Service | A `NaN`, `Infinity` or absurd FOV reaching `camera.fov` + `updateProjectionMatrix()` and blanking the canvas with no console error | mitigate | `CAMERA_TUNING_RANGES` bounds FOV to 25-100 and `clampCameraTuning` substitutes the default for any non-finite leaf; `framingForSpeed` (plan 03-02) independently coerces a non-finite speed |
| T-03-11 | Denial of Service | A damping lambda of 0 freezing the camera permanently, or a `fadeFloorOpacity` of 0 making buildings vanish entirely | mitigate | Both range minima are strictly greater than zero, asserted directly by `tests/camera-tuning.test.ts` |
| T-03-12 | Tampering (requirement integrity) | A camera skin changing distance, damping or targeting, violating CAM-03's explicit "presentation only" wording | mitigate | `camera-skin.ts` imports neither `three` nor the rig, and `tests/camera-skin.test.ts` asserts at the source level that no non-comment line references any camera/rig identifier |
| T-03-13 | Tampering | DOM injection via the chrome overlay label | mitigate | `textContent` only; `innerHTML` is banned repo-wide and asserted at zero by `tests/layering.test.ts`. The label strings are module constants, never user- or storage-derived |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `lil-gui` (needed by plan 03-06) comes from `three/addons/libs/lil-gui.module.min.js`, already bundled, per the Phase 02-09 precedent |
</threat_model>

<verification>
1. `npx vitest run tests/camera-tuning.test.ts tests/camera-skin.test.ts` passes.
2. `npx vitest run tests/camera-heading.test.ts tests/camera-framing.test.ts` still passes — plan 03-02's core is untouched.
3. `npx vitest run tests/layering.test.ts` passes with `src/core/camera-tuning.ts` and `src/render/camera/**` in scope.
4. `npm run check` exits 0.
</verification>

<success_criteria>
- CAM-01: the rig derives heading from velocity, blending to chassis forward only below `heading.blendSpeedMs`, with the lag set by one tunable lambda (D-11).
- CAM-02: altitude, distance and FOV all respond to speed with a provable 60-vs-110 mph separation on the shipped defaults.
- CAM-03: both skins exist and are proven presentation-only by a source-level assertion, not by review.
- D-12: `createChaseCameraRig` is a fully-implemented sibling behind the same interface, defaulting to the exact view plan 02-10 was signed off through.
- D-13: nothing in `CameraTarget` or either factory's signature names the player.
</success_criteria>

<output>
Create `.planning/phases/03-surfaces-helicopter-camera/03-04-SUMMARY.md` when done.
</output>
