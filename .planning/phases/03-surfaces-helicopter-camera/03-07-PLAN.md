---
phase: 03-surfaces-helicopter-camera
plan: 07
type: execute
wave: 4
depends_on: ["03-04", "03-05", "03-06"]
files_modified:
  - src/main.ts
  - src/render/camera/object-camera-target.ts
  - src/debug/tuning-panel.ts
  - src/debug/telemetry-hud.ts
  - tests/layering.test.ts
autonomous: true
requirements: [SURF-01, CAM-01, CAM-02, CAM-03]

must_haves:
  truths:
    - "Loading the page puts the player on the six-surface test scene, viewed through the helicopter camera"
    - "Driving from tarmac onto gravel changes how the car handles, live, on screen"
    - "Camera altitude, follow distance and FOV visibly change with speed"
    - "A dev hotkey swaps between the police/news and sports-broadcast skins, and another swaps between the helicopter rig and the D-12 chase fallback"
    - "Per-surface grip and every camera tunable can be swept live in the existing lil-gui panel, and re-measured in the existing telemetry panel, with no code edit"
  artifacts:
    - path: "src/render/camera/object-camera-target.ts"
      provides: "createObjectCameraTarget — adapts any THREE.Object3D plus a velocity callback to CameraTarget, importing nothing from src/physics"
      exports: ["createObjectCameraTarget"]
    - path: "src/main.ts"
      provides: "The Phase 3 composition root"
      contains: "createSurfaceScene"
    - path: "tests/layering.test.ts"
      provides: "New mechanical rules for the src/render/camera tier and a raised source-file floor"
  key_links:
    - from: "src/main.ts"
      to: "src/physics/surface-scene.ts"
      via: "createSurfaceScene replaces createVehicleScene at the composition root"
      pattern: "createSurfaceScene"
    - from: "src/main.ts"
      to: "src/render/camera/helicopter-camera.ts"
      via: "rig.update(dtMs) inside the render callback, AFTER applyAllInterpolated"
      pattern: "\\.update\\(dtMs\\)"
    - from: "src/debug/tuning-panel.ts"
      to: "src/core/surface-tuning.ts"
      via: "Surfaces folder sliders bound to the live SurfaceProfiles object"
      pattern: "SURFACE_PROFILE_RANGES"
---

<objective>
Put the whole slice on screen: the six-surface scene, the helicopter camera, both
skins, the chase fallback, and live tuning for all of it.

Purpose: this is the first moment the phase's two halves are a game rather than two
test suites. Everything up to here was provable headlessly; SC3, SC4 and SC6 cannot be.
The tuning-panel and telemetry-panel extensions are not polish — plan 03-08's go/no-go
playtest is worthless if the camera and the grip profiles cannot be retuned and
re-measured inside that same session, which is exactly the property Phase 2's plan
02-09 established and plan 02-10 then depended on.

Output: a rewired `src/main.ts`, a generic camera-target adapter, extended tuning and
telemetry panels, and new layering rules for the camera tier.
</objective>

## Phase Goal

**Note for the executor:** `.planning/ROADMAP.md`'s Phase 3 `**Goal:**` line is not in
the three-slot user-story form MVP mode expects. Reproduced verbatim rather than invented:

> The world under the tires changes how the car drives, and the game is viewed
> through its signature camera without losing the sense of speed

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-surfaces-helicopter-camera/03-CONTEXT.md
@.planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md
@.planning/phases/03-surfaces-helicopter-camera/03-PATTERNS.md
@.planning/phases/03-surfaces-helicopter-camera/03-04-SUMMARY.md
@.planning/phases/03-surfaces-helicopter-camera/03-05-SUMMARY.md
@.planning/phases/03-surfaces-helicopter-camera/03-06-SUMMARY.md
</context>

<interfaces>
<!-- Contracts extracted from the existing codebase and prior plans. -->

`src/main.ts` today (167 lines, read it in full). Key structure:
- Loads `parseSavedTuning(localStorage.getItem(TUNING_STORAGE_KEY)) ?? defaultTuning()` (lines 45-48)
- `const world = createWorld(); const scene = createVehicleScene(world, tuning); const transforms = new TransformCache(scene.bodies);`
- `const { renderer, camera } = createRenderer(canvas);`
- `const view = createVehicleView(tuning.wheels.radius, tuning.wheels.halfTrack, tuning.wheels.halfWheelbase, tuning.chassis.halfExtents);`
- `const input = new LiveInputSource();`
- `const speedo = createSpeedometer();` — always constructed, NOT gated on `DEBUG_ENABLED` (player-facing)
- The three-times-repeated dev shape: `const x = DEBUG_ENABLED ? createX(...) : null; if (x) { onDebugKey("KeyG", () => x.toggle()); }`
  — currently `hud` (via `onDebugToggle`), `panel` (`KeyG`), `telemetry` (`KeyT`)
- `const CHASE_OFFSET = { x: 0, y: 8, z: 16 }` and the placeholder camera block at lines 117-135 and 154-160
- The render callback order today (lines 143-165): speedo update FIRST (its comment explains the
  profiler HUD reads `renderer.info` after `render()` returns and that read is only valid because
  there is exactly one draw call per frame), then camera, then `view.updateWheels`, then
  `applyAllInterpolated(view.meshes, transforms, alpha)`, then `renderer.render(view.scene, camera)`
- `startLoop({ world, input, transforms, applyInput: scene.applyInput, onTickBegin: scene.preTick, render(alpha, dtMs) {...}, hud: ... })`

`src/debug/tuning-panel.ts` today (488 lines, read it in full). Key structure:
- `export function createTuningPanel(tuning: VehicleTuning, onApply: () => void, storage?: TuningStorage): TuningPanel`
- Folder order is a documented CONTRACT: `Chassis -> Suspension -> Grip -> Drive -> Assists -> Telemetry`
- `addNumber(folder, obj, key, range)` helper; wheel properties bind `.onChange(onApply)`,
  `chassis.mass`/`comOffset`/`halfExtents` bind `.onFinishChange(onApply)` (Pitfall 10)
- `writeDefaultsOnto(targetNode, freshNode, rangeNode)` mutates leaves IN PLACE because every
  lil-gui controller holds a reference to the nested object
- A single root `gui.onChange(() => { store.set(TUNING_STORAGE_KEY, serializeTuning(tuning)); })`
  persists on ANY change anywhere in the tree
- A two-click reset control is the LAST root control; it calls `writeDefaultsOnto`,
  `store.remove(TUNING_STORAGE_KEY)`, `onApply()`, then `controller.updateDisplay()` for every
  `gui.controllersRecursive()`
- `gui.hide()` at the end — the module is GATE-FREE, never checks `DEBUG_ENABLED` itself

`src/debug/telemetry-hud.ts` — `createTelemetryHud(getTuning: () => VehicleTuning)`, `KeyT`,
with a deliberate no-auto-run-on-open discipline (an unexpected 100-300 ms frame stall mid-drive
would be a bad surprise; the run is always a deliberate button press).

Landed by plan 03-04: `createHelicopterCameraRig(camera, target, cameraTuning)`,
`createChaseCameraRig(camera, target, cameraTuning)`, `interface CameraRig { kind; update(dtMs); snap(); setPitchBiasRad(rad); dispose() }`,
`interface CameraTarget { position(); velocity(); forward() }`,
`createCameraSkin(canvasClassList, chrome, initial?)`, `createCameraSkinChrome()`,
`defaultCameraTuning()`, `parseSavedCameraTuning`, `CAMERA_TUNING_STORAGE_KEY`, `CAMERA_TUNING_RANGES`.

Landed by plan 03-05: `createSurfaceScene(world, tuning, profiles): SurfaceScene` (with
`setSurfaceProfiles`), `SURFACE_SCENE_SPAWN`, `createSurfaceWorld(): SurfaceWorldView`
(`group`, `buildingMeshes`, `dispose`), and `createVehicleView(..., options?: { includePhase2Ground?: boolean })`.

Landed by plan 03-06: `runSurfaceSkidpadSweep(tuning, profiles): RoutineResult[]`.

Landed by plan 03-01: `parseSavedSurfaceProfiles`, `defaultSurfaceProfiles`, `serializeSurfaceProfiles`,
`SURFACE_TUNING_STORAGE_KEY`, `SURFACE_PROFILE_RANGES`.

`tests/layering.test.ts` — the mechanical layering gate (202 lines). Its `FILES.length` floor is
currently 20 (raised from 10 in plan 02-05 for the same reason it is raised again here).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the composition root onto the surface scene and the helicopter camera</name>
  <files>src/main.ts, src/render/camera/object-camera-target.ts</files>
  <read_first>
    - src/main.ts (the whole file — every comment in it is load-bearing, especially the render-callback ordering comment at lines 143-150 and the superseded-predecessor comments at lines 16-19 and 54-60)
    - src/render/camera/helicopter-camera.ts (plan 03-04 — `CameraTarget`, `CameraRig`, both factories)
    - src/render/camera/camera-skin.ts (plan 03-04 — `createCameraSkin`, `createCameraSkinChrome`)
    - src/core/camera-tuning.ts (plan 03-04 — `parseSavedCameraTuning`, `CAMERA_TUNING_STORAGE_KEY`)
    - src/core/surface-tuning.ts (plan 03-01 — `parseSavedSurfaceProfiles`, `SURFACE_TUNING_STORAGE_KEY`)
    - src/physics/surface-scene.ts (plan 03-05 — `createSurfaceScene`, `setSurfaceProfiles`, `SURFACE_SCENE_SPAWN`)
    - src/render/surface-view.ts (plan 03-05 — `createSurfaceWorld`, `buildingMeshes`)
    - src/render/vehicle-view.ts (the new `includePhase2Ground` option and the `meshes` array whose index 0 is the chassis)
    - src/render/interpolator.ts (what `applyAllInterpolated` writes and when the meshes hold their interpolated transform)
    - src/debug/debug-gate.ts (`DEBUG_ENABLED`, `onDebugKey`, `onDebugToggle` and the existing key assignments)
    - .planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md ("Pitfall 3: Camera math leaking into the fixed physics tick")
  </read_first>
  <action>
    First create `src/render/camera/object-camera-target.ts`:
    `export function createObjectCameraTarget(object: THREE.Object3D, velocity: () => { x: number; y: number; z: number }): CameraTarget`.
    `position()` returns the object's current `position` (the INTERPOLATED transform, see below);
    `forward()` returns `(0, 0, -1)` rotated by the object's current quaternion into a reused
    scratch `THREE.Vector3` (the vehicle's forward is local `-Z`); `velocity()` delegates to the
    callback. This file imports `three` and the `CameraTarget` type only — it must import nothing
    from `src/physics/`, which is what keeps the whole camera tier physics-free and D-13-generic.

    Then rewire `src/main.ts`:

    1. Load all three persisted tuning blobs at boot, each through its own parse-or-default:
       `tuning` (unchanged), `surfaceProfiles` from `parseSavedSurfaceProfiles(localStorage.getItem(SURFACE_TUNING_STORAGE_KEY)) ?? defaultSurfaceProfiles()`,
       `cameraTuning` from `parseSavedCameraTuning(localStorage.getItem(CAMERA_TUNING_STORAGE_KEY)) ?? defaultCameraTuning()`.
       Guard `typeof localStorage !== "undefined"` on each, exactly as the existing line 47 does.
       Extend the existing comment: these are untrusted blobs and the parse function is what makes
       them safe — never `JSON.parse` directly here (threats T-02-01 / T-03-01).

    2. Replace `createVehicleScene(world, tuning)` with `createSurfaceScene(world, tuning, surfaceProfiles)`.
       Update the existing "deliberately LEFT IN PLACE" comment block (lines 54-60) to name
       `src/physics/vehicle-scene.ts` and `src/render/vehicle-view.ts`'s ground/ramp visuals as the
       newly-superseded-at-the-composition-root pair, alongside the Phase 1 debug scene that
       already lives there. Say explicitly that `vehicle-scene.ts` remains imported by
       `tests/vehicle-scene.test.ts` and must not be deleted.

    3. Pass `{ includePhase2Ground: false }` to `createVehicleView`, then build
       `const surfaceWorld = createSurfaceWorld();` and `view.scene.add(surfaceWorld.group);`.

    4. Build the camera target from the INTERPOLATED chassis mesh, not the rigid body:
       `createObjectCameraTarget(view.meshes[0], () => scene.vehicle.body.linvel())`.
       Comment this decision explicitly and give the reason: the chassis MESH carries the
       alpha-interpolated transform `applyAllInterpolated` writes each frame, while
       `body.translation()` is the raw fixed-tick position. A camera that follows the raw position
       while the car is drawn at the interpolated one makes the car visibly jitter inside the
       frame at any refresh rate that is not exactly 60 Hz — which would read as "the camera
       shakes" and would poison SC3's drift-stability judgement for a reason that has nothing to
       do with the rig. Velocity does not need interpolating and comes straight from the body.

    5. Build both rigs and keep one active:
       `const helicopterRig = createHelicopterCameraRig(camera, target, cameraTuning);`
       `const chaseRig = createChaseCameraRig(camera, target, cameraTuning);`
       `let activeRig: CameraRig = helicopterRig;` then `activeRig.snap();` so frame 1 is already
       framed rather than swooping in from the renderer's provisional pose. These are NOT gated on
       `DEBUG_ENABLED` — the camera is player-facing, exactly like `speedo`. Only the SWAP is a dev
       control: `if (DEBUG_ENABLED) { onDebugKey("KeyC", () => { activeRig = activeRig === helicopterRig ? chaseRig : helicopterRig; activeRig.snap(); }); }`.
       Comment that per CONTEXT.md D-12 the chase rig is a genuine shipping candidate, not a dev
       escape hatch, and that if plan 03-08's go/no-go gate fails, flipping the `activeRig`
       initialiser here is the whole change.

    6. Camera skin: build the chrome element with `createCameraSkinChrome()` and the switcher with
       `createCameraSkin(canvas.classList, chrome, "police")`. Always constructed — CAM-03 makes
       the camera permanently skinned, and Phase 5 is what will drive the choice from the active
       game mode. Gate only the preview toggle (D-14):
       `if (DEBUG_ENABLED) { onDebugKey("KeyV", () => skin.cycle()); }`.
       Do not reuse `KeyG`, `KeyT` or `Backquote`; record the full key map in a comment:
       Backquote = profiler HUD, G = tuning panel, T = telemetry panel, C = camera rig swap,
       V = camera skin, O = reserved for plan 03-09's occlusion mitigation A/B.

    7. Render callback — the new order, with the existing ordering comment extended to explain it:
       a. `speedo.update(Math.hypot(v.x, v.z), dtMs)` stays FIRST, for the reason its current
          comment gives.
       b. `view.updateWheels(scene.vehicle.controller)`
       c. `applyAllInterpolated(view.meshes, transforms, alpha)`
       d. `activeRig.update(dtMs)` — AFTER (c), because the rig reads `view.meshes[0]`'s
          interpolated transform. Moving this above (c) reintroduces the one-frame-stale camera
          jitter described in step 4.
       e. `renderer.render(view.scene, camera)`
       Nothing here may be called from `applyInput` or `onTickBegin` — 03-RESEARCH.md Pitfall 3.
       Delete the `CHASE_OFFSET` constant and the entire placeholder camera block; do not leave it
       commented out.

    8. `onApply` handlers for plan 03-07 Task 2's panel:
       `() => scene.setTuning(tuning)` (unchanged), `() => scene.setSurfaceProfiles(surfaceProfiles)`,
       and `() => { /* camera tuning is read live by the rig each frame */ }` — if the rig reads
       its `CameraTuning` object by reference every `update`, the camera handler is genuinely a
       no-op and must say so in a comment rather than be silently omitted.
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 — the production bundle compiles
    - `grep -c "createSurfaceScene" src/main.ts` returns 1 and `grep -c "createVehicleScene" src/main.ts` returns 0
    - `grep -c "CHASE_OFFSET" src/main.ts` returns 0 — the placeholder is gone, not commented out
    - `grep -c "camera.lookAt" src/main.ts` returns 0 — camera aiming now lives entirely in the rig
    - In `src/main.ts`'s render callback, the line containing `applyAllInterpolated` appears BEFORE the line containing `activeRig.update(dtMs)`, which appears before `renderer.render` — verify by reading the emitted file
    - `grep -c "onDebugKey(\"KeyC\"\|onDebugKey(\"KeyV\"" src/main.ts` returns 2
    - `grep -v '^\s*[*/]' src/main.ts | grep -c "JSON.parse"` returns 0 — every blob goes through a parse-and-clamp function
    - `grep -v '^\s*[*/]' src/render/camera/object-camera-target.ts | grep -c "from \"../../physics/"` returns 0
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>`npm run dev` loads the six-surface scene viewed through the helicopter camera, with the police skin applied and both dev toggles registered under `?debug`.</done>
</task>

<task type="auto">
  <name>Task 2: Surfaces and Camera folders in the tuning panel, surface sweep in the telemetry panel</name>
  <files>src/debug/tuning-panel.ts, src/debug/telemetry-hud.ts, src/main.ts</files>
  <read_first>
    - src/debug/tuning-panel.ts (the whole file — `addNumber`, the folder-order contract at lines 161-170, `writeDefaultsOnto` at lines 108-130, the root `gui.onChange` persistence at lines ~419-421, and the two-click reset at lines ~428-470)
    - src/debug/telemetry-hud.ts (the whole file — the button/table structure and the no-auto-run-on-open discipline)
    - src/core/surface-tuning.ts and src/core/camera-tuning.ts (the ranges, defaults, serializers and storage keys the new folders bind to)
    - src/physics/telemetry/surface-routines.ts (plan 03-06 — `runSurfaceSkidpadSweep`)
    - src/main.ts (Task 1's output — the call sites being widened)
    - .planning/phases/03-surfaces-helicopter-camera/03-PATTERNS.md (section "src/debug/tuning-panel.ts (MODIFIED — extend existing panel)")
  </read_first>
  <action>
    Widen `createTuningPanel`'s signature to
    `createTuningPanel(tuning: VehicleTuning, surfaces: SurfaceProfiles, cameraTuning: CameraTuning, handlers: { onApplyVehicle(): void; onApplySurfaces(): void; onApplyCamera(): void }, storage?: TuningStorage): TuningPanel`
    and update `src/main.ts`'s single call site. Every existing `onApply` reference inside the
    panel becomes `handlers.onApplyVehicle`.

    Append TWO new folders AFTER the existing six, never reordering
    `Chassis -> Suspension -> Grip -> Drive -> Assists -> Telemetry` — that order is a documented
    contract and muscle memory from Phase 2's tuning sessions must survive:
    - `Surfaces`: twelve `addNumber` controls, `forwardGrip` and `lateralGrip` for each of the six
      surfaces in `SURFACE_TYPES` order, bound to `SURFACE_PROFILE_RANGES`'s matching leaves.
      Name each control so both the surface and the axis are visible in a 320 px panel, e.g.
      `gravel fwd` / `gravel lat`. All twelve bind `.onChange(handlers.onApplySurfaces)` —
      these are plain per-wheel multipliers applied fresh every tick and none of them invalidates
      cached mass properties the way `chassis.mass` does, so Pitfall 10's `.onFinishChange`
      restriction does not apply. Say so in a comment so the asymmetry with the Chassis folder
      reads as deliberate.
    - `Camera`: one control per numeric leaf of `CAMERA_TUNING_RANGES`, grouped by adding
      sub-folders `Framing`, `Damping`, `Heading`, `Occlusion`, `Chase fallback` inside it so the
      ~21 controls remain navigable. All bind `.onChange(handlers.onApplyCamera)`.

    Persistence: change the single root `gui.onChange` to write all three blobs —
    `serializeTuning(tuning)` under `TUNING_STORAGE_KEY`, `serializeSurfaceProfiles(surfaces)`
    under `SURFACE_TUNING_STORAGE_KEY`, `serializeCameraTuning(cameraTuning)` under
    `CAMERA_TUNING_STORAGE_KEY`. Keep the existing comment explaining why a change anywhere in the
    tree bubbles to the root, and extend it to note that three small writes on any change is
    deliberate: three separate keys means a corrupt camera blob cannot take the vehicle tuning down
    with it, which a single merged blob would.

    Reset: extend the two-click reset to call `writeDefaultsOnto` three times (once per
    range/default pair), `store.remove` all three keys, and call all three handlers, before the
    existing `controllersRecursive().updateDisplay()` loop. `writeDefaultsOnto` already walks an
    arbitrary range tree and needs no change.

    In `src/debug/telemetry-hud.ts`: widen `createTelemetryHud` to
    `createTelemetryHud(getTuning: () => VehicleTuning, getSurfaceProfiles: () => SurfaceProfiles)`
    and add a SECOND button labelled `Run surface sweep` next to the existing run button. It calls
    `runSurfaceSkidpadSweep(getTuning(), getSurfaceProfiles())` and renders the six returned
    `RoutineResult`s into the same results table the existing button populates. Preserve the
    no-auto-run-on-open discipline for the new button too — it must only ever fire on a deliberate
    click. Add a note that this sweep runs six throwaway worlds and is therefore roughly six times
    the existing run's stall, so it is deliberately a separate button rather than being folded into
    the existing one.

    Both panels stay GATE-FREE — neither module may check `DEBUG_ENABLED` itself; `src/main.ts`
    does the gating, matching `createHud`'s established convention.
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -c "addFolder(\"Surfaces\")\|addFolder(\"Camera\")" src/debug/tuning-panel.ts` returns 2
    - The `addFolder` calls for `Chassis`, `Suspension`, `Grip`, `Drive`, `Assists`, `Telemetry` still appear in that order and all six appear BEFORE the two new ones — verify by reading the emitted file
    - `grep -c "SURFACE_TUNING_STORAGE_KEY\|CAMERA_TUNING_STORAGE_KEY" src/debug/tuning-panel.ts` returns at least 4 (a `store.set` and a `store.remove` for each)
    - `grep -c "onFinishChange" src/debug/tuning-panel.ts` returns the same count as before this task — no new control was bound the slow way
    - `grep -c "DEBUG_ENABLED" src/debug/tuning-panel.ts src/debug/telemetry-hud.ts` returns 0 for both
    - `grep -c "runSurfaceSkidpadSweep" src/debug/telemetry-hud.ts` returns 1
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Every per-surface grip multiplier and every camera tunable is a live slider under `?debug` + `G`, and the six-surface skidpad sweep is one click away under `?debug` + `T`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mechanical layering rules for the new camera tier</name>
  <files>tests/layering.test.ts</files>
  <read_first>
    - tests/layering.test.ts (the whole file — the `?raw` inline-glob idiom, `isCommentLine`, `findHits`, `scan`, `format`, and every existing `describe` block's shape)
    - src/render/camera/helicopter-camera.ts, src/render/camera/camera-math.ts, src/render/camera/occlusion.ts, src/render/camera/camera-skin.ts, src/render/camera/object-camera-target.ts (the files the new rules govern)
    - .planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md ("Pitfall 3: Camera math leaking into the fixed physics tick")
  </read_first>
  <behavior>
    Extend `tests/layering.test.ts` with new `describe` blocks in its existing style — each one
    scans, formats hits as `path:line: text`, and asserts the formatted string is empty.
    - Raise the `FILES.length` floor from 20 to 28, with a comment giving the same reason plan
      02-05 gave when it raised it from 10 to 20: Phase 3 adds roughly ten source files, so the
      floor stays a meaningful guard against a broken glob rather than a value that was only ever
      true in Phase 2.
    - New rule (T-03-20): no file under `src/render/camera/` imports anything from `src/physics/`.
      Pattern: `/from\s+["'][^"']*physics\//`. This is what keeps the rig reusable for a Phase 7/8
      pursuer (D-13) and what stops camera code from reaching into the simulation.
    - New rule (T-03-21): no file under `src/render/camera/` references `DT` or a tick-count
      smoothing constant. Pattern: `/from\s+["'][^"']*sim-clock["']|\bDT\b/`. 03-RESEARCH.md
      Pitfall 3's warning sign is "a camera-smoothing constant expressed in ticks rather than
      seconds"; this is that warning sign made mechanical. The camera runs on `dtMs` from the
      variable render frame, never on the fixed tick.
    - New rule (T-03-22): `src/render/camera/camera-math.ts` and `src/render/camera/occlusion.ts`
      import nothing from `three`. Pattern: `/from\s+["']three["']/`, scoped to those two files by
      exact path. This is the property that keeps the Wave-0 tests runnable under
      `environment: "node"`; without a mechanical guard, a future edit adding one `THREE.Vector3`
      would silently make three test files unrunnable.
    - Each new `describe` includes the existing convention's "scanned at least one file" guard so a
      path typo cannot make the rule trivially green.
    - Every pre-existing rule and its test count stays unchanged.
  </behavior>
  <action>
    Edit `tests/layering.test.ts` only. Add the four changes described above, matching the file's
    existing structure exactly: a `const FORBIDDEN = /.../` per `describe`, a `FILES.filter(...)`
    to scope it, a "scanned at least one" `it`, then a per-file `it` that asserts
    `format(findHits(file, FORBIDDEN))` is `""`.

    Do not add a rule for `src/audio/` — that directory does not exist until plan 03-11, and the
    "scanned at least one file" guard would fail. Leave a comment at the end of the file naming
    `src/audio/**` as the tier plan 03-11 must add a rule for, so it is not forgotten.

    Do not weaken or reword any existing rule. In particular, do not add `\bdocument\.` to the
    `src/render/` rule — `camera-skin.ts` legitimately calls `document.createElement`, the same
    exception the `src/hud/` rule already documents for `createElementNS`.
  </action>
  <verify>
    <automated>npx vitest run tests/layering.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/layering.test.ts` passes with strictly more tests than before this task
    - `grep -c "toBeGreaterThanOrEqual(28)" tests/layering.test.ts` returns 1
    - `grep -c "physics/" tests/layering.test.ts` returns at least 1 (the new camera-tier import rule)
    - `grep -c "sim-clock" tests/layering.test.ts` returns at least 1 (the new tick-constant rule)
    - `grep -c "src/audio" tests/layering.test.ts` returns at least 1 (the forward-reference comment for plan 03-11)
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>A future edit that imports the physics tier into the camera rig, or expresses camera smoothing in ticks, or adds a `three` import to the Node-tested camera maths, fails CI rather than review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → composition root | Three untrusted blobs (`heat-street.tuning.v1`, `heat-street.surface-tuning.v1`, `heat-street.camera-tuning.v1`) are read at boot |
| `?debug` query string → dev controls | Whether any tuning/telemetry/skin/rig control exists at all is decided here |
| Physics tier → render tier | The composition root is where a layering violation is easiest to introduce and hardest to see — `src/main.ts`'s own header says so |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Three `localStorage` blobs read at boot | mitigate | Each goes through its own `parseSavedX` clamp-and-validate function; an acceptance grep asserts zero raw `JSON.parse` on a non-comment line in `src/main.ts`. Three separate keys mean a corrupt camera blob cannot take vehicle tuning down with it |
| T-02-03 / T-03-04 | Elevation of Privilege | Player-facing exposure of tuning, telemetry, rig-swap or skin controls, which would let a player alter handling and break medal-time comparability | mitigate | Every new control follows the established `const x = DEBUG_ENABLED ? createX() : null` + `onDebugKey` shape; the panel modules stay gate-free and an acceptance grep asserts `DEBUG_ENABLED` appears zero times inside them. A normal build registers zero listeners for `KeyC`/`KeyV` |
| T-03-20 | Tampering (architectural integrity) | Camera code reaching into the physics tier, coupling the rig to the player vehicle and breaking D-13's Phase 7/8 reuse | mitigate | New mechanical rule in `tests/layering.test.ts` scanning `src/render/camera/**` for any `src/physics/` import |
| T-03-21 | Tampering (simulation integrity) | Camera smoothing expressed in ticks, or camera state computed inside the fixed-tick path, coupling render cadence to simulation | mitigate | New mechanical rule banning `DT`/`sim-clock` under `src/render/camera/**`, plus the documented render-callback ordering in `src/main.ts` |
| T-03-22 | Denial of Service (test coverage) | A `three` import creeping into `camera-math.ts`/`occlusion.ts` and making three Wave-0 test files unrunnable under `environment: "node"` | mitigate | New path-scoped mechanical rule in `tests/layering.test.ts` |
| T-03-13 | Tampering | DOM injection through the camera chrome overlay | mitigate | `textContent` only; the repo-wide `innerHTML` ban is already asserted at zero by `tests/layering.test.ts` |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `lil-gui` continues to come from `three/addons/libs/lil-gui.module.min.js` per the plan 02-09 precedent |
</threat_model>

<verification>
1. `npm run check` exits 0 (typecheck + lint + full suite).
2. `npm run build` exits 0 — the production bundle compiles with the new composition root.
3. `npx vitest run tests/layering.test.ts` passes with strictly more tests than before.
4. `npx vitest run tests/determinism.test.ts tests/vehicle-scene.test.ts tests/loop.test.ts` passes unchanged — every Phase 1/2 fixture survives the composition-root swap.
</verification>

<success_criteria>
- The default page load is the six-surface scene through the helicopter camera, with the skin applied.
- Grip differences are driveable, not just measurable.
- CAM-02's speed response and CAM-03's two skins are both observable in the browser.
- D-12's chase fallback and D-14's skin preview are both one `?debug` keypress away.
- Everything the plan 03-08 playtest needs to retune and re-measure mid-session exists.
</success_criteria>

<output>
Create `.planning/phases/03-surfaces-helicopter-camera/03-07-SUMMARY.md` when done.
</output>
