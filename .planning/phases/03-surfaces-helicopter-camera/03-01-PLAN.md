---
phase: 03-surfaces-helicopter-camera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - src/core/tuning-utils.ts
  - src/core/vehicle-tuning.ts
  - src/core/surface-types.ts
  - src/core/surface-tuning.ts
  - tests/surface-tuning.test.ts
autonomous: true
requirements: [SURF-01]

must_haves:
  truths:
    - "`npm run lint` exits 0 on a freshly-checked-out Windows worktree, so `npm run check` is a single trustworthy green/red signal again"
    - "Per-surface grip multipliers exist as a clamped, hostile-blob-proof, persistable tuning object"
    - "The six surface names used by game code are provably identical to docs/schemas/road-graph.v1.md's normative SURFACE_ENUM"
    - "There is exactly ONE clamp/parse code path in the repo, shared by vehicle tuning and surface tuning"
  artifacts:
    - path: ".gitattributes"
      provides: "Repo-wide LF normalisation + binary attributes for the audio assets plan 03-10 commits"
      contains: "text=auto eol=lf"
    - path: "src/core/tuning-utils.ts"
      provides: "Generic TuningRange/isPlainObject/isTuningRange/clampNode/copyLeaves extracted from vehicle-tuning.ts"
      exports: ["TuningRange", "isPlainObject", "isTuningRange", "clampNode", "copyLeaves"]
    - path: "src/core/surface-types.ts"
      provides: "SURFACE_TYPES tuple + SurfaceType union, the single spelling source for the six surfaces"
      exports: ["SURFACE_TYPES", "SurfaceType"]
    - path: "src/core/surface-tuning.ts"
      provides: "SurfaceProfiles defaults/ranges/clamp/parse/serialize + storage key"
      exports: ["SurfaceProfiles", "SurfaceProfile", "defaultSurfaceProfiles", "SURFACE_PROFILE_RANGES", "clampSurfaceProfiles", "parseSavedSurfaceProfiles", "serializeSurfaceProfiles", "SURFACE_TUNING_STORAGE_KEY"]
    - path: "tests/surface-tuning.test.ts"
      provides: "Doc-parity gate + ASVS V5 hostile-blob suite for surface tuning"
  key_links:
    - from: "src/core/vehicle-tuning.ts"
      to: "src/core/tuning-utils.ts"
      via: "import of clampNode/copyLeaves/isTuningRange/isPlainObject/TuningRange"
      pattern: "from \"./tuning-utils\""
    - from: "src/core/surface-tuning.ts"
      to: "src/core/tuning-utils.ts"
      via: "import of the same generic clamp machinery — never a second hand-rolled pipeline"
      pattern: "from \"./tuning-utils\""
    - from: "tests/surface-tuning.test.ts"
      to: "docs/schemas/road-graph.v1.md"
      via: "SURFACE_ENUM parsed from the doc and compared to SURFACE_TYPES"
      pattern: "SURFACE_ENUM"
---

<objective>
Lay the two foundations every other Phase 3 plan depends on: a trustworthy
`npm run check` signal, and a single validated tuning pipeline that now serves
surface grip multipliers as well as vehicle tuning.

Purpose: 03-RESEARCH.md's "Don't Hand-Roll" section is explicit that a second
clamp/validate/persist boundary for surface and camera tuning is the wrong move —
one ASVS V5 input-validation code path for the whole project, not three that can
silently drift. And `.planning/STATE.md`'s Phase 2 blocker note says the repo-wide
CRLF lint failure is "worth resolving before Phase 3 so `npm run check` is
trustworthy again as a single green/red signal" — every task in this phase uses
that command as its acceptance gate, so it is fixed first.

Output: `.gitattributes`, `src/core/tuning-utils.ts`, `src/core/surface-types.ts`,
`src/core/surface-tuning.ts`, `tests/surface-tuning.test.ts`, plus a rewired
`src/core/vehicle-tuning.ts`.
</objective>

## Phase Goal

**Note for the executor:** `.planning/ROADMAP.md`'s Phase 3 `**Goal:**` line is
outcome-shaped but is not in the three-slot user-story form MVP mode expects
("As a … I want to … so that …"). It is reproduced verbatim rather than invented:

> The world under the tires changes how the car drives, and the game is viewed
> through its signature camera without losing the sense of speed

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-surfaces-helicopter-camera/03-CONTEXT.md
@.planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md
@.planning/phases/03-surfaces-helicopter-camera/03-PATTERNS.md
@.planning/phases/03-surfaces-helicopter-camera/03-VALIDATION.md
@.planning/phases/02-vehicle-feel-core/deferred-items.md
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the existing codebase; no exploration required. -->

Already in `src/core/vehicle-tuning.ts` (to be MOVED, not re-derived):

- `export interface TuningRange { readonly min: number; readonly max: number; readonly step: number; }` (line 32)
- `function isPlainObject(v: unknown): v is Record<string, unknown>` (line 450)
- `function isTuningRange(v: unknown): v is TuningRange` (line 455)
- `function clampNode(valueNode: Record<string, unknown>, rangeNode: Record<string, unknown>, fallbackNode: Record<string, unknown>): void` (line 474)
- `function copyLeaves(parsedNode: unknown, targetNode: Record<string, unknown>, rangeNode: Record<string, unknown>): void` (line 532)

Staying in `src/core/vehicle-tuning.ts` (callers of the above):

- `export function clampTuning(t: VehicleTuning): VehicleTuning` (line 505)
- `export function parseSavedTuning(raw: string | null): VehicleTuning | null` (line 581)
- `export function serializeTuning(t: VehicleTuning): string` (line 627)
- `export const TUNING_STORAGE_KEY = "heat-street.tuning.v1"` (line 520)
- `const REQUIRED_GROUP_KEYS = ["chassis", "wheels", "drive", "assists"] as const` (line 558)

Existing doc-parsing idiom to reuse verbatim, from `tests/road-graph-schema.test.ts:98-109`:
`surfaceEnumFromDoc(doc)` matches `/^SURFACE_ENUM\s*=\s*(.+)$/m`, splits on `|`, trims, drops empties.
`docs/schemas/road-graph.v1.md` carries the normative line
`SURFACE_ENUM = tarmac | gravel | dirt_road | grass | sand | mud`.

Repo-file reading under Vitest uses Vite's `?raw` glob/import with an INLINE query literal
(`@types/node` is NOT installed; `node:fs` is unavailable) — see `tests/layering.test.ts:18-22`
and `tests/road-graph-schema.test.ts`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide LF normalisation so npm run check is a single trustworthy signal</name>
  <files>.gitattributes</files>
  <read_first>
    - .planning/phases/02-vehicle-feel-core/deferred-items.md (the full diagnosis and both candidate fix paths)
    - biome.json (confirms there is no `formatter.lineEnding` override today)
    - package.json (the `check` / `lint` / `test` / `typecheck` script definitions)
  </read_first>
  <action>
    Create a repo-root `.gitattributes` implementing the first of the two fix paths
    recorded in `deferred-items.md` (`* text=auto eol=lf` + renormalize), chosen over
    the `biome.json` `formatter.lineEnding: "crlf"` alternative because biome's default
    is `lf` and the committed git blobs are already LF — matching the repo to its own
    committed content is the smaller, non-Windows-specific change.

    Contents: a `* text=auto eol=lf` catch-all line, then explicit `binary` attributes
    for the asset extensions later plans commit — `*.glb`, `*.wasm`, `*.png`, `*.jpg`,
    `*.wav`, `*.ogg`, `*.mp3` — so plan 03-10's downloaded CC0 audio files and any future
    glTF model are never line-ending-mangled. Include a short header comment naming
    `deferred-items.md` as the origin of this file.

    Then, in this order: run `npx biome check --write .` to rewrite every tracked file
    on disk with LF endings, run `git add --renormalize .`, and commit this
    normalisation as its OWN commit before any other change in this plan lands —
    a renormalisation diff mixed with real code changes is unreviewable.

    Do not change `biome.json`. Do not change any source file's actual content: this
    task changes line endings and nothing else.
  </action>
  <verify>
    <automated>npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `.gitattributes` exists at the repo root and contains the literal string `text=auto eol=lf`
    - `.gitattributes` contains a `binary` attribute line for each of `*.wav`, `*.ogg`, `*.mp3`, `*.glb`, `*.png`
    - `npm run lint` (i.e. `biome check .`) exits 0 with zero reported errors
    - `npm run test` still reports every pre-existing test passing — the count must not drop
    - `git diff --stat HEAD~1` for the normalisation commit shows zero net insertions/deletions of non-whitespace content (line-ending-only changes)
  </acceptance_criteria>
  <done>`npm run check` runs typecheck, lint and tests and exits 0 end to end on this worktree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the generic clamp/copy machinery into src/core/tuning-utils.ts</name>
  <files>src/core/tuning-utils.ts, src/core/vehicle-tuning.ts</files>
  <read_first>
    - src/core/vehicle-tuning.ts (the whole file — lines 29-36 for `TuningRange`, 449-494 for `isPlainObject`/`isTuningRange`/`clampNode`, 522-555 for `copyLeaves`, and 496-611 for the three callers that stay behind)
    - tests/vehicle-tuning.test.ts (the existing behavioural gate this refactor must not move)
    - tests/tuning-persist.test.ts (the existing ASVS V5 hostile-blob gate this refactor must not move)
    - .planning/phases/03-surfaces-helicopter-camera/03-PATTERNS.md (section "src/core/tuning-utils.ts (new — factored generic clamp/parse utility)")
  </read_first>
  <behavior>
    This is a pure refactor-extraction. The pre-existing suites are the RED/GREEN gate:
    - `tests/vehicle-tuning.test.ts` and `tests/tuning-persist.test.ts` must pass
      identically before and after, with no test edited, added or removed.
    - No observable behaviour of `clampTuning`, `parseSavedTuning` or `serializeTuning`
      changes: same clamping, same non-finite-to-fallback substitution, same `null`
      returns on malformed input.
  </behavior>
  <action>
    Create `src/core/tuning-utils.ts` and MOVE these five declarations into it verbatim,
    exporting all five: the `TuningRange` interface, `isPlainObject`, `isTuningRange`,
    `clampNode`, `copyLeaves`. Carry their existing doc comments across unchanged — those
    comments are the ASVS V5 rationale and must not be summarised away.

    Give the new file the `src/core/` layering header used by `vehicle-tuning.ts` lines
    23-27: may import nothing from `three` or `@dimforge/rapier3d`, must not touch the DOM
    or any wall clock. `tests/layering.test.ts` enforces this mechanically.

    Then edit `src/core/vehicle-tuning.ts` to import those five from `./tuning-utils`
    instead of declaring them. `TuningRange` must continue to be re-exported from
    `vehicle-tuning.ts` (`export type { TuningRange } from "./tuning-utils";` or an
    equivalent re-export) because `src/debug/tuning-panel.ts` imports it from there today —
    do not force a change in `tuning-panel.ts` as part of this task.

    Everything else in `vehicle-tuning.ts` stays exactly where it is: `VehicleTuning`,
    `defaultTuning`, `TUNING_RANGES`, `clampTuning`, `TUNING_STORAGE_KEY`,
    `REQUIRED_GROUP_KEYS`, `parseSavedTuning`, `serializeTuning`.
  </action>
  <verify>
    <automated>npx vitest run tests/vehicle-tuning.test.ts tests/tuning-persist.test.ts tests/layering.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/tuning-utils.ts` exists and `grep -c "^export" src/core/tuning-utils.ts` returns at least 5
    - `grep -n "from \"./tuning-utils\"" src/core/vehicle-tuning.ts` returns at least one line
    - `grep -v '^\s*[*/]' src/core/vehicle-tuning.ts | grep -c "^function clampNode\|^function copyLeaves\|^function isTuningRange\|^function isPlainObject"` returns 0 — the definitions moved, they were not copied
    - `npx vitest run tests/vehicle-tuning.test.ts tests/tuning-persist.test.ts` passes with the same test count as before the change
    - `git diff --stat tests/` shows no change to any test file in this task
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>One clamp/copy implementation exists in the repo, in `src/core/tuning-utils.ts`, and every pre-existing vehicle-tuning test passes unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface name contract and the SurfaceProfiles tuning object</name>
  <files>src/core/surface-types.ts, src/core/surface-tuning.ts, tests/surface-tuning.test.ts</files>
  <read_first>
    - src/core/vehicle-tuning.ts (post-Task-2 state — the `defaultTuning`/`TUNING_RANGES`/`clampTuning`/`parseSavedTuning`/`serializeTuning` shape being mirrored, and the per-field `[MEASURED]`/`[ASSUMED]` doc-comment convention at lines 93-102 and 415-446)
    - src/core/tuning-utils.ts (the five helpers this file must delegate to)
    - tests/tuning-persist.test.ts (the hostile-blob case list to mirror for the new key)
    - tests/road-graph-schema.test.ts (lines 90-115 — the `surfaceEnumFromDoc` parser and the `?raw` import idiom to reuse)
    - docs/schemas/road-graph.v1.md (the normative `SURFACE_ENUM` line)
    - .planning/phases/03-surfaces-helicopter-camera/03-RESEARCH.md (section "Surface Grip Ranking" — the multiplier table and its provenance tags are copied verbatim into doc comments)
  </read_first>
  <behavior>
    Write `tests/surface-tuning.test.ts` FIRST; it must fail before the modules exist.
    - Doc parity: the array parsed out of `docs/schemas/road-graph.v1.md`'s `SURFACE_ENUM`
      line equals `SURFACE_TYPES` exactly — same six values, same order, including
      `dirt_road` (NOT `dirt`).
    - Key parity: `Object.keys(defaultSurfaceProfiles())` equals `SURFACE_TYPES`, and
      `Object.keys(SURFACE_PROFILE_RANGES)` equals `SURFACE_TYPES`.
    - Tarmac is the untouched Phase 2 baseline: `defaultSurfaceProfiles().tarmac` is
      `{ forwardGrip: 1, lateralGrip: 1 }` exactly.
    - Ordering: every non-tarmac surface has `forwardGrip < 1` and `lateralGrip < 1`.
    - D-04 floor discipline: every `forwardGrip` and `lateralGrip` default is `>= 0.4`.
    - D-05 anchor: `gravel.lateralGrip` and `dirt_road.lateralGrip` are each strictly
      lower than `grass.lateralGrip`, `sand.lateralGrip` and `mud.lateralGrip` is not
      the intent — assert instead that `gravel.lateralGrip < gravel.forwardGrip` and
      `dirt_road.lateralGrip < dirt_road.forwardGrip` by at least 0.15, i.e. lateral grip
      is cut harder than forward grip on exactly the two Dukes-of-Hazzard surfaces.
    - Clamp: `clampSurfaceProfiles` replaces `NaN`, `Infinity`, `-Infinity`, `null`,
      a string, and a missing key with the matching `defaultSurfaceProfiles()` value,
      and clamps an out-of-range finite number to the `SURFACE_PROFILE_RANGES` bound.
    - Parse: `parseSavedSurfaceProfiles` returns `null` for `null` input, for
      non-JSON text, for a JSON array, for a JSON scalar, and for an object missing
      any of the six surface keys; it never throws for any string input.
    - Round trip: `parseSavedSurfaceProfiles(serializeSurfaceProfiles(defaultSurfaceProfiles()))`
      deep-equals `defaultSurfaceProfiles()`.
    - A parsed blob whose `mud.forwardGrip` is `1e9` comes back clamped to the range max,
      never `1e9` — the value that would otherwise reach `setWheelFrictionSlip` (T-03-02).
  </behavior>
  <action>
    Create `src/core/surface-types.ts`: export `SURFACE_TYPES` as a
    `readonly ["tarmac", "gravel", "dirt_road", "grass", "sand", "mud"]` tuple (use
    `as const`) and `export type SurfaceType = (typeof SURFACE_TYPES)[number]`. Its doc
    comment must state that this order and spelling are NORMATIVE, sourced from
    `docs/schemas/road-graph.v1.md`'s `SURFACE_ENUM`, that `dirt_road` is deliberately not
    `dirt` (CONTEXT.md and REQUIREMENTS.md's prose shorthand "dirt" means this value), and
    that `tests/surface-tuning.test.ts` parses the doc and asserts parity — so the doc is
    the source and this file is the mirror, never the reverse.

    DEVIATION from 03-PATTERNS.md, state it in the file's doc comment: `SurfaceType` lives
    in `src/core/`, not in `src/physics/surface.ts` as that document assigns. Reason:
    `src/core/surface-tuning.ts`, `src/render/surface-fx.ts` and `src/audio/surface-audio.ts`
    all need the type, and `src/core/` must not import from `src/physics/` (which imports
    `@dimforge/rapier3d`). `src/physics/surface.ts` (plan 03-03) imports the type from here.

    Create `src/core/surface-tuning.ts` mirroring `vehicle-tuning.ts`'s exact structure:
    - `export interface SurfaceProfile { forwardGrip: number; lateralGrip: number }`
    - `export type SurfaceProfiles = { [K in SurfaceType]: SurfaceProfile }`
    - `export function defaultSurfaceProfiles(): SurfaceProfiles` returning a FRESH object
      each call, carrying 03-RESEARCH.md's "Surface Grip Ranking" table verbatim:
      tarmac 1.00/1.00, gravel 0.75/0.55, dirt_road 0.78/0.55, grass 0.55/0.60,
      sand 0.45/0.55, mud 0.40/0.50. Each entry gets a doc comment carrying that table's
      own provenance tag and rationale — `[CITED: hpwizard.com/tire-friction-coefficient.html]`
      for tarmac/gravel/dirt_road, `[ASSUMED]` for grass/sand/mud — plus the "retune in
      plan 03-11's feel session, exactly like rearSideFriction/powerOversteerGain/
      bodyRollGain were in Phase 2's plan 02-10" note.
    - `export const SURFACE_PROFILE_RANGES` mirroring `SurfaceProfiles`' shape, every leaf a
      `TuningRange`. Use `{ min: 0.2, max: 1.2, step: 0.01 }` for every leaf: the floor is
      deliberately 0.2 rather than 0 because D-04 rules out surfaces that can bog the car
      down, and `handbrakeRearSideFriction`'s own doc comment in `vehicle-tuning.ts` already
      warns what a near-zero side-friction value does; the ceiling is above 1.0 so a feel
      session can try a grippier-than-tarmac surface without a code edit.
    - `export function clampSurfaceProfiles(p: SurfaceProfiles): SurfaceProfiles` delegating
      to `clampNode` from `./tuning-utils`, with `defaultSurfaceProfiles()` as the fallback
      tree — same body shape as `clampTuning`.
    - `export function parseSavedSurfaceProfiles(raw: string | null): SurfaceProfiles | null`
      mirroring `parseSavedTuning` exactly: null-guard, try/catch `JSON.parse` that never
      rethrows, `isPlainObject` guard, a required-keys guard over `SURFACE_TYPES` (the
      analogue of `REQUIRED_GROUP_KEYS`), then `copyLeaves` onto a fresh
      `defaultSurfaceProfiles()`, then `clampSurfaceProfiles`.
    - `export function serializeSurfaceProfiles(p: SurfaceProfiles): string` — `JSON.stringify`.
    - `export const SURFACE_TUNING_STORAGE_KEY = "heat-street.surface-tuning.v1"` — its own
      key, never merged into `TUNING_STORAGE_KEY`'s blob.

    Do NOT re-implement `clampNode`/`copyLeaves`/`isPlainObject`/`isTuningRange` here.
  </action>
  <verify>
    <automated>npx vitest run tests/surface-tuning.test.ts tests/layering.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/surface-tuning.test.ts` passes with at least 12 assertions covering every bullet in `<behavior>`
    - `grep -c "dirt_road" src/core/surface-types.ts` returns at least 1 and `grep -c '"dirt"' src/core/surface-types.ts` returns 0
    - `grep -n "from \"./tuning-utils\"" src/core/surface-tuning.ts` returns at least one line
    - `grep -v '^\s*[*/]' src/core/surface-tuning.ts | grep -c "function clampNode\|function copyLeaves"` returns 0
    - `grep -c "heat-street.surface-tuning.v1" src/core/surface-tuning.ts` returns 1, and `grep -c "heat-street.tuning.v1" src/core/surface-tuning.ts` returns 0
    - `npx vitest run tests/layering.test.ts` passes — `src/core/surface-types.ts` and `src/core/surface-tuning.ts` import neither `three` nor `@dimforge/rapier3d` and touch no DOM/wall clock
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>A hostile `localStorage` string under `heat-street.surface-tuning.v1` can never produce a non-finite or out-of-range grip multiplier, and the six surface names in code are proven identical to the schema doc's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → `src/core/surface-tuning.ts` | A user- and devtools-editable string blob that survives across sessions crosses into typed game state here |
| `src/core/surface-tuning.ts` → `src/physics/vehicle.ts` Rapier setters | Numbers leaving this module reach `setWheelFrictionSlip` / `setWheelSideFrictionStiffness` on the next fixed tick (plan 03-03) |
| `docs/schemas/road-graph.v1.md` → `src/core/surface-types.ts` | A normative spec doc and its code mirror can drift silently; the test is the only link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `parseSavedSurfaceProfiles` reading `heat-street.surface-tuning.v1` | mitigate | Mirror `parseSavedTuning` exactly: try/catch `JSON.parse` that never rethrows, `isPlainObject` guard, required-key guard over `SURFACE_TYPES`, `copyLeaves` onto a fresh default, then `clampSurfaceProfiles`. Asserted by `tests/surface-tuning.test.ts`'s hostile-blob cases |
| T-03-02 | Tampering / Denial of Service | A non-finite multiplier reaching `setWheelFrictionSlip` | mitigate | `clampNode`'s existing non-finite-to-fallback substitution (not clamp-to-min) is reused unchanged; a `NaN` friction value would corrupt every body in `world.step()`, not just this vehicle — the same reasoning `vehicle-tuning.ts`'s own doc comment already records |
| T-03-03 | Tampering | Duplicated clamp logic drifting between vehicle and surface tuning | mitigate | Single extracted `src/core/tuning-utils.ts`; acceptance greps assert zero re-declaration of `clampNode`/`copyLeaves` in either caller |
| T-03-04 | Spoofing (data-integrity) | `SURFACE_TYPES` silently diverging from `docs/schemas/road-graph.v1.md`, so Phase 4's compiled `.map.json` emits a surface name no runtime profile exists for | mitigate | `tests/surface-tuning.test.ts` parses the doc's `SURFACE_ENUM` line with `tests/road-graph-schema.test.ts`'s existing `surfaceEnumFromDoc` idiom and asserts exact array equality |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 03-RESEARCH.md's Package Legitimacy Audit records that this phase installs ZERO new packages — every capability is served by the already-pinned `three@0.185.1` / `@dimforge/rapier3d@0.20.0` or a bundled `three/addons` module. No install task exists in this plan, so the gate has nothing to audit. Any plan that later proposes an install must re-run the Package Legitimacy Gate protocol first |
</threat_model>

<verification>
1. `npm run check` exits 0 (typecheck + lint + full Vitest suite).
2. `npx vitest run tests/vehicle-tuning.test.ts tests/tuning-persist.test.ts` passes with an unchanged test count — the extraction moved no behaviour.
3. `npx vitest run tests/surface-tuning.test.ts` passes.
4. `npx vitest run tests/layering.test.ts` passes with the two new `src/core/` files included in its glob.
</verification>

<success_criteria>
- `npm run lint` exits 0 on this Windows worktree; the Phase 2 CRLF blocker in `.planning/STATE.md` is closed.
- Exactly one `clampNode`/`copyLeaves` implementation exists under `src/`.
- `SURFACE_TYPES` is provably equal to `docs/schemas/road-graph.v1.md`'s `SURFACE_ENUM`.
- `defaultSurfaceProfiles().tarmac` is `{ forwardGrip: 1, lateralGrip: 1 }`, so tarmac driving is unchanged from Phase 2 by construction.
</success_criteria>

<output>
Create `.planning/phases/03-surfaces-helicopter-camera/03-01-SUMMARY.md` when done.
</output>
