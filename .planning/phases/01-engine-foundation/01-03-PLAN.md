---
phase: 01-engine-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - docs/adr/0001-map-data-source.md
  - docs/schemas/road-graph.v1.md
  - fixtures/road-graph.sample.json
  - LICENSE-MAPDATA
  - tests/road-graph-schema.test.ts
  - tests/docs-present.test.ts
  - tests/no-google-pipeline.test.ts
  - heat-street-design-doc.md
  - CLAUDE.md
  - .planning/research/STACK.md
autonomous: true
# No numbered REQUIREMENTS.md id maps to this plan. ROADMAP Phase 1 maps only VEH-03, which
# plans 01-01/01-02/01-04/01-05/01-06/01-07 cover. This plan satisfies phase success criterion
# SC5, which REQUIREMENTS.md itself frames as enabling infrastructure for Phase 4 rather than a
# numbered requirement. `requirements` is therefore intentionally empty so that any tool parsing
# it as a list of REQ-IDs sees no fabricated id. SC coverage is recorded separately below.
requirements: []
success_criteria_covered: [SC5]   # phase success criteria, NOT REQUIREMENTS.md ids - do not parse as REQ-IDs

must_haves:
  truths:
    - "The repo states in one authoritative place that map data comes from OpenStreetMap plus an open DEM and that zero Google-sourced bytes may enter the pipeline"
    - "Every document that previously recommended the Google Maps extraction pipeline now points at the ADR that supersedes it"
    - "The road-graph schema a Phase 4 compiler must emit is written down, with a machine-checkable sample fixture"
    - "An automated test fails if a future edit reintroduces a Google Maps pipeline recommendation without a supersession pointer"
  artifacts:
    - path: "docs/adr/0001-map-data-source.md"
      provides: "The frozen P0 legal decision, licence citations, attribution strings and the supersession list"
      contains: "OpenStreetMap"
      min_lines: 60
    - path: "docs/schemas/road-graph.v1.md"
      provides: "Road graph v1 contract consumed by the Phase 4 compiler and by NAV/P2P/CIRC/GET/SURF requirements"
      contains: "schemaVersion"
      min_lines: 80
    - path: "fixtures/road-graph.sample.json"
      provides: "A 4-node square loop with one gravel edge that the schema test validates"
      contains: "gravel"
    - path: "LICENSE-MAPDATA"
      provides: "ODbL 1.0 coverage for compiled *.map.json artifacts - the free safe path on the open Derivative Database question"
      contains: "ODbL"
    - path: "tests/no-google-pipeline.test.ts"
      provides: "Grep gate with an explicitly stated file scope, closing PATTERNS.md risk R1"
  key_links:
    - from: "heat-street-design-doc.md"
      to: "docs/adr/0001-map-data-source.md"
      via: "inline supersession pointer in section 5"
      pattern: "docs/adr/0001-map-data-source.md"
    - from: "CLAUDE.md"
      to: "docs/adr/0001-map-data-source.md"
      via: "inline supersession pointer on the minimap and city-scale lines"
      pattern: "docs/adr/0001-map-data-source.md"
    - from: "tests/road-graph-schema.test.ts"
      to: "fixtures/road-graph.sample.json"
      via: "reads the fixture and asserts it satisfies every required field in the v1 schema"
      pattern: "road-graph.sample.json"
    - from: "tests/docs-present.test.ts"
      to: "docs/frame-budget.md"
      via: "cross-plan presence assertion on a file created by plan 01-02"
      pattern: "docs/frame-budget.md"
---

<objective>
Freeze the map-data sourcing decision and the road-graph schema in the repository, retire every contradicting document, and make both facts automatically enforceable.

Purpose: ROADMAP Phase 1 SC5 requires this recorded before any map work starts. RESEARCH.md "Pitfall 6" is blunt about why an ADR alone is insufficient: three documents currently give two contradictory answers, and a future agent reading `heat-street-design-doc.md` section 5 will build Phase 4 against Google-sourced data, violating the P0 legal decision in STATE.md and failing Phase 4 SC4. SC5 is satisfied only when the ADR SUPERSEDES the contradicting text and a test keeps it superseded.

This plan carries no numbered REQUIREMENTS.md id because ROADMAP Phase 1 maps only VEH-03, which the timing plans cover. SC5 is a phase success criterion with no numbered requirement of its own - it is enabling infrastructure for Phase 4's NAV/P2P/CIRC/GET/SURF work. The `requirements` frontmatter field is deliberately `[]` rather than carrying an invented id; `success_criteria_covered: [SC5]` records the real mapping in a field no REQ-ID parser reads.

Output: an ADR, a schema doc, a sample fixture, a map-data licence file, three tests, and three amended documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-foundation/01-RESEARCH.md
@.planning/phases/01-engine-foundation/01-PATTERNS.md

<wave_placement>
This plan is wave 3, after 01-02, even though its own content depends only on 01-01.

Reason: Task 3 creates `tests/docs-present.test.ts`, which asserts `docs/frame-budget.md`
exists. That file is created exclusively by plan 01-02. `.planning/config.json` sets
`parallelization: true`, so two plans in the same wave with no `depends_on` edge between them
may execute concurrently in isolated contexts - and this plan's Task 3 verify command would
fail if it ran before or alongside 01-02.

The fix is the dependency edge, NOT a local stub. Do not create, touch, or placeholder
`docs/frame-budget.md` in this plan - 01-02 owns that file, and writing it here would produce a
two-plan write conflict on a single path. If `docs/frame-budget.md` is missing when Task 3 runs,
that is a real ordering failure to report, not something to paper over.
</wave_placement>

<grep_scope_decision>
PATTERNS.md risk R1 flagged that Google-Maps-pipeline language exists in more files
than RESEARCH.md named, and required the planner to pick a scope rather than leave
it to the implementer. DECISION: option (a), with one addition.

tests/no-google-pipeline.test.ts scans exactly this set:
  IN SCOPE   CLAUDE.md
             heat-street-design-doc.md
             PROJECT.md            (repo root - verified to contain no Google reference)
             README.md             (if present)
             docs/**/*.md
             src/**/*.ts
             tests/**/*.ts
  ALLOWLIST  docs/adr/0001-map-data-source.md   (the ADR IS the authority; it names
                                                 Google in order to prohibit it)
             tests/no-google-pipeline.test.ts   (the test names the pattern it greps for)
  OUT OF SCOPE
             .planning/**   - frozen planning and research record, deliberately historical
             node_modules/**, dist/**, .git/**

Rationale for excluding .planning/: those files are a dated audit trail, not
instructions to a builder. Option (b) - a repo-wide grep with a maintained
allowlist - would require amending six more historical documents and keeping the
allowlist in sync forever, for no additional protection of the shipped pipeline.

Compensating control: this plan still adds a single superseded banner to
.planning/research/STACK.md (the one .planning/ file whose stale lines are most
likely to be read as build guidance, per PATTERNS.md R1), which covers its four
stale references at once without line-by-line edits.

Note also: PATTERNS.md R1 correctly observed that .planning/PROJECT.md line 75
mentions Google while the ROOT PROJECT.md does not - RESEARCH.md conflated the two.
Root PROJECT.md is in scope and clean; .planning/PROJECT.md is out of scope.
</grep_scope_decision>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the map-data ADR and retire every contradicting document</name>
  <files>docs/adr/0001-map-data-source.md, LICENSE-MAPDATA, heat-street-design-doc.md, CLAUDE.md, .planning/research/STACK.md</files>
  <read_first>
    - .planning/phases/01-engine-foundation/01-RESEARCH.md - "Map Data Decision" (the complete ADR body with licence citations, attribution strings, the Google ToS prohibition, the supersession list, and the open ODbL question)
    - .planning/phases/01-engine-foundation/01-RESEARCH.md - "Pitfall 6" (why adding an ADR without superseding is insufficient) and "Security Domain" (the version-pinning note that belongs in this ADR)
    - .planning/STATE.md - the locked P0 decision this ADR records
    - heat-street-design-doc.md lines 50-58 - the exact text being superseded
    - CLAUDE.md line 112 (minimap bullet) and line 198 (Google-Maps-derived city scale) - the exact lines being amended
    - .planning/research/STACK.md - locate its stale references before adding the banner
  </read_first>
  <action>
    Create `docs/adr/0001-map-data-source.md` using standard ADR structure: Status (Accepted, dated 2026-09-08), Context, Decision, Consequences, Supersedes, Open Questions.

    Decision content, sourced from RESEARCH.md "Map Data Decision":
    - Road network comes from OpenStreetMap under ODbL v1.0. Required attribution is to "OpenStreetMap"; the forms "(c) OpenStreetMap contributors" and "(c) OpenStreetMap" are explicitly acceptable. For games the OSMF guidelines permit attribution on the credits page, in the menu, or another suitable location, and require the URL https://www.openstreetmap.org/copyright where linking is not possible. Cite openstreetmap.org/copyright and osmfoundation.org/wiki/Licence/Attribution_Guidelines.
    - Elevation comes from an open DEM. Record BOTH approved sources with an explicit selection rule rather than blocking on which real-world area Phase 4 targets: USGS 3DEP at 1 m or 1/3 arc-second is public domain with no mandatory notice and is preferred when the area is inside the United States; Copernicus DEM GLO-30 at 30 m is the global fallback and carries a MANDATORY notice, which must be reproduced verbatim in the ADR - "(c) DLR e.V. 2010-2014 and (c) Airbus Defence and Space GmbH 2014-2018 provided under COPERNICUS by the European Union and ESA; all rights reserved", plus the modified-data form "produced using Copernicus WorldDEM-30 (c) DLR e.V. ...". Cite the USGS national-map terms FAQ and the Copernicus License-COPDEM-30 PDF. This selection rule is the recorded resolution of RESEARCH.md Open Question 2.
    - State the prohibition explicitly: no bytes derived from Google Maps, Google Earth or Street View may enter the repository, the asset pipeline, or the shipped build - including road geometry traced from Google imagery. Link https://cloud.google.com/maps-platform/terms and reference section 3.2.4 as directional rather than paraphrasing it as verbatim legal fact; RESEARCH.md could not retrieve the clause text verbatim. Add a "not legal advice" line.
    - Add a Consequences bullet carrying RESEARCH.md's supply-chain note: `three` and `@dimforge/rapier3d` are pinned to exact versions because a patch bump can alter solver behaviour, and bumping either requires re-recording the Phase 6 designer reference runs.
    - Open Questions section records, without resolving: whether a compiled `.map.json` road graph constitutes an ODbL Derivative Database (the `.glb` is almost certainly a Produced Work), and which real-world area Phase 4 targets, since that determines 3DEP versus Copernicus.

    Supersedes section must name each retired location explicitly, by file and section:
    1. `heat-street-design-doc.md` section 5 "Maps & Locations" - the "Leroy has already built a separate tool that takes an area of Google Maps ... This is the preferred pipeline" bullet. SUPERSEDED.
    2. `CLAUDE.md` minimap bullet - "the road polylines you already have from the Google Maps extraction tool". SUPERSEDED; polylines come from the OSM-derived road graph.
    3. `CLAUDE.md` Gaps/Open Items - "Google-Maps-derived city scale" as a scale descriptor. SUPERSEDED; the descriptor is now OSM-derived.
    4. `.planning/research/STACK.md` - all references to the Google Maps extraction tool as a map source. SUPERSEDED wholesale by this ADR.

    Create `LICENSE-MAPDATA` at the repo root. Take RESEARCH.md's recommended free safe path on the open Derivative Database question - this file is the recorded resolution of RESEARCH.md Open Question 1. State that all compiled `*.map.json` road-graph artifacts distributed with this project are licensed under the Open Database License (ODbL) v1.0, include the licence URL https://opendatacommons.org/licenses/odbl/1-0/, and note that the compiled `.glb` geometry is treated as an ODbL Produced Work requiring attribution only. Cross-reference `docs/adr/0001-map-data-source.md`.

    Amend `heat-street-design-doc.md` section 5: replace the Google Maps bullet with a bullet stating that map data is compiled offline from OpenStreetMap plus an open DEM, and append an inline pointer containing the literal path `docs/adr/0001-map-data-source.md`. Keep the surrounding bullets (no procedural generation, hand-built fallback, map variety) intact.

    Amend `CLAUDE.md`: on the minimap bullet, replace the Google Maps extraction tool clause with "the road polylines from the OSM-derived road graph (see docs/adr/0001-map-data-source.md)". On the Gaps/Open Items line, change "Google-Maps-derived city scale" to "OSM-derived city scale". CLAUDE.md is agent-facing canon read by every future agent, which is why RESEARCH.md calls this the highest-value five minutes in the phase.

    Amend `.planning/research/STACK.md`: insert a single blockquote banner immediately after its title/frontmatter reading that its map-source references are superseded by `docs/adr/0001-map-data-source.md` and that OpenStreetMap plus an open DEM is the only approved source. Do not edit its individual lines.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const adr=fs.readFileSync('docs/adr/0001-map-data-source.md','utf8');for(const s of ['OpenStreetMap','ODbL','3DEP','Copernicus','Supersede','openstreetmap.org/copyright']){if(!adr.includes(s))throw new Error('ADR missing: '+s)}for(const f of ['heat-street-design-doc.md','CLAUDE.md','.planning/research/STACK.md']){if(!fs.readFileSync(f,'utf8').includes('docs/adr/0001-map-data-source.md'))throw new Error('no supersession pointer in '+f)}if(!fs.readFileSync('LICENSE-MAPDATA','utf8').includes('ODbL'))throw new Error('LICENSE-MAPDATA missing ODbL');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/0001-map-data-source.md` exists, is at least 60 lines, and contains the literal strings `OpenStreetMap`, `ODbL`, `3DEP`, `Copernicus`, `openstreetmap.org/copyright`, `cloud.google.com/maps-platform/terms`
    - The ADR contains the verbatim Copernicus mandatory notice including `DLR e.V.` and `Airbus Defence and Space GmbH`
    - The ADR has a section naming all four superseded locations by file
    - The ADR contains an Open Questions section mentioning both `Derivative Database` and the Phase 4 area selection
    - `heat-street-design-doc.md`, `CLAUDE.md` and `.planning/research/STACK.md` each contain the literal path `docs/adr/0001-map-data-source.md`
    - `heat-street-design-doc.md` no longer contains the phrase `preferred pipeline` in connection with Google Maps
    - `CLAUDE.md` no longer contains `Google Maps extraction tool` and no longer contains `Google-Maps-derived`
    - `LICENSE-MAPDATA` exists and contains `ODbL` and `opendatacommons.org/licenses/odbl/1-0`
  </acceptance_criteria>
  <done>One authoritative ADR records the OSM plus open-DEM decision with citations, and every document that previously recommended the Google Maps pipeline now points at it.</done>
</task>

<task type="auto">
  <name>Task 2: Road graph v1 schema document and sample fixture</name>
  <files>docs/schemas/road-graph.v1.md, fixtures/road-graph.sample.json, tests/road-graph-schema.test.ts</files>
  <read_first>
    - .planning/phases/01-engine-foundation/01-RESEARCH.md - "Road Graph Schema v1" (the complete annotated JSONC document, the six design decisions to record, and the OSM-to-game surface mapping table)
    - .planning/ROADMAP.md - Phase 4 success criteria and the NAV/P2P/CIRC/GET/SURF requirements this schema must serve
    - docs/adr/0001-map-data-source.md (created in Task 1) - the attribution strings the schema's attribution block must be able to carry
  </read_first>
  <action>
    Create `docs/schemas/road-graph.v1.md`. Reproduce RESEARCH.md's "Road Graph Schema v1" annotated document as the normative shape, keeping its inline comments as prose explanations. Required top-level keys: `schemaVersion`, `areaId`, `name`, `source` (with `osmExtract`, `osmSnapshot`, `demSource`, `compilerVersion`), `attribution` (with `osm`, `osmLicense`, `osmLicenseUrl`, `dem`), `origin` (with `lat`, `lon`, `projection`), `bounds` (with `minX`, `minZ`, `maxX`, `maxZ`), `nodes`, `edges`, and optional `spawns`.

    Node fields: `id` (dense compiler-assigned integer), `x`, `y`, `z` (local metres), `junction` (boolean), `osmNodeId` (provenance only).

    Edge fields: `id`, `from`, `to`, `points` (centreline polyline including both endpoints, local metres), `lengthM`, `surface`, `roadClass`, `lanes`, `widthM`, `oneway`, `speedLimitKph`, `bridge`, `tunnel`, `layer`, `osmWayId`.

    `surface` is a CLOSED game enum with exactly these six values: `tarmac`, `gravel`, `dirt_road`, `grass`, `sand`, `mud`. Include RESEARCH.md's OSM-to-game mapping table with all fifteen documented `surface=*` values and the `highway=*` fallbacks, plus the note that the OSM wiki flags `cobblestone` as unclear and recommends handling `sett` and `unhewn_cobblestone` explicitly rather than lumping them into `tarmac`.

    Record all six design decisions verbatim in intent: local ENU metres with a recorded lat/lon origin and Y-up (lat/lon never reaches the runtime, and Y is authored by the compiler from the DEM rather than sampled at runtime); dense compiler-assigned integer node ids rather than sparse 64-bit OSM ids; edges stored undirected with a `oneway` flag so one representation serves collision, minimap and pathfinding; `surface` as a closed enum whose OSM mapping lives in the compiler so an unknown value fails the BUILD rather than defaulting silently at runtime; mandatory `schemaVersion`; mandatory provenance fields.

    Add a Consumers section listing which downstream requirement each field group serves - NAV-03 minimap polylines, NAV-04/05 road-aware pathfinding, P2P-01 and CIRC-01 checkpoint placement, CIRC-02 and GET-01/03 AI pathfinding, SURF-01 surface types carried from source, and Phase 4 SC3's single-source-of-truth requirement.

    Create `fixtures/road-graph.sample.json`: a hand-written 4-node square loop, 100 m per side, with four edges. Exactly one edge has `surface: "gravel"`; the other three are `tarmac`. Every field named in the schema must be present and populated with plausible values. `schemaVersion` is `1`, `areaId` is `sample-square`, `origin.projection` is `local-enu-metres`, `attribution.osmLicense` is `ODbL-1.0`. Include one `spawns` entry. `bounds` must actually contain every node coordinate.

    Create `tests/road-graph-schema.test.ts`. Read the fixture with `node:fs` and `JSON.parse`, then assert structurally with plain TypeScript - no zod, per RESEARCH.md's recommendation that a plain assertion keeps the schema language-agnostic for a future non-TypeScript map compiler. Assertions:
    - every required top-level key is present
    - `schemaVersion` is the number 1
    - `nodes` ids are dense and contiguous from 0
    - every `edge.from` and `edge.to` references an existing node id
    - every `edge.surface` is one of the six enum values
    - exactly one edge has surface `gravel`
    - every `edge.points` has at least two entries and each entry is a 3-number tuple
    - `edge.points` first and last entries match the `from` and `to` node coordinates within 1e-6
    - every node coordinate lies inside `bounds`
    - `attribution.osmLicenseUrl` is the OSM copyright URL
    - the enum list asserted in the test is read from, or cross-checked against, the six values named in `docs/schemas/road-graph.v1.md`, so adding a seventh surface to the doc without updating the test is caught
  </action>
  <verify>
    <automated>npx vitest run tests/road-graph-schema.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `docs/schemas/road-graph.v1.md` exists, is at least 80 lines, and contains `schemaVersion`, `local-enu-metres`, `oneway`, `osmSnapshot`, `compilerVersion`
    - The schema doc names all six surface enum values and contains a mapping table row for `sett`
    - `fixtures/road-graph.sample.json` parses as JSON, has exactly 4 nodes and 4 edges, and contains exactly one `"surface": "gravel"`
    - `npx vitest run tests/road-graph-schema.test.ts` exits 0 with at least 10 assertions
    - Deliberately changing one fixture edge's `surface` to `"asphalt"` makes the test fail - confirm once, then restore
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The Phase 4 compiler contract is written down and a sample artifact proves it is machine-checkable rather than aspirational.</done>
</task>

<task type="auto">
  <name>Task 3: SC5 enforcement tests - docs present and no Google pipeline recommendation</name>
  <files>tests/docs-present.test.ts, tests/no-google-pipeline.test.ts</files>
  <read_first>
    - .planning/phases/01-engine-foundation/01-PATTERNS.md - "Discovered Risks > R1" (the full inventory of files containing Google-Maps-pipeline language and the two scope options)
    - The grep_scope_decision block in this plan's context section - the scope is DECIDED, implement it exactly as written
    - The wave_placement block in this plan's context section - explains why `docs/frame-budget.md` is asserted here but created elsewhere
    - .planning/phases/01-engine-foundation/01-RESEARCH.md - "Pitfall 6" and "Validation Architecture" rows for SC5
    - docs/adr/0001-map-data-source.md and docs/schemas/road-graph.v1.md (created in Tasks 1-2)
  </read_first>
  <action>
    Create `tests/docs-present.test.ts`. Using `node:fs`, assert that each of `docs/adr/0001-map-data-source.md`, `docs/schemas/road-graph.v1.md`, `docs/frame-budget.md`, `fixtures/road-graph.sample.json` and `LICENSE-MAPDATA` exists and is non-empty. Enforce minimum sizes so a placeholder cannot pass: the ADR and the schema doc must each exceed 1500 characters. Assert the ADR contains `OpenStreetMap` and the schema doc contains `schemaVersion`.

    `docs/frame-budget.md` is created by plan 01-02 and is the one asserted path this plan does not own. Add a comment above that entry stating: created by plan 01-02; this assertion is the deliberate cross-plan presence check; plan 01-03 declares `depends_on: ["01-01", "01-02"]` precisely so this cannot run first. Do NOT create, touch or stub `docs/frame-budget.md` from this plan - a second writer on that path would conflict with 01-02. If it is genuinely absent when this test runs, report the ordering failure rather than creating the file.

    Create `tests/no-google-pipeline.test.ts` implementing the scope decided in this plan's grep_scope_decision block. Do not widen or narrow it; encode the scope as two explicit arrays at the top of the file, `SCANNED_GLOBS` and `ALLOWLIST`, with a comment naming this plan as the decision record.

    Scan set: `CLAUDE.md`, `heat-street-design-doc.md`, `PROJECT.md`, `README.md` if it exists, every `.md` under `docs/`, every `.ts` under `src/` and `tests/`. Allowlist: `docs/adr/0001-map-data-source.md` and `tests/no-google-pipeline.test.ts` itself.

    Comment hygiene: before matching, drop lines whose trimmed form starts with `//`, `*`, `/*` or `>`, so a supersession banner or a code comment restating the prohibition cannot itself trip the gate. Do not use a bare unfiltered count.

    Rule: for every remaining line matching the case-insensitive pattern for Google Maps, Google Earth or Google Street View, the containing file must include the literal string `docs/adr/0001-map-data-source.md` somewhere within six lines of the match. Any match failing that rule is a test failure, and the failure message must print the file path, the line number and the offending line so a future agent knows exactly what to fix.

    Add a discriminating self-check: the test must also assert that the scan set is non-empty and that at least one file was actually read, so a broken glob cannot make the suite trivially green. Add a second assertion that appending a synthetic line containing `Google Maps extraction tool` to a temporary in-memory copy of a scanned file's contents would be reported as a violation by the same matcher function - extract the matcher into an exported helper so this is testable without touching the filesystem.
  </action>
  <verify>
    <automated>npx vitest run tests/docs-present.test.ts tests/no-google-pipeline.test.ts && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <acceptance_criteria>
    - `tests/docs-present.test.ts` asserts all five paths, and carries a comment recording that `docs/frame-budget.md` is owned by plan 01-02 and is why this plan depends on 01-02
    - `tests/no-google-pipeline.test.ts` declares `SCANNED_GLOBS` and `ALLOWLIST` as named top-level constants with a comment citing plan 01-03 as the scope decision record
    - The scan explicitly excludes `.planning`, `node_modules`, `dist` and `.git`
    - The matcher is exported as a helper and is exercised by a negative test proving it flags a synthetic `Google Maps extraction tool` line
    - The test asserts at least one file was scanned
    - `npx vitest run tests/docs-present.test.ts tests/no-google-pipeline.test.ts` exits 0
    - Temporarily removing the ADR pointer from `CLAUDE.md` makes `no-google-pipeline` fail with a message naming `CLAUDE.md` and a line number - confirm once, then restore
    - `npx biome check .` exits 0
  </acceptance_criteria>
  <done>SC5 is automated: the required documents cannot go missing and the Google Maps pipeline cannot be silently reintroduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party geodata to shipped build | Externally licensed data (OSM, DEM) will enter the asset pipeline in Phase 4. Phase 1 sets the contract that governs it. |
| Repository documentation to future agents | Stale documents are read as instructions by autonomous agents; a contradictory doc is an injection vector for wrong behaviour. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Contradicting design docs reintroducing the Google Maps pipeline | mitigate | `tests/no-google-pipeline.test.ts` with an explicitly scoped, comment-stripped grep requiring an ADR pointer within six lines of any match. Failure message names file and line. |
| T-01-08 | Repudiation | Map artifact provenance | mitigate | `docs/schemas/road-graph.v1.md` makes `source.osmSnapshot`, `source.osmExtract`, `source.demSource` and `source.compilerVersion` mandatory, so a shipped map can always be traced to its inputs. Asserted by `tests/road-graph-schema.test.ts`. |
| T-01-09 | Information disclosure | Licence non-compliance on shipped data | mitigate | Mandatory `attribution` block in the schema means the credits screen is generated from the data rather than hand-maintained; `LICENSE-MAPDATA` takes the ODbL safe path on the open Derivative Database question rather than leaving it unresolved. |
| T-01-10 | Tampering | Unknown OSM surface value silently defaulting to tarmac | mitigate | Schema records `surface` as a closed enum whose OSM mapping lives in the Phase 4 compiler and must fail the BUILD on an unmapped value. The fixture test asserts the enum is closed. |
| T-01-SC | Tampering | npm installs | mitigate | No new packages. Schema validation is a hand-written assertion rather than zod, per RESEARCH.md, which also avoids pulling a Phase 6 dependency forward. |
</threat_model>

<verification>
1. `npx vitest run tests/docs-present.test.ts tests/no-google-pipeline.test.ts tests/road-graph-schema.test.ts` - all pass
2. `npx tsc --noEmit` and `npx biome check .` - both exit 0
3. Read `docs/adr/0001-map-data-source.md` end to end and confirm the Supersedes section names all four locations
4. Confirm each of `heat-street-design-doc.md`, `CLAUDE.md`, `.planning/research/STACK.md` contains the ADR path
5. Negative check: temporarily strip the ADR pointer from `CLAUDE.md`, confirm `no-google-pipeline` fails with file and line, then restore
6. Confirm `docs/frame-budget.md` exists and was produced by plan 01-02, not by this plan - `git log --oneline -- docs/frame-budget.md` must not attribute it to an 01-03 commit
</verification>

<success_criteria>
- `docs/adr/0001-map-data-source.md` records the OSM plus open-DEM decision with licence citations, both DEM options with a selection rule, the Google prohibition, the version-pinning consequence, a four-item Supersedes list, and two open questions
- `LICENSE-MAPDATA` covers compiled `*.map.json` under ODbL 1.0
- `docs/schemas/road-graph.v1.md` fully specifies the v1 contract including the closed six-value surface enum and the OSM mapping table
- `fixtures/road-graph.sample.json` is a valid 4-node square loop with exactly one gravel edge and passes the schema test
- `heat-street-design-doc.md`, `CLAUDE.md` and `.planning/research/STACK.md` all point at the ADR
- `tests/no-google-pipeline.test.ts` has a stated scope, strips comment lines, proves it scanned files, and proves its matcher is discriminating
- `tests/docs-present.test.ts` passes with `docs/frame-budget.md` present, and no file in this plan's `files_modified` list is `docs/frame-budget.md`
</success_criteria>

<output>
Create `.planning/phases/01-engine-foundation/01-03-SUMMARY.md` when done.
</output>
