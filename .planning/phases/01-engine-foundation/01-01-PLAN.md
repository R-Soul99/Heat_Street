---
phase: 01-engine-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - .nvmrc
  - tsconfig.json
  - vite.config.ts
  - vitest.config.ts
  - biome.json
  - index.html
  - src/main.ts
  - tests/rapier-smoke.test.ts
autonomous: true
requirements: [VEH-03]

must_haves:
  truths:
    - "npm run build succeeds and emits the Rapier .wasm as a separate hashed asset, not base64-inlined into JS"
    - "npx vitest run can import @dimforge/rapier3d in Node without a resolver error"
    - "npx tsc --noEmit and npx biome check . both exit 0"
    - "Every dependency version is an exact literal with no caret or tilde range"
  artifacts:
    - path: "package.json"
      provides: "Exact-pinned dependency set and the dev/build/test/typecheck/lint scripts"
      contains: "0.185.1"
    - path: "vitest.config.ts"
      provides: "The mainFields resolver fix without which every Rapier test fails to import"
      contains: "mainFields"
    - path: "vite.config.ts"
      provides: "esnext target plus assetsInlineLimit 0 so the wasm stays a separate asset"
      contains: "assetsInlineLimit"
    - path: "tests/rapier-smoke.test.ts"
      provides: "Wave 0 proof that the verification strategy for the whole phase actually runs"
      contains: "@dimforge/rapier3d"
  key_links:
    - from: "tests/rapier-smoke.test.ts"
      to: "@dimforge/rapier3d"
      via: "ESM import resolved through vitest.config.ts ssr.resolve.mainFields"
      pattern: "@dimforge/rapier3d"
    - from: "src/main.ts"
      to: "@dimforge/rapier3d"
      via: "top-level-await WASM ESM import under build.target esnext"
      pattern: "@dimforge/rapier3d"
---

<objective>
Initialise the Heat Street repository as a single npm package with exact-pinned dependencies, a working Vite build, a working Vitest configuration that can import Rapier, and a browser bootstrap that proves the WASM actually executes.

Purpose: This is the phase's de-risking wave. RESEARCH.md's sequencing note states plainly that if the Vitest resolver config is wrong, every Rapier test fails to import — which takes out SC1, the phase's headline criterion. RESEARCH.md also flags in-browser WASM execution as the one MEDIUM-confidence claim in the whole stack. Both are cheap to prove now and expensive to discover in wave 4.

Output: A repo that installs, dev-serves, builds, typechecks, lints and runs tests, with Rapier proven working in both Node and the browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-foundation/01-RESEARCH.md
@.planning/phases/01-engine-foundation/01-PATTERNS.md
@CLAUDE.md

<interfaces>
Contracts established by this plan that waves 2-5 consume. Create exactly these names.

package.json scripts (downstream plans call these by name in their verify commands):
  dev        -> vite
  build      -> vite build
  preview    -> vite preview
  test       -> vitest run
  typecheck  -> tsc --noEmit
  lint       -> biome check .
  lint:fix   -> biome check --write .
  check      -> npm run typecheck && npm run lint && npm run test

Directory layout established here (RESEARCH.md "Recommended Project Structure"):
  src/core/      pure TS       - no `three`, no `document`, no `performance.now()`
  src/physics/   Rapier + core - no `three`
  src/render/    `three` + reads physics/core - never writes sim state
  src/debug/     may import anything - must never affect sim timing
  src/loop.ts    the ONLY place requestAnimationFrame is called
  src/main.ts    composition root
  tests/         Vitest, node environment
  docs/, fixtures/

Import convention: relative paths, no file extension, no path aliases
(for example "./core/sim-clock", "../core/frame-budget"). Named exports only;
`export default` is used solely by vite.config.ts and vitest.config.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the single-package repo and install exact-pinned dependencies</name>
  <files>package.json, package-lock.json, .gitignore, .nvmrc, tsconfig.json, biome.json, index.html</files>
  <read_first>
    - .planning/phases/01-engine-foundation/01-RESEARCH.md - "Standard Stack" (install commands), "Security Domain" (version pinning rationale), "Environment Availability" (Node v24.14.1)
    - .planning/phases/01-engine-foundation/01-PATTERNS.md - "Shared Patterns > Version pinning", "No Reference Pattern Found" (biome.json, index.html, tsconfig.json are all unspecified)
    - .planning/phases/01-engine-foundation/01-CONTEXT.md - D-06 (single package), D-07 (npm), D-08 (Biome in Phase 1)
    - CLAUDE.md - "Development Tools" and "What NOT to Use"
  </read_first>
  <action>
    Create the repo scaffold at the project root. This is a single npm package per D-06, managed with npm per D-07. No monorepo, no workspaces, no sub-packages.

    package.json: name `heat-street`, `"private": true`, `"type": "module"`, `"version": "0.1.0"`, `"engines": { "node": ">=24" }`. Scripts exactly as listed in the interfaces block above.

    Install with EXACT versions. No `^` and no `~` on any entry. RESEARCH.md "Security Domain" requires this specifically for `three` and `@dimforge/rapier3d` because a patch bump can alter solver behaviour and silently invalidate every recorded medal time; apply it uniformly for consistency.
    - dependencies: `three` at `0.185.1`, `@dimforge/rapier3d` at `0.20.0`
    - devDependencies: `vite` at `8.2.2`, `typescript` at `7.0.2`, `@types/three` at `0.185.4`, `vitest` at `5.0.0`, `@biomejs/biome` at `2.5.12`
    Do NOT install `stats-gl` (optional, not required by D-03/D-04), `lil-gui`, `zod`, `ngraph.graph`, `ngraph.path`, `vite-plugin-wasm`, or `vite-plugin-top-level-await`. RESEARCH.md "State of the Art" verified the last two are dead weight under Vite 8.
    Use `npm install --save-exact` and COMMIT `package-lock.json`.

    .nvmrc: the single line `24`. Research verified against Node v24.14.1.

    .gitignore: `node_modules/`, `dist/`, `.vite/`, `coverage/`, `*.local`, `.DS_Store`. Do NOT ignore `package-lock.json`.

    tsconfig.json compilerOptions: `target: "ES2022"`, `lib: ["ES2022", "DOM", "DOM.Iterable"]`, `module: "ESNext"`, `moduleResolution: "bundler"` (this exact value was tsc-verified clean against Rapier in RESEARCH.md), `strict: true`, `noEmit: true`, `verbatimModuleSyntax: true`, `isolatedModules: true`, `skipLibCheck: true`, `resolveJsonModule: true`, `types: ["vite/client"]`. `include: ["src", "tests", "vite.config.ts", "vitest.config.ts"]`. Do NOT set `noUncheckedIndexedAccess` - later plans index `Float64Array` buffers directly and it would force noise-only non-null assertions. Note `verbatimModuleSyntax` means type-only imports must use `import type`.

    biome.json: start from `npx biome init` output, then set `formatter.enabled: true`, `formatter.indentStyle: "space"`, `formatter.indentWidth: 2`, `formatter.lineWidth: 100`, `linter.enabled: true` with the recommended rule set, and `javascript.formatter.quoteStyle: "double"` (every RESEARCH.md excerpt uses double quotes). Keep the `$schema` key that `biome init` emits. Exclude `dist`, `node_modules`, `.planning`, `docs`, `fixtures` from both linting and formatting.

    index.html: minimal HTML5 document with `<html lang="en">`, `<meta charset="utf-8">`, `<meta name="viewport" content="width=device-width, initial-scale=1">`, `<title>Heat Street</title>`. Body contains a single `<canvas id="game"></canvas>` and `<script type="module" src="/src/main.ts"></script>`. An inline `<style>` block resets `body` margin to 0 with `overflow: hidden` and background `#101014`, and sizes `#game` as `display: block; width: 100vw; height: 100vh`. The profiler HUD appends itself to `document.body` at runtime in plan 01-06, so no HUD element is authored here.
  </action>
  <verify>
    <automated>npm ls --depth=0 && node -e "const p=require('./package.json');const all={...p.dependencies,...p.devDependencies};for(const [k,v] of Object.entries(all)){if(/^[^~]/.test(v)===false)throw new Error('unpinned: '+k+' '+v)};for(const [k,v] of Object.entries(all)){if(v.startsWith('^')||v.startsWith('~'))throw new Error('unpinned: '+k+' '+v)};console.log('all pinned')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains exactly `three` = `"0.185.1"` and `@dimforge/rapier3d` = `"0.20.0"` as literal strings with no range prefix
    - `package.json` devDependencies contains `vite` `"8.2.2"`, `typescript` `"7.0.2"`, `@types/three` `"0.185.4"`, `vitest` `"5.0.0"`, `@biomejs/biome` `"2.5.12"`, all literal with no range prefix
    - `package.json` scripts contains all eight keys: `dev`, `build`, `preview`, `test`, `typecheck`, `lint`, `lint:fix`, `check`
    - `package.json` contains `"type": "module"`
    - `package-lock.json` exists on disk and the string `package-lock` does not appear in `.gitignore`
    - `tsconfig.json` compilerOptions.moduleResolution is exactly `"bundler"` and compilerOptions.strict is `true`
    - `tsconfig.json` does not contain the key `noUncheckedIndexedAccess`
    - `package.json` contains none of these strings: `vite-plugin-wasm`, `vite-plugin-top-level-await`, `lil-gui`, `zod`, `stats-gl`
    - `index.html` contains `id="game"` and `src="/src/main.ts"`
    - `npx biome check biome.json` exits 0
  </acceptance_criteria>
  <done>Repo installs cleanly from a fresh `npm ci`, every dependency version is an exact literal, and tsconfig/biome/index.html exist and are valid.</done>
</task>

<task type="auto">
  <name>Task 2: Vite config plus a browser WASM bootstrap that proves Rapier executes</name>
  <files>vite.config.ts, src/main.ts</files>
  <read_first>
    - .planning/phases/01-engine-foundation/01-RESEARCH.md - "Architecture Patterns" (Vite/WASM findings), "Code Examples > Example 2" (world setup, no `await RAPIER.init()`), "State of the Art" (optimizeDeps.exclude verified unnecessary), "Metadata" (in-browser WASM execution is the one MEDIUM-confidence claim)
    - .planning/phases/01-engine-foundation/01-PATTERNS.md - "Pattern Assignments > vite.config.ts"
    - CLAUDE.md - "Rapier package variant", "What NOT to Use"
    - index.html - the script entry it points at (created in Task 1)
  </read_first>
  <action>
    Create `vite.config.ts` exporting `defineConfig` from `vite` with exactly two required settings under `build`: `target: "esnext"` (Rapier's WASM ESM import makes the importing module an async module and requires top-level await) and `assetsInlineLimit: 0` (Vite's default base64-inlines `.wasm` files below the threshold, which bloats the JS bundle and defeats separate asset caching). Also set `server.port` to `5173`.

    Do NOT add `optimizeDeps.exclude` with `@dimforge/rapier3d`. CLAUDE.md lists it as a conditional fallback; RESEARCH.md "State of the Art" verified Vite 8.2.2 pre-bundles Rapier correctly and serves the rewritten wasm import with a 200. Adding it is cargo-culting. Do NOT add `vite-plugin-wasm` or `vite-plugin-top-level-await`.

    Create `src/main.ts` as a TEMPORARY bootstrap whose only job is to prove the WASM executes in a real browser. Plan 01-07 replaces this file entirely with the real composition root, so keep it under about 30 lines and open it with a comment stating that.
    - `import * as RAPIER from "@dimforge/rapier3d"`. Do NOT call `RAPIER.init()` - the non-compat build's `init.js` is literally `export {}`, and calling it is the single most likely copy-paste error from `-compat` tutorials.
    - Construct `new RAPIER.World({ x: 0, y: -9.81, z: 0 })`, create one fixed rigid body with a cuboid collider as ground, create one dynamic rigid body above it with a cuboid collider, call `world.step()` ten times, then read `body.translation().y`.
    - Write the result into the DOM using `textContent` only, never `innerHTML` (RESEARCH.md "Security Domain", DOM XSS mitigation). Append a `<pre>` to `document.body` whose `textContent` includes `RAPIER.version()` and the settled y value.
    - Do not import `three` in this file yet.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const d='dist/assets';const files=fs.readdirSync(d);const wasm=files.filter(f=>f.endsWith('.wasm'));if(wasm.length!==1)throw new Error('expected exactly 1 separate .wasm asset, got '+JSON.stringify(files));for(const f of files.filter(f=>f.endsWith('.js'))){const s=fs.statSync(d+'/'+f).size;if(s>1500000)throw new Error('JS bundle '+f+' is '+s+' bytes - wasm was probably inlined')}console.log('separate wasm asset:',wasm[0])" && npx tsc --noEmit</automated>
    <human-check>Run `npm run dev`, open http://localhost:5173, and confirm the page shows a Rapier version line reading 0.20.0 and a settled y value, with no console errors. This is the only check that proves the WASM executes in a browser rather than merely resolving at build time.</human-check>
  </verify>
  <acceptance_criteria>
    - `vite.config.ts` contains the literal strings `"esnext"` and `assetsInlineLimit` with value `0`
    - `vite.config.ts` does not contain the string `optimizeDeps`
    - `src/main.ts` contains `@dimforge/rapier3d` and does not contain `RAPIER.init(`
    - `src/main.ts` contains `textContent` and does not contain `innerHTML`
    - `npm run build` exits 0 and `dist/assets` contains exactly one `.wasm` file
    - No emitted `.js` file in `dist/assets` exceeds 1.5 MB (proves the 2.02 MB wasm was not base64-inlined)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>`npm run build` produces a separate hashed `.wasm` asset alongside a small JS bundle, and `npm run dev` serves a page that constructs and steps a Rapier world in the browser.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest resolver config and the Rapier import smoke test</name>
  <files>vitest.config.ts, tests/rapier-smoke.test.ts</files>
  <read_first>
    - .planning/phases/01-engine-foundation/01-RESEARCH.md - "Pitfall 3" (the complete verified vitest.config.ts and the exact failure it prevents), "Validation Architecture" (test commands), "Don't Hand-Roll" (takeSnapshot as the equality primitive)
    - .planning/phases/01-engine-foundation/01-PATTERNS.md - "Pattern Assignments > vitest.config.ts" (marked copy-verbatim), "Intra-Phase Pattern Seeding" (this file must land before any other test)
    - vite.config.ts and package.json (created in Tasks 1-2)
  </read_first>
  <action>
    Create `vitest.config.ts` importing `defineConfig` from `vitest/config`. It must set all three of the following, because `@dimforge/rapier3d@0.20.0`'s package.json has a `module` field but no `main` and no `exports` map, so Vitest's Node/SSR resolution path fails with `Failed to resolve entry for package "@dimforge/rapier3d"` under default config:
    - `resolve.mainFields` = `["module", "main"]`
    - `ssr.resolve.mainFields` = `["module", "main"]`
    - `test.server.deps.inline` = `["@dimforge/rapier3d"]`
    Also set `test.environment` to `"node"`. RESEARCH.md "Pitfall 3" gives this exact config as verified working - reproduce it faithfully rather than paraphrasing.

    Create `tests/rapier-smoke.test.ts`. This is the ten-minute task that de-risks the entire phase's verification strategy, so it must fail loudly and immediately if the resolver config is wrong. Assertions:
    1. `RAPIER.version()` returns exactly `"0.20.0"` - catches both a resolver failure and an accidental version drift.
    2. A `RAPIER.World` can be constructed with gravity `{ x: 0, y: -9.81, z: 0 }`, a fixed ground body and a dynamic cuboid body can be created, `world.step()` runs 60 times, and the dynamic body's y translation is finite and strictly less than its starting y.
    3. `world.takeSnapshot()` returns a `Uint8Array` with `length > 0`. This is the primitive every determinism test in plan 01-04 depends on, so prove it exists here.
    4. `world.bodies.len()` returns `2`, and `world.forEachActiveRigidBody` is a callable function. Both are profiler-HUD data sources in plan 01-06.
    Use named imports from `vitest` (`describe`, `expect`, `it`) and `import * as RAPIER from "@dimforge/rapier3d"`.
  </action>
  <verify>
    <automated>npx vitest run tests/rapier-smoke.test.ts && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `mainFields` at least twice (once under `resolve`, once under `ssr.resolve`) and contains the literal `"@dimforge/rapier3d"` inside a `server.deps.inline` array
    - `npx vitest run tests/rapier-smoke.test.ts` exits 0 with 4 or more passing assertions and zero failures
    - The test output contains no occurrence of `Failed to resolve entry for package`
    - Deleting `ssr` from `vitest.config.ts` makes the smoke test fail to import - confirm once during development, then restore the config (this proves the config line is load-bearing, not decorative)
    - `npx biome check .` exits 0 across the whole repo
  </acceptance_criteria>
  <done>Rapier imports and runs under Vitest in Node, `takeSnapshot()` returns non-empty bytes, and the full lint/typecheck/test triple is green on a bare repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local disk | Third-party code enters the build. This is the only externally-sourced material in the phase. |
| Rapier WASM binary to browser runtime | A 2.02 MB compiled binary executes in the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (`three`, `@dimforge/rapier3d`, `vite`, `typescript`, `@types/three`, `vitest`, `@biomejs/biome`) | mitigate | RESEARCH.md "Package Legitimacy Audit" ran slopcheck with explicit `-e npm`: 7 `[OK]`, 1 `[SUS]` (`vitest`) adjudicated a false positive - it is Vite's first-party test runner at `github.com/vitest-dev/vitest`, 483 published versions, MIT. No `[ASSUMED]` and no `[SLOP]` packages remain, so no blocking human checkpoint is required. `npm view <pkg> scripts.postinstall` returned empty for all eight. |
| T-01-01 | Tampering | Dependency drift between install and CI | mitigate | Commit `package-lock.json`; pin every version as an exact literal with no `^`/`~`. Enforced by Task 1's automated verify, which throws on any range prefix. A `three` or `@dimforge/rapier3d` patch bump can alter solver behaviour and silently invalidate recorded medal times. |
| T-01-02 | Tampering | `src/main.ts` DOM write | mitigate | Bootstrap writes via `textContent` only; `innerHTML` is absent and asserted absent in acceptance criteria. |
| T-01-03 | Information disclosure | Source maps / dist output | accept | Offline single-player browser game with no secrets, no backend, no accounts. Nothing to disclose. |
</threat_model>

<verification>
Run in order from a clean checkout:
1. `npm ci` - installs from the committed lockfile with no resolution
2. `npx tsc --noEmit` - exits 0
3. `npx biome check .` - exits 0
4. `npx vitest run` - all tests pass, zero `Failed to resolve entry for package` occurrences
5. `npm run build` - exits 0, `dist/assets` holds exactly one `.wasm` and no `.js` over 1.5 MB
6. Manual: `npm run dev`, load http://localhost:5173, page shows Rapier `0.20.0` and a settled y value with a clean console
</verification>

<success_criteria>
- The repo is a single npm package with all eight scripts wired and `package-lock.json` committed
- Every dependency is pinned to an exact version literal
- Vite builds with `target: "esnext"` and `assetsInlineLimit: 0`, emitting the Rapier WASM as a separate hashed asset
- Vitest imports `@dimforge/rapier3d` in Node and `world.takeSnapshot()` returns non-empty bytes
- `RAPIER.version()` is `"0.20.0"` in both Node and the browser
- No `vite-plugin-wasm`, `vite-plugin-top-level-await`, or `optimizeDeps.exclude` anywhere in the repo
</success_criteria>

<output>
Create `.planning/phases/01-engine-foundation/01-01-SUMMARY.md` when done.
</output>
