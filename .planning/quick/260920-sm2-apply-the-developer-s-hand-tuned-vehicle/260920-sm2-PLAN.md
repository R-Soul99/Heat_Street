---
phase: quick-260920-sm2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/vehicle-tuning.ts
  - src/core/camera-tuning.ts
  - tests/vehicle-tuning.test.ts
  - tests/tuning-persist.test.ts
  - docs/vehicle-tuning-guide.md
autonomous: true
requirements: [QUICK-260920-sm2]

must_haves:
  truths:
    - "defaultTuning() returns the developer's seven hand-tuned vehicle values"
    - "defaultCameraTuning() returns the developer's three hand-tuned damping lambdas"
    - "Every changed field's doc comment states old value, new value, and that it came from a live hand-tuning session (not an automated sweep)"
    - "camera framing/heading/occlusion/chaseFallback are byte-unchanged, with an in-file note explaining WHY framing was excluded"
    - "src/core/surface-tuning.ts is byte-unchanged"
    - "The full test suite is green with no band, threshold or range loosened"
  artifacts:
    - path: "src/core/vehicle-tuning.ts"
      provides: "Seven retuned defaults + extended doc comments"
      contains: "260920-sm2"
    - path: "src/core/camera-tuning.ts"
      provides: "Three retuned damping lambdas + framing-exclusion note"
      contains: "260920-sm2"
    - path: "tests/vehicle-tuning.test.ts"
      provides: "Literal-value assertions matching the new defaults"
      contains: "1390"
    - path: "tests/tuning-persist.test.ts"
      provides: "Hostile-blob fallback assertions matching the new default mass"
      contains: "1390"
    - path: "docs/vehicle-tuning-guide.md"
      provides: "Default column matching the shipped defaults for all ten changed fields"
  key_links:
    - from: "src/core/vehicle-tuning.ts defaultTuning()"
      to: "TUNING_RANGES"
      via: "clampTuning fallback path — every new default must sit inside its own range"
      pattern: "clampTuning"
    - from: "src/core/camera-tuning.ts defaultCameraTuning()"
      to: "CAMERA_TUNING_RANGES.damping"
      via: "clampCameraTuning fallback path"
      pattern: "clampCameraTuning"
---

<objective>
Transcribe the developer's hand-tuned vehicle and camera-damping values —
found live through the `?debug` tuning panel and exported via quick task
260920-l94's Export feature — into `defaultTuning()` and
`defaultCameraTuning()`, then prove the existing regression gates still hold
at the new baseline.

Purpose: `src/core/vehicle-tuning.ts` has been knowingly stale since quick
task 260920-j4d (see STATE.md's open blocker: "the shipped defaults do not
reflect the current feel"). This closes that blocker. The values are already
human-verified by the driving session that produced them — this task is
transcription plus regression-proofing, not a new feel judgement.

Output: ten changed constants, ten extended doc comments, updated test
literals, an updated tuning-guide Default column, and a green `npm run check`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/core/vehicle-tuning.ts
@src/core/camera-tuning.ts
@tests/vehicle-tuning.test.ts
@tests/tuning-persist.test.ts

<values>
<!-- Already diffed against the shipped defaults by the orchestrator. -->
<!-- Do NOT re-derive these. Apply them exactly as written. -->

src/core/vehicle-tuning.ts, defaultTuning():
| Path                                | Old   | New   |
|-------------------------------------|-------|-------|
| chassis.mass                        | 1600  | 1390  |
| chassis.comOffset.y                 | -0.15 | -0.25 |
| wheels.frontSideFriction            | 1.0   | 1.21  |
| drive.engineForcePerRearWheel       | 3650  | 4800  |
| drive.handbrakeRearSideFriction     | 0.01  | 0.005 |
| assists.bodyRollGain                | 0.12  | 0.075 |
| assists.slideCatchGain              | 0.12  | 0.1   |

src/core/camera-tuning.ts, defaultCameraTuning():
| Path                     | Old | New |
|--------------------------|-----|-----|
| damping.positionLambda   | 6.0 | 3.3 |
| damping.headingLambda    | 2.5 | 4.5 |
| damping.framingLambda    | 1.5 | 1.9 |
</values>

<range_check>
<!-- Already read out of the two range tables by the planner. Every new value
     is inside its own range, so clampTuning(defaultTuning()) and
     clampCameraTuning(defaultCameraTuning()) both stay no-ops. NO range edits
     are needed or permitted. Confirm mechanically in Task 3, do not assume. -->

TUNING_RANGES.chassis.mass                    = { min: 800,  max: 2600, step: 10    }  -> 1390 OK
TUNING_RANGES.chassis.comOffset.y             = { min: -0.5, max: 0.1,  step: 0.01  }  -> -0.25 OK
TUNING_RANGES.wheels.frontSideFriction        = { min: 0.2,  max: 1.5,  step: 0.01  }  -> 1.21 OK
TUNING_RANGES.drive.engineForcePerRearWheel   = { min: 1000, max: 10000,step: 100   }  -> 4800 OK
TUNING_RANGES.drive.handbrakeRearSideFriction = { min: 0,    max: 0.05, step: 0.001 }  -> 0.005 OK
TUNING_RANGES.assists.bodyRollGain            = { min: 0,    max: 0.15, step: 0.005 }  -> 0.075 OK
TUNING_RANGES.assists.slideCatchGain          = { min: 0,    max: 0.6,  step: 0.02  }  -> 0.1 OK (step is a
    slider display hint only; `clampNode` clamps to min/max and never snaps to step)
CAMERA_TUNING_RANGES.damping.{position,heading,framing}Lambda
                                              = { min: 0.2,  max: 20,   step: 0.1   }  -> 3.3 / 4.5 / 1.9 OK
</range_check>

<doc_comment_convention>
<!-- The established in-file style for a retuned constant. Copied verbatim from
     the shipped `slideCatchGain` comment so the new entries match it exactly. -->

     * TUNED in the plan 02-10 feel session; supersedes the previous default of
     * 0.1. Minor adjustment alongside the other session changes; raising it
     * further (0.3, tested during the same session's straight-line-instability
     * diagnosis) made the unrelated high-speed spin trigger EARLIER, not
     * later — that instability's actual fix was `rearSideFriction` (see its
     * own doc comment), not this gain, so 0.3 was rejected and 0.12 kept.
</doc_comment_convention>

<test_literals>
<!-- The exact literal-value assertions that break at the new defaults.
     Located by grep; these are the only ones in the suite. -->

tests/vehicle-tuning.test.ts:54   expect(b.chassis.comOffset.y).toBe(-0.15);           -> -0.25
tests/vehicle-tuning.test.ts:64   expect(t.drive.handbrakeRearSideFriction).toBe(0.01); -> 0.005
tests/vehicle-tuning.test.ts:68   expect(t.assists.bodyRollGain).toBe(0.12);            -> 0.075
tests/vehicle-tuning.test.ts:71   expect(t.chassis.mass).toBe(1600);                    -> 1390
tests/tuning-persist.test.ts:68   it("replaces a null mass with the default (1600)…")   -> title says 1390
tests/tuning-persist.test.ts:72   expect(result?.chassis.mass).toBe(1600);              -> 1390
tests/tuning-persist.test.ts:116  expect(result?.chassis.mass).toBe(1600);              -> 1390

tests/camera-tuning.test.ts asserts NO literal damping values — it only checks
`CAMERA_TUNING_RANGES.damping.*.min > 0` (lines 171-173) and a round-trip that
compares against `defaultCameraTuning()` itself (line 194). It needs NO edit.
Confirm this by running it, do not edit it speculatively.
</test_literals>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the seven vehicle defaults and extend their doc comments</name>
  <files>src/core/vehicle-tuning.ts, tests/vehicle-tuning.test.ts, tests/tuning-persist.test.ts</files>
  <action>
In `defaultTuning()`, change exactly the seven values in the `<values>` table
above. Change nothing else in that object literal.

For EACH of the seven fields, EXTEND (never replace, never delete) the existing
doc comment on the matching `VehicleTuning` interface leaf, appending a
paragraph in the `<doc_comment_convention>` style that states: the old value,
the new value, and a `[TUNED live, 260920-sm2]` tag. State plainly that the
source was the developer's own hand-tuning session through the `?debug` panel
(exported as JSON via quick task 260920-l94's Export feature), NOT an automated
sweep — so unlike `rearSideFriction`'s or `engineForcePerRearWheel`'s existing
`[MEASURED]` paragraphs there is no telemetry figure to cite for most of them.
Do not invent a measured number for any field that was feel-tuned. Where the
new value contradicts a prior measured claim, say so explicitly rather than
quietly leaving the old claim standing:
  - `engineForcePerRearWheel` 3650 -> 4800 moves off plan 02-07's 6.52 s 0-60
    figure that landed the D-14 6.0-7.0 s band. Note that the D-14 band is now
    expected to shift and that Task 3 re-runs the accel routine; report the
    new measured 0-60 in the SUMMARY.
  - `bodyRollGain` 0.12 -> 0.075 goes BELOW plan 02-07's 0.08, which was itself
    chosen to keep the roll-assist-stability gate under its 15 deg cutoff.
    Lowering it should only make that gate easier; Task 3 confirms.
  - `handbrakeRearSideFriction` 0.01 -> 0.005 leaves 02-RESEARCH.md's stated
    0.004..0.04 useful band's documented 0.01 exemplar but stays inside the
    band and above the 0.0 spin-out floor. Note the developer wanted a bigger
    handbrake kick-out (STATE.md's 260920-j4d blocker records the request).

Three of the seven currently have only a terse one-line comment and need
converting to a block comment, without losing the existing sentence:
  - `chassis.mass` ("Kilograms. Scales every impulse…")
  - `wheels.frontSideFriction` ("Front-axle lateral grip multiplier…")
  - `chassis.comOffset` carries ONE comment for the whole `{x,y,z}` object and
    has no per-leaf comment at all. Keep the group comment intact and add the
    y-specific note beneath it rather than rewriting the group line.

Also correct the two now-stale summary claims elsewhere in the file:
  - The module header's "Every default value below is `02-RESEARCH.md`'s
    measured Config A, with Config B's deltas already applied" and its "eight
    load-bearing fields" count — append a sentence recording that the
    260920-sm2 session's live hand-tuning supersedes several of those values.
  - `defaultTuning()`'s own doc comment repeats the Config A claim; append the
    same supersession note there.

Then update the seven test literals listed in `<test_literals>` for
`tests/vehicle-tuning.test.ts` and `tests/tuning-persist.test.ts`, including
the `tuning-persist` test TITLE that names 1600 in prose. Update the inline
explanatory comments above the changed `vehicle-tuning.test.ts` assertions
(lines 60-67) so they describe the current value's provenance rather than only
plan 02-10's. Do NOT weaken any assertion (`toBe` stays `toBe`; no
`toBeCloseTo`, no range assertions).

Do not touch `src/core/surface-tuning.ts`, `src/physics/vehicle.ts`,
`src/physics/vehicle-assists.ts`, or `src/debug/tuning-panel.ts`.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npx vitest run tests/vehicle-tuning.test.ts tests/tuning-persist.test.ts tests/tuning-snapshot.test.ts && npx tsc --noEmit && git diff --stat -- src/core/surface-tuning.ts src/physics/ src/debug/ | grep -c . | grep -q '^0$' && echo "MECHANISM FILES UNTOUCHED"</automated>
  </verify>
  <done>`defaultTuning()` returns all seven new values; all three named test files pass; typecheck clean; `git diff` shows zero changes under `src/physics/`, `src/debug/` or `src/core/surface-tuning.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the three camera damping lambdas and record the framing exclusion</name>
  <files>src/core/camera-tuning.ts</files>
  <action>
In `defaultCameraTuning()`, change exactly `damping.positionLambda` 6.0 -> 3.3,
`damping.headingLambda` 2.5 -> 4.5, `damping.framingLambda` 1.5 -> 1.9.

Extend each of the three leaves' doc comments on the `CameraTuning` interface
in the same `[TUNED live, 260920-sm2]` style as Task 1, and REMOVE their now-
false `[ASSUMED]` tags (these three are no longer assumed — they are the
product of a real looking-at-the-screen session). Note the directional read
that each value implies in plain language: a lower `positionLambda` means the
rig follows the car more loosely; a higher `headingLambda` means the camera
tracks a heading change more tightly, which narrows — but does not erase —
D-11's deliberate chopper-pilot lag, so keep D-11's framing in the comment
rather than deleting it.

`defaultCameraTuning()`'s own doc comment claims "Every value below is
`[ASSUMED]`". Correct it to carve out the three damping leaves.

Add a clearly-worded note immediately above the `damping` block inside
`defaultCameraTuning()` recording WHY the rest of the same exported tuning
JSON was deliberately NOT applied: the developer's export also contained new
`framing.*AltitudeM`/`*DistanceM`/`*FovDeg`/`*SpeedMs` values, and applying
them would have broken quick task 260919-cam's documented design invariant —
`lowAltitudeM`/`lowDistanceM` (45:8) and `highAltitudeM`/`highDistanceM`
(90:16) are exact multiples of the same ratio, which is what holds the pitch
CONSTANT at ~79.9 deg across the whole speed range and is asserted directly by
`tests/camera-tuning.test.ts`. The developer chose to keep the shipped framing
geometry. Write this so a future editor who finds the same export file does not
"helpfully" apply the remaining fields without re-reading this decision.

Do NOT change any value under `framing`, `heading`, `occlusion` or
`chaseFallback`. Do NOT change `CAMERA_TUNING_RANGES`. Do NOT touch
`src/core/camera-math.ts` or `src/render/helicopter-camera.ts`.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npx vitest run tests/camera-tuning.test.ts tests/camera-framing.test.ts tests/camera-heading.test.ts tests/camera-skin.test.ts tests/occlusion.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The three lambdas read 3.3/4.5/1.9; all five camera/occlusion test files pass unmodified; the framing-exclusion rationale is present in the file; `git diff src/core/camera-tuning.ts` shows no value change outside the `damping` block.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression sweep and tuning-guide sync</name>
  <files>docs/vehicle-tuning-guide.md</files>
  <action>
First, update `docs/vehicle-tuning-guide.md`'s Default column for all ten
changed fields. Confirmed locations: lines 112 (`mass` 1600), 114
(`comOffset.y` -0.15), 145 (`frontSideFriction` 1.0), 152
(`engineForcePerRearWheel` 3650), 157 (`handbrakeRearSideFriction` 0.01), 168
(`bodyRollGain` 0.12), 171 (`slideCatchGain` 0.12), 225 (`positionLambda` 6.0),
226 (`headingLambda` 2.5), 227 (`framingLambda` 1.5). Also fix the inline
"(default 1.0)" / "(default 0.2)" / "(0.12, raised from plan 02-07's 0.08…)"
prose around lines 298 and 315 where it names a value that just changed.
Append a short `[TUNED live, 260920-sm2]` note to each changed row's
description — do NOT delete the existing measured-history prose in those rows;
it remains the record of how the value got where it was. Line 138's "a 1600 kg
car whose static per-wheel load is already 3924 N" is a quotation of the
`maxSuspensionForce` rationale's original arithmetic: leave the arithmetic but
note that shipped mass is now 1390 kg (static per-wheel load ~3409 N), so the
20000 N ceiling is now roughly 5.9x static load rather than 5x. Leave
`docs/adr/0002-helicopter-camera-go-no-go.md` alone — it is a point-in-time
decision record and names `headingLambda` only, with no value.

Then run the full suite and read the results carefully. The following gates
compose with the changed constants and must be confirmed green, not assumed:
  - `tests/vehicle-telemetry.test.ts` — the six canonical routines AND the
    "roll assist stability" gate (15 deg cutoff, previously 5.42 deg at
    bodyRollGain 0.12; lowering to 0.075 should only improve it).
  - `tests/vehicle-recovery.test.ts` — 260920-j4d's `powerSlideRecoveryRoutine`
    and `highSpeedPulseRoutine` locked bands. These construct tuning from
    `defaultTuning()` then override single fields, so their BASELINE has moved
    under them (mass, engine force, front friction all changed).
  - `tests/surface-telemetry.test.ts` and `tests/surface.test.ts` — the
    six-surface separation gate composes `frontSideFriction` (now 1.21) with
    each surface's `lateralGrip`.
  - `tests/vehicle.test.ts`, `tests/vehicle-scene.test.ts`,
    `tests/tuning-guide-sync.test.ts`, `tests/determinism.test.ts`.

Additionally, assert mechanically (a throwaway node/vitest one-liner is fine,
do not commit a new test file unless it earns its place) that
`clampTuning(defaultTuning())` deep-equals `defaultTuning()` and
`clampCameraTuning(defaultCameraTuning())` deep-equals
`defaultCameraTuning()` — this is what proves no new default fell outside its
own range and silently changed the hostile-blob fallback value.

STOP-AND-REPORT RULE: if any gate fails, diagnose it and report. Do NOT adjust
any of the ten developer-chosen values to force a pass, and do NOT loosen a
band, threshold, cutoff or range. A band that moved because the baseline
legitimately moved (e.g. 0-60 time shifting out of D-14's 6.0-7.0 s window at
4800 N) is a finding to surface to the developer with the measured number, not
something to silently re-anchor in this task.
  </action>
  <verify>
    <automated>cd "D:/Projects 2/Heat Street" && npm run check</automated>
  </verify>
  <done>`npm run check` is green (typecheck + Biome + full vitest) with zero band/threshold/range edits; the clamp-idempotence assertion passes for both tuning modules; the guide's Default column matches the shipped defaults for all ten fields; any gate that shifted is written up in the SUMMARY with its measured value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` -> `parseSavedTuning` / `parseSavedCameraTuning` | A user-editable blob crosses into `world.step()` and `camera.fov` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-sm2-01 | Tampering | `defaultTuning()` as the clamp fallback | mitigate | A new default outside its own `TUNING_RANGES` entry would make `clampTuning` rewrite it on every load, silently shipping a different value than the source reads. Task 3 asserts `clampTuning(defaultTuning())` deep-equals `defaultTuning()`. |
| T-sm2-02 | Denial of Service | `defaultCameraTuning().damping` lambdas | mitigate | A zero/near-zero lambda freezes the rig permanently with no console error. All three new values (3.3/4.5/1.9) sit above the 0.2 range floor; Task 3's clamp-idempotence assertion covers this. |
| T-sm2-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. `package.json` must be byte-unchanged — verify with `git diff --exit-code package.json package-lock.json`. |
</threat_model>

<verification>
1. `npm run check` green.
2. `git diff --stat` touches exactly the five files in `files_modified` — no
   `src/physics/`, no `src/debug/`, no `src/core/surface-tuning.ts`, no
   `src/core/camera-math.ts`, no `src/render/`, no `package.json`.
3. `git diff src/core/camera-tuning.ts` shows changed values only inside the
   `damping` block.
4. Each of the ten changed fields' doc comments contains the old value, the new
   value and a `260920-sm2` tag.
</verification>

<success_criteria>
- `defaultTuning()` and `defaultCameraTuning()` return the developer's ten
  hand-tuned values, exactly as specified — no substitutions, no rounding.
- Every changed field carries an EXTENDED (not replaced) doc comment giving old
  value, new value, provenance (live `?debug` session, not an automated sweep)
  and a `260920-sm2` tag, with no invented telemetry figures.
- The camera framing exclusion and its pitch-invariant rationale are recorded
  in `src/core/camera-tuning.ts` next to `damping`.
- Every existing gate passes with zero loosening; any gate whose measured value
  legitimately moved is reported with its number rather than re-anchored.
- `docs/vehicle-tuning-guide.md`'s Default column no longer lies.
</success_criteria>

<output>
Create `.planning/quick/260920-sm2-apply-the-developer-s-hand-tuned-vehicle/260920-sm2-SUMMARY.md` when done.

The SUMMARY must record: the measured 0-60 time at `engineForcePerRearWheel`
4800 (and whether it still lands in D-14's 6.0-7.0 s band), the roll-assist
stability gate's max tilt at `bodyRollGain` 0.075, and the
`vehicle-recovery.test.ts` band results at the new baseline. It must also note
that STATE.md's open `[Quick 260920-j4d, open]` blocker — "`src/core/vehicle-tuning.ts`
is UNCHANGED in the meantime — do not assume the shipped defaults reflect the
current feel" — is now closed by this task.
</output>
